# 探索主线 V1 S1：UAT 004 部署前置复核与执行确认

> 日期：2026-07-24
>
> 结论：**上一份 UAT 004 部署授权的停写、目标库、保护快照、预检与失败处置前置均仍成立。现明确恢复并确认：允许 QA / 部署执行人按既有授权，仅对 `knowledge_base_uat` 执行一次受控 004 migration。**
>
> 依据：`docs/product/当前运行事实.md`、`docs/architecture/探索主线-V1-S1-Schema004总体稳定审阅与UAT部署授权.md`、`docs/architecture/探索主线-V1-S1-UAT004跨库只读保护快照路径授权.md`。

## 【前置复核：全部通过】

| 前置 | 复核结果 |
|---|---|
| 停写窗口 | 通过。H5 `127.0.0.1:10086` 与 API `127.0.0.1:32146` 前后均无监听；无 H5、API、Vitest、migration、restore、清库或业务写入并行。 |
| 目标库 | 通过。`.env.uat` 已确认指向 `127.0.0.1:3306 / knowledge_base_uat`。 |
| 容器边界 | 通过。仅健康的 `knowledge_base-mysql-1` 暴露 `127.0.0.1:3306`。 |
| 跨库保护快照 | 通过。两个运行库部署前完整快照已在项目外、仅 Administrator 可访问的受限临时目录保存。 |
| 快照只读性 | 通过。脚本只使用容器 Unix socket 与固定只读查询 / mysqldump；两库静态窗口连续两次的 `schema.tsv`、`records.sql`、`manifest.sha256` 逐字节一致。 |
| 凭据与环境隔离 | 通过。工件不含 root 密码、`MYSQL_PWD`、root 用户名、host、port、连接串或容器环境；未修改用户、权限、Compose、端口、环境文件或业务代码。 |
| 004 运行前状态 | 仍须在执行命令前即时复验：UAT `schema_migrations` 精确为 001 / 002 / 003，且 004 目标表、列、索引、外键均不存在。 |
| 日常库保护 | 通过。`knowledge_base` 继续为 schemaVersion 4 既成状态；本次不得连接、迁移、清库、恢复或写入该库。 |

## 【明确执行授权】

授权有效范围仅为：

```text
显式加载 .env.uat
→ 确认 MYSQL_DATABASE = knowledge_base_uat
→ 以 UAT migrator 运行既有 migration runner
→ 在 knowledge_base_uat 执行 004_add_exploration_tracks.sql
→ 立即完成既有部署后验证与 QA 验收
```

此授权不是基础 Contracts、Repository、Application、API、H5、Backup V3、S2 或 S3 的授权。

## 【执行前即时核验】

在实际调用 migration runner 前，执行人必须重新确认：

```text
1. 10086 与 32146 无监听。
2. 无 H5 / API、Vitest、migration、restore、清库或人工业务操作并行。
3. .env.uat 的 MYSQL_HOST / MYSQL_PORT / MYSQL_DATABASE 为 127.0.0.1 / 3306 / knowledge_base_uat。
4. 只读快照受限目录仍可访问，两个部署前 manifest 完整。
5. knowledge_base_uat：schema_migrations 精确为 001 / 002 / 003。
6. knowledge_base_uat：exploration_tracks、items.exploration_track_id、
   exploration_tracks_normalized_name_unique、
   exploration_tracks_active_updated_idx、
   items_exploration_track_created_idx、
   items_exploration_track_fk 均不存在。
```

任一项不成立，自动失效本次授权：停止，不执行 migration，并回报架构师。

## 【执行后验收与失败处置】

严格按 `探索主线-V1-S1-Schema004总体稳定审阅与UAT部署授权.md` 完成既有后验：

```text
UAT schema_migrations = 001 / 002 / 003 / 004，文件名 / checksum 匹配。
information_schema 中表、列、三个索引、外键定义正确。
重跑 migration 幂等成功。
UAT app 用户 DDL 被 ER_TABLEACCESS_DENIED_ERROR 拒绝。
.env.uat API health = knowledge_base_uat / schemaVersion 4。
knowledge_base 部署前后快照完全一致。
knowledge_base_uat 十个既有业务集合与 system_metadata 记录哈希完全一致。
UAT schema 仅允许出现 004 规定的结构变化与 schema_migrations 004 记录。
```

发生预检、DDL、权限、health、快照或比较异常时：

```text
立即停止。
不得执行 DDL 回退、手工修表、删除 schema_migrations、清库、恢复或启动 H5 / API。
保留前后 manifest、schema.tsv、records.sql、schema_migrations、information_schema 与脱敏日志。
流转架构师重新裁决。
```

## 【持续冻结】

```text
基础 Contracts。
S2、S3、Repository、Application、API、H5、Backup V3。
对 knowledge_base 的任何 migration、DDL 回退、清库、恢复或测试业务写入。
对 knowledge_base_uat 执行除本次 004 外的任何 migration、清库、恢复或业务写入。
双写、同步、回填、fallback、浏览器直连 MySQL。
```
