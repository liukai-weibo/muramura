# 平台角色与最小权限管理 V1—切片 5 P1 真实渲染第二次最小修复与复测授权

日期：2026-07-31

状态：已授权第二次最小前端修复及随后完整切片 5 E2E 复测；V1 仍未通过最终验收

## 结论

产品依据 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1真实渲染诊断与第二次最小修复架构裁决.md`，确认真实 Chromium 几何诊断已给出可验证根因，现授权按冻结范围执行第二次最小前端修复，并在工程门通过后执行完整 8 项独立 QA 复测。

本授权替代上一轮针对同一顶部按钮 P1 的 `z-index`、`position` 与 `[disabled]` 存在选择器方案；不改变搜索、分页、权限、写锁、unknown-outcome 或任何后端语义。

## 第一阶段：第二次最小前端修复授权

唯一允许修改：

- `apps/client/src/pages/index/index.scss`
- `tests/platform-administration-h5-flow.test.ts`
- 必要的 QA、状态、授权与验收记录
- 按章程触发时追加 `docs/daily-contributions/2026-07-31.md`

`index.scss` 仅允许：

- 为 `.platform-administration-header > taro-button-core.platform-administration-refresh` 与 `.platform-administration-search > taro-button-core` 增加桌面基础 `width: auto`。
- 保留既有 `display: inline-flex`、内容居中、`box-sizing: border-box` 与 `flex: 0 0 auto`。
- 删除上一轮为这两个管理按钮增加的 `position: relative` 与 `z-index: 1`，不得继续叠加层级。
- 删除会匹配 `disabled="false"` 的 `[disabled]` 属性存在选择器；禁用命中规则只允许使用 `[disabled='true']`。
- 仅在桌面端当前存在且可见 `.platform-administration:not(.platform-administration-hidden)` 时，使对应 `.app-main` 获得 `overflow-y: auto`；其他模块继续保持既有 overflow 语义。
- 保留小屏管理按钮 `width: 100%` 的既有响应式覆盖。

不得使用 `!important` 掩盖错误选择器，不得增加全局 Button 重置、透明覆盖层、负 margin、扩大点击层、JavaScript 点击转发或父容器代理点击。

直接测试只能更新为：

- 断言两个桌面 Taro 管理按钮具有 `width: auto`。
- 断言管理按钮规则不存在 `[disabled]` 属性存在选择器，只接受 `[disabled='true']`。
- 断言不存在针对管理按钮的 `z-index` 补丁。
- 断言可见管理页在桌面具有滚动资格，隐藏管理页及其他模块不被扩大。
- 保持既有事件、请求次数、状态和权限断言不变。

禁止修改 TSX、状态文件、API Client、E2E、harness、DOM、事件、文案、搜索、分页、权限、operationId、目标级写锁或两类 unknown-outcome 语义。

前端完成后须先执行直接 H5 flow 测试、既有状态/API Client 测试、完整 H5 回归、`typecheck`、`build:h5` 与 `git diff --check`。工程门全部通过后才能进入第二阶段。

## 第二阶段：完整独立 QA 复测授权

- `tests/platform-administration-v1-slice5.e2e.test.ts` 与 `tests/helpers/platform-administration-v1-slice5-harness.ts` 只能原样执行，不得修改。
- 必须串行重新执行既有切片 5 E2E 全部 8 项，不得只重跑搜索、清除或刷新场景。
- 禁止 force click、DOM click、坐标偏移、键盘替代刷新、延长 timeout、运行时注入 CSS 或修改测试编排。
- 在真实随机 Schema 6 链路重新采集搜索与刷新按钮的 bounding box、computed width、computed pointer-events、`.app-main` 的 overflow/scrollHeight/clientHeight，以及按钮中心点 `elementsFromPoint`；栈顶必须为目标按钮自身。
- 搜索、清除、刷新均须由真实鼠标完成；搜索和刷新每次操作各只允许新增一条既有管理 GET，`%_=` 必须按字面量提交。
- 完整重做角色管理、会话管理、角色与会话两类 unknown-outcome、503、Abort、响应丢失及显式恢复闭环。
- 继续验证 Schema 5 启动拒绝、同一临时库执行既有 006 后 Schema 6 启动、初始管理员 CLI、旧 Cookie 角色实时刷新、403/404/409、actor 事务内降级、双管理员并发互撤及 owner/Backup 不扩权。
- 临时 database、账号、进程、端口与浏览器 context 必须 finally 为 0；敏感信息扫描通过；`knowledge_base` 与 `knowledge_base_uat` 前后必须分别 `SNAPSHOTS_IDENTICAL`。

## 本轮 root 连接值一次性例外

- 本次完整 8 项 E2E 复测重新独立授权 QA 仅从仓库根未跟踪且已忽略的私有 `.env` 读取 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD`，并只注入当前测试子进程内存。
- 只允许本机 `127.0.0.1:3306` 或等价 `localhost:3306`；任何其他 host 或 port 必须拒绝。
- 禁止读取 `.env.uat`，禁止回显、持久化、写回或把秘密放入命令参数、PowerShell transcript、stdout/stderr、Vitest reporter、截图、报告、临时 manifest、构建产物或 Git。
- 子进程结束后必须清除注入值。本例外只适用于本次第二次修复后的完整 8 项复测，不延续至任何后续 QA、部署或任务。
- root 只可管理本轮正则约束的随机临时 database/账号、核对临时 grants 及建立两个运行库的只读快照；不得对运行库执行 DDL/DML。

## 持续禁止

- 禁止真实执行 006、授予真实管理员、重启或部署服务、写入运行库或进行云端操作。
- 禁止修改 `.env`、`.env.uat`、Docker、MySQL 配置、现有账号/grants、mysql-data 或两个运行库。
- 禁止修改切片 5 E2E/harness 或任何未列入本授权的生产与测试文件。
- 若复测仍需生产注入点、路由、DTO、日志、配置或扩大样式/交互范围，必须立即阻断并回流架构与产品，不得自行处理。

## 验收门

只有前端工程门和切片 5 全部 8 项 E2E 均通过，真实几何及鼠标命中证据符合冻结要求，无新增 P0/P1，临时资源零残留，敏感扫描无泄漏，两个运行库分别 `SNAPSHOTS_IDENTICAL`，才能回产品经理执行平台角色 V1 最终产品验收。

本授权不构成切片 5 QA 通过、V1 最终验收、真实 006、真实管理员授予、运行部署或封板授权。
