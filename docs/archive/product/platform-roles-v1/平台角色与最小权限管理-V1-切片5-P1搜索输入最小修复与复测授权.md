# 平台角色与最小权限管理 V1—切片 5 P1 搜索输入最小修复与复测授权

日期：2026-07-31

状态：已授权最小前端修复及随后完整切片 5 E2E 复测；V1 仍未通过最终验收

## 结论

产品依据 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1搜索输入最小修复架构裁决.md`，授权关闭真实 Chromium 搜索输入导致工作台卸载的 P1。本授权不改变搜索契约、管理能力、后端或运行环境。

## 第一阶段：最小前端修复授权

唯一允许修改：

- `apps/client/src/pages/index/platform-administration.tsx`
- `tests/platform-administration-h5-flow.test.ts`
- 必要的 QA、架构、产品状态及验收记录
- 按章程触发时追加 `docs/daily-contributions/2026-07-31.md`

修复只能：

- 在搜索 `onInput` 事件处理函数内同步读取一次 `event.currentTarget.value`，保存为不可变局部字符串。
- 将该字符串传入既有 `setQueryDraft` updater 和 `acceptPlatformUserQueryDraft`。
- 保证 updater、Promise、timer、effect 或其他异步回调不再持有或读取事件对象。
- 补充同步捕获、输入不提交及既有显式提交边界的直接回归断言。

必须保持：

- 输入只更新 `queryDraft`，不自动提交或发送 HTTP。
- Enter、搜索按钮、清除、分页、刷新、字面量搜索与 80 code unit 边界不变。
- 401/403 卸载、operationId、目标级写锁、旧请求保护和两类 unknown-outcome 不变。

禁止修改 `platform-administration-state.ts`、API Client、样式、导航、其他前端文件、后端各层、Migration、脚本、配置或运行库。

前端修复后须先执行直接 flow 测试、既有状态/API Client 测试、完整 H5 回归、`typecheck`、`build:h5` 和 `git diff --check`。这些验证通过后才能进入第二阶段。

## 第二阶段：切片 5 完整独立 QA 复测授权

- 既有 `tests/platform-administration-v1-slice5.e2e.test.ts` 与 `tests/helpers/platform-administration-v1-slice5-harness.ts` 只能原样执行，不得修改。
- 必须重新执行既有切片 5 E2E 全部 8 项，不得只重跑搜索断言或跳过此前级联阻断场景。
- 真实 Chromium 输入 `%_=` 后工作台、认证上下文和 URL 必须保持；输入阶段新增 HTTP 请求为 0。
- 显式提交后只允许一条冻结 GET，并完成字面量搜索；清除、分页和刷新保持正常。
- 完整重做角色管理、会话管理、角色/会话两类 unknown-outcome、503、Abort、响应丢失及显式恢复闭环。
- 继续验证 Schema 5 启动拒绝、随机临时库 006 后 Schema 6 启动、owner/Backup 不扩权、敏感信息不泄漏、finally 零残留。
- `knowledge_base` 与 `knowledge_base_uat` PRE/POST 必须分别 `SNAPSHOTS_IDENTICAL`，并保持 15 表、Schema 5、平台表 0。

## 本轮 root 连接值一次性例外

- 本次完整 E2E 复测重新独立授权 QA 仅从仓库根未跟踪私有 `.env` 读取 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD`，注入当前切片 5 测试子进程内存。
- 只允许连接本机 `127.0.0.1:3306` 或等价 `localhost:3306`；任何其他 host/port 必须拒绝。
- 禁止读取 `.env.uat`，禁止修改或写回 `.env`，禁止将秘密放入命令参数、PowerShell transcript、stdout/stderr、Vitest reporter、截图、报告、临时 manifest 或 Git。
- 子进程结束后必须清除注入值；本例外只适用于本次切片 5 全部 8 项复测，不自动延续到后续 QA、真实部署或其他任务。
- root 仍只可创建/删除本轮正则约束的随机临时 database/账号、核对 grants 和建立两运行库只读快照；不得对运行库执行 DDL/DML。

## 持续禁止

- 禁止执行真实 006、真实管理员授予、服务重启/部署、运行库写入或云端操作。
- 禁止修改 `.env`、Docker、MySQL 配置、现有账号/grants、mysql-data 或两个运行库。
- 禁止修改切片 5 E2E/harness、其他生产代码或既有测试以规避失败。
- 复测发现需要扩大生产修改时立即阻断并回架构/产品，不得自行修复。

## 验收门

只有前端工程门和切片 5 全部 8 项 E2E 均通过、临时资源零残留、敏感扫描无泄漏、两运行库 `SNAPSHOTS_IDENTICAL` 且无新增 P0/P1，才能回产品经理执行平台角色 V1 最终验收。本授权本身不构成验收或运行部署授权。
