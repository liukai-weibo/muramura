# 架构历史归档

- `sqlite-migration/`：已停用的 IndexedDB / SQLite 主库迁移路线。
- `layout-sprints/`：已完成布局 Sprint 的技术评审。
- `item-repository/`：已关闭的事项 Repository 一致性评审。
- `method-lifecycle/`：已完成的方法生命周期治理评审过程。
- `start-action-snapshot/`：已完成的启动动作快照评审过程。
- `state-contracts/`：已完成的事项状态契约扩展评审过程。
- `item-experience/`：已完成的事项体验补充评审过程。

归档内容只用于追溯，不作为当前实现依据。
