# 在线账户与单用户数据隔离 V0：切片 5 独立临时库 QA 产品编码授权

日期：2026-07-30  
状态：切片 5 QA 通过；产品功能验收通过，等待 Git 独立归档

## 范围

- 建立并执行仅面向随机独立临时数据库、独立账号的自动化 QA 与浏览器验证。
- 覆盖注册/登录/退出、Cookie、全业务隔离、跨用户 404、claim、Backup 409 零写入、unknown-outcome、503 与既有单用户回归。

## 允许修改

- `tests/` 下直接测试和必要测试辅助文件。
- 必要 QA/产品/架构记录及当天贡献记录。

## 硬边界与验收

- 每次测试必须创建随机临时数据库和独立账号，并在 finally 清理；禁止连接或读取 `knowledge_base`、`knowledge_base_uat`、云端运行库。
- 不改生产代码、migration、配置、Docker、部署或任何运行库。
- 产出可复现 QA 报告与零污染证据，之后才可申请 V0 产品最终验收；Tauri、安卓、同步、协作、HTTPS/域名/部署仍不得开始。

## QA 结论（2026-07-30）

功能和独立临时库回归通过：真实 H5/API 端到端 1 文件 3 项、账户 Schema/认证/owner 隔离/claim/Backup 4 文件 11 项、完整 MySQL 回归 13 文件 135 项、H5 去重回归 15 文件 97 项，以及 typecheck、`build:h5`、`git diff --check` 均通过。随机临时数据库、账号、进程、浏览器上下文和 QA 临时目录均零残留，两个运行库前后只读摘要一致。

整体 QA 不通过：本机实际 UAT app/migrator 密码与 Git 跟踪示例值相同，且对应账号存在，构成 P1。另 `knowledge_base_uat` 当前存在但为 0 个 base table，与历史 `schemaVersion=4` 状态不同。未经最小凭据轮换、架构复审、定向安全复测及状态归档，不得进行 V0 最终验收或封板。产品最小修复范围见 `docs/product/在线账户与单用户数据隔离-V0-切片5-P1-UAT凭据轮换与事实盘点授权.md`。

## P1 关闭与产品验收（2026-07-30）

原 P1 已完成受控凭据轮换并通过定向安全复测：公开示例凭据认证失败，新凭据各自仅执行 `SELECT 1` 成功，账号插件和权限未漂移，新秘密在 Git、H5 构建产物和近期日志中零命中。切片 5 整体 QA 现通过，V0 产品功能验收亦通过；最终裁决见 `docs/product/在线账户与单用户数据隔离-V0-最终产品验收与归档裁决.md`。

当前仅等待受控 Git 独立归档，尚不得封板。`knowledge_base_uat` 仍为 0 个 base table，不得作为 UAT 使用，本结论不授权修复其 Schema。
