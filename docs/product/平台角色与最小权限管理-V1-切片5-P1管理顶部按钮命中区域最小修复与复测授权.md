# 平台角色与最小权限管理 V1—切片 5 P1 管理顶部按钮命中区域最小修复与复测授权

日期：2026-07-31

状态：已授权局部样式修复及随后完整切片 5 E2E 复测；V1 仍未通过最终验收

## 结论

产品依据 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1管理顶部按钮命中区域最小修复架构裁决.md`，授权关闭真实 H5“搜索”和“刷新”按钮无法通过指针点击的 P1。本授权只补齐管理页顶部 Taro H5 元素的局部命中盒，不改变 DOM、事件、状态或业务语义。

## 第一阶段：最小前端修复授权

唯一允许修改：

- `apps/client/src/pages/index/index.scss`
- `tests/platform-administration-h5-flow.test.ts`
- 必要的 QA、架构、产品状态及验收记录
- 按章程触发时追加 `docs/daily-contributions/2026-07-31.md`

样式只允许作用于：

- `.platform-administration-header > taro-button-core.platform-administration-refresh`
- `.platform-administration-search > taro-button-core`

允许设置：

- `display: inline-flex` 及现有内容居中。
- `box-sizing: border-box`。
- `flex: 0 0 auto`。
- 仅按钮自身的局部 `position: relative` 与最小必要层级。
- 启用态 `pointer-events: auto` 与既有 pointer cursor。
- `taro-button-core[disabled='true']` 继续 `pointer-events: none`。
- 复用既有尺寸、颜色、边框和响应式布局，不改变可见设计。

直接测试只能补充上述局部 Taro 选择器、启用/禁用命中边界及父容器不得设置 `pointer-events: none` 的断言。

禁止对 header/search 父容器设置 `pointer-events: none`；禁止透明覆盖层、绝对定位扩大点击区、负 margin、坐标补偿、JavaScript/父容器转发点击或全局 Button 重置。禁止修改 TSX、状态文件、API Client、事件绑定、DOM 结构、文案或其他样式区域。

搜索输入、显式提交、清除、分页、固定 20 条、权限、旧请求保护、operationId、写锁和两类 unknown-outcome 必须保持不变。“搜索”和“刷新”每次真实点击只能触发一次既有 GET。

修复后须执行直接 flow 测试、既有状态/API Client 测试、完整 H5 回归、`typecheck`、`build:h5` 和 `git diff --check`。全部通过后才能进入第二阶段。

## 第二阶段：完整独立 QA 复测授权

- `tests/platform-administration-v1-slice5.e2e.test.ts` 与 `tests/helpers/platform-administration-v1-slice5-harness.ts` 只能原样执行，不得修改。
- 必须串行重新执行全部 8 项 E2E，不得只重跑按钮场景。
- 禁止 `force: true`、DOM `click()`、坐标偏移、键盘替代刷新、延长 timeout 或修改编排。
- 真实鼠标点击“搜索”只产生一条 GET，`%_=` 按字面量提交并可真实点击清除。
- 新页面真实鼠标点击“刷新”只产生一条 GET，并完成 role-unknown 显式重读。
- 完整重做角色、会话及两类 unknown-outcome、503、Abort、响应丢失与恢复闭环。
- 继续验证 Schema 5/6 启动门、owner/Backup 不扩权、敏感信息不泄漏、临时资源 finally 零残留。
- `knowledge_base` 与 `knowledge_base_uat` PRE/POST 必须分别 `SNAPSHOTS_IDENTICAL`，并保持 15 表、Schema 5、平台表 0。

## 本轮 root 连接值一次性例外

- 本次完整 8 项 E2E 复测重新独立授权 QA 仅从仓库根未跟踪私有 `.env` 读取 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD`，注入当前测试子进程内存。
- 只允许本机 `127.0.0.1:3306` 或等价 `localhost:3306`；任何其他 host/port 必须拒绝。
- 禁止读取 `.env.uat`、修改或写回 `.env`，禁止把秘密放入命令参数、transcript、stdout/stderr、reporter、截图、报告、manifest 或 Git。
- 子进程结束后必须清除注入值；本例外仅适用于本次按钮修复后的全部 8 项复测，不延续到其他 QA、部署或任务。
- root 仍只可管理本轮正则约束的随机临时 database/账号、核对 grants 和建立两个运行库的只读快照，不得对运行库执行 DDL/DML。

## 持续禁止

- 禁止真实 006、真实管理员授予、服务重启/部署、运行库写入或云端操作。
- 禁止修改 `.env`、Docker、MySQL 配置、现有账号/grants、mysql-data 或两个运行库。
- 禁止修改切片 5 E2E/harness、其他生产代码或既有测试以规避真实指针失败。
- 复测发现需要扩大生产修改时立即阻断并回架构/产品。

## 验收门

只有前端工程门和切片 5 全部 8 项 E2E 均通过、真实指针入口可用、临时资源零残留、敏感扫描无泄漏、两运行库 `SNAPSHOTS_IDENTICAL` 且无新增 P0/P1，才能回产品经理执行平台角色 V1 最终验收。本授权不构成运行部署授权。
