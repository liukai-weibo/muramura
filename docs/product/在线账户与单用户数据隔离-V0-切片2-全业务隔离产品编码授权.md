# 在线账户与单用户数据隔离 V0：切片 2 全业务隔离产品编码授权

日期：2026-07-30  
状态：产品验收通过并归档；切片 3 可开始，切片 4–5 不得开始

## 范围

- 将服务端当前用户 scope 接入事项、方法、复盘、探索主线、回收站、搜索与 Dashboard 的全部既有读取和写入。
- 所有跨用户资源读取、写入、删除、恢复与 ID 猜测统一返回不泄露存在性的既有 404。

## 允许修改

- `packages/contracts/src/index.ts`、`packages/application/src/index.ts`、`packages/storage-mysql/src/*.ts`、`apps/api/src/index.ts`。
- 仅直接测试、架构/QA/产品记录及当天贡献记录。

## 硬边界与验收

- 不新增前端 `userId`、角色、团队、共享、协作或同步能力；不得仅以 UI 过滤代替服务端授权。
- 每个查询与事务内写入必须按当前用户约束；跨用户 ID 不得泄露存在性。
- Backup 与初始归属迁移不在本切片；仅用随机临时库验证全域隔离和既有单用户回归。

## 产品验收与归档（2026-07-30）

验收通过。QA 已确认全部业务 Repository 按服务端当前会话 `userId` 隔离；跨用户读取、写入、关联、删除、恢复与 ID 猜测统一返回不泄露存在性的 404。搜索、Dashboard、回收站和十个业务集合的 owner 归属均已覆盖，包括 `method_tombstones`。

跨用户操作前后十集合深度快照一致；完整测试前后 `knowledge_base`、`knowledge_base_uat`、`system_metadata`、migration 记录与临时资源清单哈希一致，无临时数据库或账号残留。组合回归 4 文件 20 测试、完整 MySQL 回归 13 文件 135 测试、typecheck 与 `git diff --check` 均通过。

本结论不验收 Backup、初始 owner claim、历史 NULL owner 回填、H5 或部署。切片 3 条件授权现生效；切片 4–5 继续不得开始。
