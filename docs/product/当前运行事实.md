**桌面版本 0.1.30 发布（2026-08-31）：** 登录页新增「最近账号」下拉框与「记住密码」：成功登录/注册后自动记录最近账号（最多 8 个、最新在前、去重，本地 localStorage）；桌面端勾选记住密码后密码存系统钥匙串（keyring，新增 `save/read/clear_desktop_login_password` Tauri command），未勾选或取消勾选登录时清除该账号已存密码；浏览器端账号/密码降级 localStorage。签名安装包 `MaruMaru_0.1.30_x64-setup.exe`（6,405,162 字节）+ `.sig` 已上传 GitHub Release v0.1.30（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.30），updater.json 已更新指向 v0.1.30（gh-proxy 实时代理）。**本版起更换 updater 签名密钥**：旧私钥（2026-08-29 生成，密钥 ID FE3D36848EB4D8F2）口令已丢失且穷尽检索+常见口令尝试均无法解密，已备份于 `src-tauri/key-backup-20260831-old/`（不入库）；新密钥对（公钥 ID 49B8A457647EFD7C）已安装为 `src-tauri/private.key(.pub)` 并更新 tauri.conf.json plugins.updater.pubkey，新口令已存 Windows 凭据管理器（cmdkey 条目 MaruMaruTauriSigning）+ `src-tauri/.signing-password.key` + `C:\Users\Administrator\Documents\marumaru-signing-password.key`（全部不入库）。已装 0.1.29 及更早版本的桌面端内置旧公钥、无法校验新签名，需手动下载安装 v0.1.30 一次，此后自动更新恢复正常。

**桌面版本 0.1.29 发布（2026-08-30）：** 今日饮食推荐「个人档案」改为服务端存储 + 安全审计（每用户单行 `user_diet_profiles`，Migration 031 已应用；保存记录审计 daily_diet/update、绑定当前用户、支持多端同步）；配置弹窗异步载入/保存，提示词 await 加载档案。`MaruMaru_0.1.29_x64-setup.exe`（6,404,854 字节）+ .sig 上传 Release v0.1.29（id 379211214，https://github.com/liukai-weibo/muramura/releases/tag/v0.1.29）；updater.json 指向 v0.1.29（gh-proxy 实时代理）。

# 当前运行事实（唯一状态锚点）

**桌面版本 0.1.28 发布（2026-08-29）：** 个人档案身高/体重/年龄输入改用内层 .weui-input 高度+行高+文字居中（消除左上偏移与下方留白）；详情弹窗「个人档案/重新生成」与配置弹窗按钮组统一为 `> button, > taro-button-core` flex:1 均分等高等宽 + inline-flex 居中布局。`MaruMaru_0.1.28_x64-setup.exe`（6,403,381 字节）+ .sig 上传 Release v0.1.28。

**桌面版本 0.1.27 发布（2026-08-29）：** 个人档案弹窗身高/体重/年龄输入居中、详情弹窗「个人档案」与重新生成按钮等高；移除首页卡齿轮角标（个人档案入口改为详情弹窗内）。`MaruMaru_0.1.27_x64-setup.exe`（6,403,137 字节）+ .sig 上传 Release v0.1.27。

**桌面版本 0.1.26 发布（2026-08-29）：** 今日饮食推荐新增「个人档案」配置（身高/体重/年龄/性别/目标/日常活动量/健康状态，可单独配置并拼入提示词，入口为首页卡齿轮角标+详情弹窗按钮，存本机 localStorage「mararumu.daily-diet.profile」）；默认提示词新增盒马等新零售/普通超市常见食材偏好，移除硬编码身高体重。`MaruMaru_0.1.26_x64-setup.exe`（6,407,678 字节）+ .sig 上传 Release v0.1.26。

**桌面版本 0.1.25 发布（2026-08-29）：** 今日饮食推荐 AI 提示词更新为顶级营养师版（身高 178/体重 81、按目前时间分析今日剩余饮食、推荐简单好记、结尾无问句钩子、结论不重复基础信息）。`MaruMaru_0.1.25_x64-setup.exe`（6,405,715 字节）+ .sig 上传 Release v0.1.25。

**桌面版本 0.1.24 发布（2026-08-29）：** 修复用户管理/成员列表「更多」弹窗被列表滚动容器裁剪（删除账号需滚动、层级不足）：菜单改 fixed 顶层底部面板（z-index 999，脱离 overflow 列表），三选项全部直接显示。下载进度含平滑过渡与字节显示（0.1.22 起）。检查更新 endpoint 为 gh-proxy 实时代理 raw（0.1.19 起，规避 jsDelivr 分支缓存）。

**桌面版本 0.1.17 发布（2026-08-29）：** 修复检查更新下载进度条：各分块字节未累计导致进度恒，现按分块累加显示真实百分比；download/install/restart 闭环正常（下载经 gh-proxy.com 镜像加速）。`MaruMaru_0.1.17_x64-setup.exe`（6,403,366 字节）+ .sig 上传 Release v0.1.17（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.17）。

**桌面版本 0.1.15 发布（2026-08-29）：** 检查更新 endpoint 由 raw.githubusercontent.com 改用 jsDelivr CDN（cdn.jsdelivr.net/gh/liukai-weibo/muramura@main/updater.json），解决部分网络不可达导致「无法获取最新版本」；同步审计刷新/卡片高度/审计校验修复。`MaruMaru_0.1.15_x64-setup.exe`（6,403,432 字节）+ .sig 上传 Release v0.1.15（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.15），updater.json 指向 v0.1.15。

**桌面版本 0.1.14 发布（2026-08-29）：** 桌面端新增【检查更新】（「我的-账户」入口，Tauri updater：展示当前/最新版本、下载进度、自动安装重启）；修复自定义卡片 AI 提示指令编辑框高度（此前只显示两行）；刷新生成缓存审计记录为「刷新」并展示可读卡片标题（Migration 029）；修复审计中心「审计事件响应结果无效」（api-client 动作枚举补 refresh）。构建 `MaruMaru_0.1.14_x64-setup.exe`（6,401,711 字节）+ updater 签名 .sig 已上传 GitHub Release v0.1.14（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.14）；updater.json 指向 v0.1.14。tauri cli 升至 ^2.11.0 匹配 tauri crate 2.11.x。
**桌面更新能力（已随桌面版 v0.1.14 发布，Release v0.1.14）：** 桌面端「我的-账户」新增检查更新入口（Tauri updater 插件 + GitHub Release 源；更新弹窗展示当前/最新版本、下载进度并自动安装重启）。updater endpoint=`https://cdn.jsdelivr.net/gh/liukai-weibo/muramura@main/updater.json`（仓库根 updater.json，发布时用 tauri build 生成的 .sig 更新）。构建签名密钥 `src-tauri/private.key`（公钥已写入 tauri.conf.json plugins.updater.pubkey）；私钥及其口令仅存本机/CI secret、不入库（.gitignore 已排除）。tauri cli 已升至 ^2.11.0 以匹配 tauri crate 2.11.x，否则 __TAURI_BUNDLE_TYPE 不匹配导致 updater 不可用。

**桌面版本 0.1.13 发布（2026-08-28）：** AI 时间感知（注入本地真实时间、记录时刻上屏、基于时刻推理，date-utils/ai-context/hono 路由）与长期探索升级（工作台「长期探索」移居首位并默认进入；大项归档/取消归档/归档列表，归档级联 items.archived_at 且行动列表隐���，归档大项不可继续记录——创建/分配均 409，Migration 028 已应用；审计刷新动作审计（刷新记录为 refresh、快照含卡片标题）经 Migration 029 扩展 action CHECK 约束，Schema 29）。修复：自定义 AI 卡片 Markdown 渲染（详情弹窗与首页预览换行，预览截断保留换行结构并剥离孤立 ** 标记）。提交 ed3fd5c/5c315f6/2f34247/f105773/fa82eb4 均直推 main；CI run 33185772092 success（Docker 镜像 ACR/GHCR + 云服务器自动部署）。Tauri 桌面构建 `MaruMaru_0.1.13_x64-setup.exe`（5,358,311 字节）并上传 GitHub Release v0.1.13（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.13）。

**桌面版本 0.1.12 发布（2026-08-27）：** 首页近期状态小结/今日饮食推荐卡片生成优化（打开页面自动生成、点击重新生成后后台持续生成且关闭弹窗不中断、卡片与弹窗流式草稿预览、服务端新增 ephemeral 轻量生成模式跳过搜索/会话/全量手记并精简 system 使卡片类 prompt 由 ~30k 降至 ~10k 首字明显加快）。四问题修复：弹窗重新生成后内容实时同步（refreshTick 重拉）、弹窗长文本滚动容器（max-height 46vh + overflow-y auto）、移除两弹窗「由 AI 基于你的记录自动生成」冗余提示、首页卡片改用 ExperimentalAiMarkdown 渲染（无原始 ** 符号、换行排版正常）。提交 37abe13/4a32754/7f5364a 均直推 main；CI run 33041106982 success（首跑被外部取消后 rerun 成功，Docker 镜像 ACR/GHCR + 云服务器自动部署）。Tauri 桌面构建 `MaruMaru_0.1.12_x64-setup.exe`（5,356,154 字节）并上传 GitHub Release v0.1.12（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.12）。Schema 仍为 27（本轮无新 Migration）。

**桌面版本 0.1.11 发布（2026-08-27）：** 开发直接基于 `main`（此后不再使用 fast-ui 分支）。本轮改动：安全审计中心视觉重构（筛选区布局/导出移至右上角/搜索与时间同行/模块与操作标签圆角/表格 5 列权重并移除目标ID列/快照字段前缀加粗/分页页码按钮）、审计模块精简（移除状态小结/饮食推荐筛选项并清理历史记录）、时区校验修复（mood/meals/daily-summaries/daily-diet/home-ai-cards 五处「不能晚于今天」改为 UTC 上界 utcDatePlusDays(1)，新增 `packages/application/src/date-utils.ts`，凌晨窗口不再误判）、AI 提示词历史状态码补全。提交 5c0dbbd/1c86e12/867a662/b675a5e/8476a0c 均直推 main；CI run 33027227010/33031323571/33032362104 全部 success（Docker 镜像 ACR/GHCR + 云服务器自动部署）。Tauri 桌面构建 `MaruMaru_0.1.11_x64-setup.exe`（5,352,810 字节）并上传 GitHub Release v0.1.11（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.11）。Schema 仍为 27（本轮无新 Migration）。

**桌面版本 0.1.10 封板（2026-08-26）：** 当前工作区分支 `fast-ui`，本轮（安全审计中心快照中文展示与点击弹窗、搜索中文字段等价命中、复盘快照补全与审计精简、近期状态小结/今日饮食推荐弹窗 Markdown 渲染）已通过全量 typecheck、41 个定向测试与 build:h5，四笔提交（e7ee9cf/a917f85/63bb51b/19aec40）推送 fast-ui 后合并 main（8b3bfa9）并推送；Docker 镜像 CI run 32961094090 成功（构建并推送 ACR/GHCR）；Tauri 桌面构建 `MaruMaru_0.1.10_x64-setup.exe`（5.35 MB）并上传 GitHub Release v0.1.10（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.10）。Schema 仍为 27（本轮无新 Migration）。

**fast-ui 完成并合并 main 封板（2026-08-24）：** fast-ui 分支（含奶油线稿视觉基线、情绪模块 Schema 21、一日三餐模块 Schema 22、速记悬浮球整栈移除、情绪/三餐弹窗与日历图例统一暖奶油色板、情绪弹窗移除 Ctrl+Enter 提示与快捷标签）已通过 typecheck、build:h5 与定向测试后合并入 main 并推送，触发 Docker 镜像构建与云服务器部署 CICD；桌面版本号升至 0.1.7，待构建 NSIS 安装包并发布 GitHub Release。


**首页自定义 AI 卡片模块 Schema 26 状态（2026-08-25）：** 源码已新增 `user_home_ai_cards` 表与 Migration `026_add_home_ai_cards.sql`（id CHAR(36) 主键、owner_user_id 外键 users、card_title VARCHAR(50)、ai_prompt TEXT、card_size 默认 `medium`、card_theme 默认 `cream`、refresh_mode 默认 `daily`、sort_index、is_hidden、created_at/updated_at DATETIME(3)，KEY (owner_user_id, sort_index)）与 `user_home_ai_card_caches` 表（每 `(owner_user_id, card_id, cache_date)` 一行 upsert，UNIQUE 幂等，card_id 外键 ON DELETE CASCADE）。Contracts/Application/Hono API：`GET /api/v1/home-ai-cards`（列表，按 sort_index 升序）、`GET /api/v1/home-ai-cards/caches?date=YYYY-MM-DD`（当日缓存）、`POST /api/v1/home-ai-cards`（创建，每用户最多 12 张）、`PUT /api/v1/home-ai-cards/:cardId`（更新）、`DELETE /api/v1/home-ai-cards/:cardId`（删除并级联清缓存）、`PUT /api/v1/home-ai-cards/:cardId/caches/:cacheDate`（缓存 upsert，含长度/枚举/日期校验）。H5 首页：系统卡之下新增 `.home-custom-ai-row` 两列网格（≤720px 单列，行尾虚线「➕ 新增自定义AI卡片」占位）；卡片尺寸 small（紧凑 1 列 ~120px）/medium（标准 1 列 260px）/large（跨两列整行）、主题 cream/green/beige（含深色主题适配）、刷新方式 daily（跨天检测 + 跨午夜定时补触发自动生成）/manual（详情弹窗手动刷新）；卡片点击打开详情弹窗（完整内容、ESC/蒙层关闭、手动刷新），⚙️ 打开编辑弹窗（标题 ≤50、提示词 ≤2000、分段选择），删除需确认。自动生成复用全局 AI 接口 `/experimental/ai-chat/stream-ephemeral`（以卡片自身 aiPrompt 为提示词），成功后写入当日缓存，已有缓存不重复生成，AI 未配置静默降级为空态 + 手动按钮，卡片失败不自动重试。Backup V10（含 `homeAiCards`/`homeAiCardCaches` 导出与恢复校验；恢复 V9 及以下备份时两集合重置为空）。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 26；本地 `knowledge_base` 已于 2026-08-26 执行 026，`/health` 实测为 `ready / knowledge_base / schemaVersion=26`，`user_home_ai_cards`/`user_home_ai_card_caches` 表已建，当前 0 行；API 已以新源码重启，`/api/v1/home-ai-cards` 四条路由经 openapi.json 确认注册（未认证 401）。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema25-before-026-2026-08-26-00-59-44.sql`）。UAT `knowledge_base_uat` 尚未执行 025/026，未迁移不得视为可用。

**安全审计中心二期·枚举扩展 Schema 27 状态（2026-08-26）：** 源码已扩展 `activity_audit_events` 的 module/action 枚举并经 Migration `027_expand_activity_audit_enum.sql` 更新表 CHECK 约束：module 由 5 个扩为 14 个（新增 exploration_track/method/review/daily_summary/daily_diet/home_ai_card/ai_preference/ai_conversation/ai_config），action 由 4 个扩为 11 个（新增 assign/remove/restore/purge/archive/complete/append，AI 配置复用 update/delete）。新增捕获点覆盖：探索轨道全套、事项分配/移除探索轨道、复盘提交、方法应用创建事项、方法回收/恢复、回收站 purge、状态小结/饮食推荐/首页 AI 卡片写入、AI 偏好、AI 会话（消息/新建/改名/归档/恢复/删除/清空）、AI 配置修改/清空（路由层按 actor 补录）、手记绑 AI 会话。审计仍 best-effort 吞错不阻断业务。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 27。**027 已执行：** 本地日常 knowledge_base 于 2026-08-26 经 `pnpm db:migrate` 应用（迁移前逻辑备份 knowledge_base-before-027-2026-08-26-13-34-43.sql，位于项目外 维护相关/运行库备份/），schema_migrations 含 27，/health 实测 ready / knowledge_base / schemaVersion=27；UAT 等其余库仍未迁移，未迁移不得视为可用。

**安全审计中心模块 Schema 25 状态（2026-08-25）：** 源码已新增 `activity_audit_events` 表与 Migration `025_add_activity_audit_events.sql`（id VARCHAR(128) 主键、actor_user_id/actor_username、module_code、action_code、entity_id 可空、snapshot TEXT 可空、risk_level 默认 `normal`、created_at DATETIME(3)；module 限 daily_note/mood/meal/item/search，action 限 create/update/delete/search；actor_user_id 外键 users ON DELETE RESTRICT）、Contracts/Application/Repository（`ActivityAuditRecorder` 与 `ScopedActivityAuditRecorder`，`safeAuditRecord` 永远不阻断业务写入）、Hono API（仅 `platform_admin` 可访问：`GET /api/v1/admin/audit/events` 分页列表 + `GET /api/v1/admin/audit/export` CSV 导出，BOM + `text/csv; charset=utf-8`）与 H5「安全审计中心」页（共享筛选栏按用户 + 功能模块 + 时间 + 操作类型 + 关键词过滤，6 列表格，20 条/页，≤720px 单列；仅平台管理员入口）。审计事件在业务写入成功后追加，覆盖手记（新建/编辑）、情绪（新建/编辑/删除）、三餐（整日保存）、事项（新建/编辑/删除/恢复）、全局搜索（非空关键词）；`risk_level` 预留未启用筛选。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 25；本地 `knowledge_base` 已于 2026-08-25 执行 025，`/health` 实测为 `ready / knowledge_base / schemaVersion=25`，`activity_audit_events` 表已建（两条索引 + 三条约束，当前 0 行）；审计事件在后续业务写入时才开始累积，此前行为不补录。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema24-before-025-2026-08-25-2026-08-25-203258.sql`）。UAT `knowledge_base_uat` 尚未执行 025，未迁移不得视为可用。

**事项流程重构（2026-08-18）：** 当前新业务流程收敛为 `doing`（进行中）与 `reviewed`（已复盘）。新建/快速捕获直接进入进行中，复盘编辑为临时态，提交后进入已复盘；旧状态仅保留历史读取兼容。迁移 019 负责在事务中归一化长期探索关联事项并清理无关联旧未复盘事项，执行前必须完成备份与 UAT 演练。

**本地 H5 局域网开发入口（2026-08-17）：** H5 开发服务器监听 `0.0.0.0:10086`，允许局域网设备经当前开发机的私网 IPv4 访问 `http://<开发机私网IP>:10086/index.html#/pages/mobile/index`。Windows 防火墙入站规则放行 TCP `10086`；API `32146` 与 MySQL `3306` 继续仅监听 `127.0.0.1`，不对局域网开放。H5 `/api` 与 `/health` 仍通过本机同源代理访问 API，并将转发 Origin 规范为允许的 loopback H5 来源，不扩张 API CORS 白名单。

**今日小记 Schema 18 状态（2026-08-12）：** 源码已新增云端 `daily_notes`、日记 AI 专属会话关联、API、Backup V5、Tauri 草稿 Flush 与首页预览卡；本地日常 `knowledge_base` 已执行 `017_add_daily_notes.sql` 并运行于 Schema 17，下一步仅在本地执行 `018_add_daily_note_ai_conversation.sql` 升级到 Schema 18 后验收日记 AI。云端仍待部署日记 Migration 17/18 与新镜像，云端未迁移前不得将今日小记或日记 AI 视为可用能力。

> 最后更新：2026-08-25
>
**情绪模块 Schema 21 状态（2026-08-23）：** 源码已新增 `mood_entries` 表与 Migration `021_add_mood_entries.sql`、Contracts/Application/Hono API（`/api/v1/mood-entries`，GET/POST/PUT/DELETE）、H5 情绪模块（月度情绪日历 + 卡片网格 + 记录/编辑/删除/等级筛选）与 Backup V6（含 `moodEntries` 导出与恢复校验）。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 21；本地 `knowledge_base` 已于 2026-08-23 经用户授权顺序执行 019–021，`/health` 实测为 `ready / knowledge_base / schemaVersion=21`，情绪模块可验收。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema18-before-019-020-021-2026-08-23-05-39-20.sql`）；021 在真实库执行时发现 TEXT 列不能带 DEFAULT，已移除 `tags` 默认值后重跑成功，该修复已入工作区待提交。UAT `knowledge_base_uat` 尚未执行 019–021，未迁移不得视为可用。

**一日三餐模块 Schema 22 状态（2026-08-23）：** 源码已新增 `meal_entries` 表与 Migration `022_add_meal_entries.sql`（每 `(owner_user_id, entry_date, meal_type)` 一行 upsert；content 最长 1000 可空、feeling 1–5 必填）、Contracts/Application/Hono API（`/api/v1/meal-entries`，GET `from/to` 范围读取 + PUT `/{entryDate}` 整日保存）、H5 三餐模块（首页「今天吃了吗」渐变卡 → 弹窗；灵感todo 第五个子 Tab「三餐」：月视图 + 年表格，早/午/晚按 feeling 五档着色）与 Backup V7（含 `mealEntries` 导出与恢复）。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 22；本地 `knowledge_base` 已于 2026-08-23 经用户授权执行 022，`/health` 实测为 `ready / knowledge_base / schemaVersion=22`，三餐模块可验收。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema21-before-022-2026-08-23-17-07-51.sql`）。UAT `knowledge_base_uat` 尚未执行 022，未迁移不得视为可用。

**每日状态小结模块 Schema 23 状态（2026-08-24）：** 源码已新增 `daily_summaries` 表与 Migration `023_add_daily_summaries.sql`（每 `(owner_user_id, entry_date)` 一行 upsert；content 最长 4000，无 DELETE）、Contracts/Application/Hono API（`/api/v1/daily-summaries`，GET `from/to` 范围 + GET `/{entryDate}` + PUT `/{entryDate}`）、H5 首页第三卡「今日状态小结」（奶油线稿同规格渐变卡，预览 2 行截断 → 详情弹窗（日期前后切换 + 空态 + ESC/蒙层关闭 + 手动重新生成））与 Backup V8（含 `dailySummaries` 导出与恢复）。自动生成采用跨天检测 + 页面开着跨午夜定时补触发（无系统级调度器），复用 `/experimental/ai-chat/stream` 同款知识上下文；新增非 admin 端点 `GET /experimental/ai-config-status`（返回 `{configured}`）与 `POST /experimental/ai-chat/stream-ephemeral`（不落 AI 会话历史的流式通道，避免污染用户 AI 对话历史）。AI 知识范围：圈圈助手（主 AI）上下文注入全日期情绪/三餐（不注入每日状态小结）；手记侧 AI 仅注入该手记当日的情绪与三餐（按 entryDate 过滤）。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 23；本地 `knowledge_base` 已于 2026-08-24 执行 023，`/health` 实测为 `ready / knowledge_base / schemaVersion=23`；「今日状态小结」可验收（自动/手动生成经 Playwright 实测通过，AI 已配置时首页自动生成并展示）。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema22-before-023-2026-08-24-17-03-22.sql`）。UAT `knowledge_base_uat` 尚未执行 023，未迁移不得视为可用。

**今日饮食推荐模块 Schema 24 状态（2026-08-25）：** 源码已新增 `daily_diet_recommendations` 表与 Migration `024_add_daily_diet_recommendations.sql`（每 `(owner_user_id, entry_date)` 一行 upsert；content 最长 1200，无 DELETE）、Contracts/Application/Hono API（`/api/v1/daily-diet`，GET `from/to` 范围 + GET `/{entryDate}` + PUT `/{entryDate}`）、H5 首页第三行第二卡「今日饮食推荐」（奶油线稿同规格暖橙渐变卡，预览 2 行截断 → 详情弹窗（固定今日、空态 + ESC/蒙层关闭 + 手动重新生成））与 Backup V9（含 `dailyDietRecommendations` 导出与恢复，恢复时对 V8/V9 备份的 `dailySummaries` 一并恢复）。首页布局由「checkin-row 三卡 + 手记/横幅」改为 2×3：第 1 行手记+主横幅（home-primary-row）、第 2 行情绪+三餐（home-checkin-row 两列）、第 3 行状态小结+饮食推荐（home-dynamic-row 两列），≤720px 单列。生成采用跨天检测 + 午夜定时补触发，复用 `/experimental/ai-chat/stream-ephemeral` 全局 AI 接口（非饮食专用 prompt，可覆盖饮食/作息/行动/心情，1-2 点，150 字内，不编造）；AI 知识范围不变（圈圈助手全日期情绪/三餐，不注入状态小结；手记侧当日情绪/三餐）。`MYSQL_REQUIRED_SCHEMA_VERSION` 已升至 24；本地 `knowledge_base` 已于 2026-08-25 执行 024，`/health` 实测为 `ready / knowledge_base / schemaVersion=24`；「今日饮食推荐」可验收（自动生成 + 手动生成 + 2×3 布局 + 720px 单列经 Playwright 实测通过）。迁移前已做逻辑备份（项目外 `维护相关/运行库备份/schema23-before-024-2026-08-25.sql`）。「今日状态小结」已于 2026-08-25 更名为「近期状态小结」（仅用户可见文案，表 daily_summaries、路由 /daily-summaries、方法名与备份 V8 不变）；「今日饮食推荐」名称不变，其生成先分析最近 7 天饮食/手记/情绪再给饮食建议（buildDietPrompt 纳入最近 7 天手记）。
UAT `knowledge_base_uat` 尚未执行 024，未迁移不得视为可用。

**DeepSeek Harness 当前状态（2026-08-16）：** 已从 Knowledge_Base 活动代码、H5/Tauri 代理、API 路由、管理员配置和启动脚本中撤除；当前运行路径不提供 `/deepseek/` 或 Harness 嵌入能力。历史贡献与归档文档中的 Harness 记录仅代表历史实现，不代表当前可用能力。
> 本文是 Knowledge_Base 当前运行路径、阶段状态与禁止事项的唯一高优先级事实源。

**桌面端 HTTP 临时认证通道（2026-08-12，云端已部署，待人工登录验收）**：在 HTTPS 域名可用前，Tauri 桌面端可在登录或注册成功后从仅允许 `tauri://localhost` 的响应头取得现有数据库 session secret，并以 `Authorization: Bearer` 发送后续 API 请求；不新增 JWT、表、Schema、Migration 或备份格式。Windows 桌面端优先将 token 存入 Tauri WebView 本地存储，并同步尝试写入当前 Windows 用户的 Credential Manager；启动时恢复本地存储 token，若无则回退读取凭据库。登出、401、账号密码修改、软删除和管理员撤销后清除或拒绝既有 token。H5 与非 Tauri 来源继续仅使用既有 HttpOnly Cookie。云端 `http://47.97.69.175:10086` 已实测为 `ready / knowledge_base / schemaVersion=15`，并返回 Tauri 所需的 CORS 来源、`authorization` 请求头许可与 `x-kb-session-token` 暴露头；未认证会话返回 401。该通道仅用于当前 HTTP 联调，Bearer 在 HTTP 上传输不具备 HTTPS 的保密性；域名 HTTPS 可用后应单独评审并恢复 Cookie 跨站会话路径。
> 与历史 M 阶段任务书、封板结论或聊天记录冲突时，以本文为准；历史文档仅描述其写作当时的阶段事实，不得覆盖本文。

**fast-ui 视觉基线封板（2026-08-12）：** 当前工作区分支为 `fast-ui`。展示层第 1-5 阶段已完成产品人工复验并封板：首页行动主控台、奶油线稿视觉、暖棕深色主题、玻璃/兼容显示效果、原生 CSS 微动效及桌面顶轨与侧栏一致性均以当前实现为准。封板范围仅覆盖 `apps/client` 与 Tauri 展示层；不改变 API、Contracts、Application、数据库、备份、状态机或微信端。后续新增或重构页面必须遵循 `docs/design/奶油线稿全局视觉规范.md` 与 `docs/architecture/fast-ui-视觉阶段1-5技术实现基线.md`；任何新的业务入口、数据读取或状态语义仍须按既有产品与架构流程单独立项。

**桌面前端渐进演进基线（2026-08-12）：** 后续研发以 Tauri 桌面端为主要目标。新增桌面模块优先采用 React + Vite 与 Radix UI 的无样式交互原语，并按 `components/ui`、`features`、`layouts` 组织；现有 SCSS 设计 Token 与奶油线稿视觉规范继续为唯一视觉来源。Taro 与 NutUI 仅在现有页面和仍需跨端复用的范围内保留，不为技术替换重写已封板页面，也不同时引入 Base UI 或 shadcn/ui。纯 React + Vite 的迁移按新增模块和明确重构项目逐步推进，任何涉及业务语义、数据契约或微信端的变化仍须单独立项。

## 当前运行路径

```text
H5
→ /api 开发代理
→ loopback Node API（127.0.0.1:32146）
→ Hono OpenAPIHono
→ Application
→ MySQL
```

本机 API 文档（仅 loopback）：`http://127.0.0.1:32146/docs`（Scalar），契约同源 `GET /openapi.json`。

```text
日常运行与隔离 UAT 共用单一 MySQL Docker 容器：127.0.0.1:3306（docker-compose.yml；数据目录 mysql-data）
├─ knowledge_base      日常 H5 / API 数据库
└─ knowledge_base_uat  隔离 UAT / 破坏性测试数据库
H5 验收入口：http://127.0.0.1:10086
H5 仅监听：127.0.0.1:10086
API 仅监听：127.0.0.1:32146
H5 的 /health 与 /api 代理指向：127.0.0.1:32146
API CORS 仅允许本机 H5 来源：http://127.0.0.1:10086、http://localhost:10086、http://[::1]:10086
```

**当前状态汇总（2026-08-07）：** 当前主线为 `main`，本地与 `origin/main` 已同步到提交 `b343068`（完善 AI 流式渲染与桌宠交互体验）。H5 AI 页面已完成流式稳定性与桌宠交互升级：支持分块增量 Markdown、Worker、虚拟消息列表、流式资源清理、Rive 桌宠与 SVG 降级、思考态/欢迎态及本地多状态气泡文案。桌宠和气泡仅属于 H5 展示层，不进入 Provider 请求、消息、摘要、数据库或备份；微信小程序暂不作为本轮桌宠与流式优化的开发和验收目标。

当前本机运行记录中的 API 健康状态为 `ready / knowledge_base / schemaVersion=14`，H5 入口为 `http://127.0.0.1:10086`，API 入口为 `http://127.0.0.1:32146`。Docker 应用镜像 `1.1.5` 与 `latest` 已基于提交 `55deb82` 指向同一 digest：`sha256:b12b65fb480acf07d6dc956afe55c9338cadaf7536143c5e289fdf04ddcdc356`；尚未执行云端容器重建或数据库迁移。

**本机服务重启事实（2026-08-06）：** 当前 API 已按源码直接启动并监听 `127.0.0.1:32146`，健康检查为 `ready / knowledge_base / schemaVersion=14`；H5 已启动并监听 `127.0.0.1:10086`，`/index.html` 返回 200。由于本机当前仅有 Docker Desktop WSL、没有项目使用的 Linux 发行版，未使用统一 `pnpm dev` 编排入口；本次未执行 Migration、数据库写入、Backup restore 或数据清理。

日常 H5 / API 只加载 `.env`，固定连接 `knowledge_base`；UAT API、迁移与破坏性测试只在显式加载 `.env.uat` 后连接 `knowledge_base_uat`。每个 API 进程仅连接一个业务数据库。

**WSL 本机统一启动入口与 Schema 9 运行事实（2026-08-04）：** 已只读确认 `http://127.0.0.1:32146/health` 为 `ready / knowledge_base / schemaVersion=9`，`127.0.0.1:3306`、`32146`、`10086` 均在监听；进程树明确为 `pnpm dev → scripts/local-dev.mjs → API/H5`，证明当前本机可用单一前台命令启动源码服务。新入口只读取日常 `.env`，按 API/H5 白名单传递环境变量，API health 通过后才启动 H5，`Ctrl+C` 仅清理本次 POSIX 进程组；不启动或停止 MySQL，不执行 Migration/Backup restore/管理员授予/owner claim。旧 `kb-init/start/stop.ps1` 已移入 `archive/scripts/windows-local-launcher/`，不再是活动入口；原生 Windows前台编排当前失败关闭，应在 WSL/Linux 运行。本文下方关于“当前仍为 Schema 6/旧 API 未重启”的 2026-08-03 条目保留为历史过程事实，不再代表当前运行状态。本次仅观察到 Schema 9 已生效，不据此猜测或补写其迁移执行过程。

**旧 SQLite Local API 归档与账户客户端接入（2026-08-04）：** `apps/local-api` 及其直接 `local-api-s6a` 测试已分别移动到 `archive/apps/local-api`、`archive/tests/local-api-s6a.test.ts`，并从 pnpm 活动 workspace/lockfile 和活动 TypeScript/Vitest 范围移除；当前后端继续只有 `apps/api`。H5 API Client 已接入本人修改用户名/密码、管理员修改目标用户名/重置密码四条 Schema 9 API，复用单次写入 unknown-outcome、管理员目标与 operationId 校验、成功响应结构验证；当前密码错误通过公开 `AUTH_CURRENT_PASSWORD_INVALID` businessCode 保留为表单错误，真正的会话 401 仍触发认证门。本项只完成客户端逻辑和直接测试，未修改页面组件或样式。

**账户软删除与恢复源码实现完成（2026-08-03）：** Migration 008、Schema 8 启动门、认证与会话拦截、平台 Application/Repository 原子事务、API/OpenAPI/RPC、H5 删除/恢复与结果未知确认已经实现。直接逻辑测试 64 项、随机临时 MySQL 集成测试 30 项、typecheck 与 H5 生产构建已通过；包含删除与创建会话的真实并发验证，测试临时库与账号已清理。账号删除后保留用户、member 与业务数据，同时撤销全部会话和 `platform_admin`；恢复不恢复旧会话或管理员权限。需求、必要规则和注意事项统一记录在 `docs/tasks/2026-08-03-账户软删除与恢复/README.md`。当前源码最低版本已为 Schema 8，但运行中的旧 API 与日常 `knowledge_base` 仍为 Schema 6；未迁移 `knowledge_base` 或 `knowledge_base_uat`，也未重启服务。真实 007/008 执行前仍须通过 `/health` 确认准确目标库。

**开发环境重新初始化完成（2026-08-03）：** 用户已明确当前为开发状态，原有业务与账户数据无需保留。迁移前 `.env` 实际连接的 `knowledge_base` 为 12 张 base table、`schemaVersion=4`，001–004 与当前 Migration 校验和一致，005/006 无半执行残留，十个业务集合与 `system_metadata` 均为 0 行。已在 API/H5 无监听时使用仓库既有 migration runner 顺序执行 005–006；迁移后日常库为 17 张 base table、`schemaVersion=6`，001–006 记录完整，所有业务、账户、会话、角色与审计表仍为 0 行。`knowledge_base_uat` 迁移前后均存在且为 0 张表，结构摘要一致，确认未被触及。日常 API 已在 `127.0.0.1:32146` 启动，实测 `/health` 为 `ready / knowledge_base / schemaVersion=6`；H5 尚未启动，当前未注册账户、未授予平台管理员，也无需执行 owner claim。当前 MySQL 继续由 `my-notebook-mysql-1` 提供并绑定项目内 `mysql-data`；未启动的 `knowledge-base-mysql-1` 与新命名卷 `knowledge_base_mysql_data` 不是当前数据源。下方 2026-07-31 的既有 `admin`、历史数据与备份位置记录已不代表当前运行环境。

**Schema 公共时间字段最小任务书已输出（2026-08-03）：** 架构已依据产品立项完成只读表盘点与方案冻结，见 `docs/architecture/Schema公共时间字段-最小任务书.md`：技术结论为有条件可行；建议新增 Migration `007` 并将 `MYSQL_REQUIRED_SCHEMA_VERSION` 升为 7；对非 `id` 主键表、只追加表、`user_roles` 物理删除与 Backup 不升版等给出有界例外，待产品书面确认后按切片 1（Schema/启动门）与切片 2（Repository/Backup 列）分别签发编码授权。本条**仍不授权编码、不授权对运行库执行 migrate、不授权改 001–006**；当前运行库继续以 Schema 6 为准。

**Schema 公共时间字段原子切片实施完成（2026-08-03，待独立 QA）：** 已按 `docs/product/Schema公共时间字段-原子切片产品编码授权.md` 完成 Migration 007、Schema 7 启动门、全部生产 MySQL 写入/Backup 恢复列与未来新表结构守卫。MySQL 三字段继续作为每表真实列，不以复合标量、JSON 或触发器替代；非 `id` 稳定主键、只追加时间、角色物理删除与 Backup V3 等五项例外保持冻结。随机临时库已证明 16 张业务表均具备稳定主键与 `created_at / updated_at DATETIME(3) NOT NULL`，Schema 6→7 历史回填及部分 DDL 后安全重跑通过；相关迁移/账户/权限回归、typecheck、启动诊断与 `git diff --check` 通过。完整 MySQL 入口仍有 6 个与本切片无关的探索主线旧错误码期望失败；浏览器 E2E 因本机缺少 Playwright Chromium 未执行。测试前后日常库与 UAT 库只读深度摘要完全一致，临时库和账号均为 0。当前源码最低版本已为 Schema 7，但运行中的旧 API 与日常 `knowledge_base` 仍为 `ready / knowledge_base / schemaVersion=6`；在另行授权并执行 007 前不得用当前源码重启 API。当前不授权对 `knowledge_base` 或 `knowledge_base_uat` 执行 007。

**Schema 公共时间字段产品立项与架构评审授权（2026-08-03）：** 产品已签发 `docs/product/Schema公共时间字段-产品立项与架构评审授权.md`：要求全部业务 base table 在产品语义上具备 `id`、`created_at`（创建后不变）、`updated_at`（业务更新与约定删除类写操作刷新）。本条仅授权架构师输出最小任务书；**不授权编码、Migration、手工 DDL、改已应用 001–006、运行库操作、用户改删或超级管理员扩展**。在架构任务书与后续独立产品编码授权之前，Schema/存储/API/H5 均不得修改；当前运行库继续以既有 Schema 6 事实为准。

**本机 API 启动诊断与 Schema 6 运行事实适配实施完成（2026-08-03，待独立 QA）：** 已按 `docs/architecture/本机API启动诊断与Schema6启动器适配-最小任务书.md` 与产品授权的精确范围完成实施：HTTP/H5 脱敏 503 不变，本机标准 API 入口提供安全结构化启动诊断，不自动迁移/重试；PowerShell 不再复制固定 Schema 版本，而是信任已通过 API 唯一版本门的 `ready / knowledge_base` health 并输出其实际 `schemaVersion`。授权见 `docs/product/本机API启动诊断与Schema6启动器适配-产品编码授权.md`。本轮未修改 Migration/Schema/运行库/容器/账户；当前只转独立 QA，不自动构成产品验收或封板。

**当前本地服务与日常库状态（2026-07-31）**：本机日常 `knowledge_base` 已在项目与 Git 外完成迁移前备份，并成功部署既有 001–006 Migration；当前为 17 张 base table、`schemaVersion=6`，账户、owner、平台角色与安全审计 Schema 均已存在。唯一既有 `admin` 同时拥有 `member + platform_admin`，初始管理员审计 1 条。用户此前已以唯一有效会话的明确 userId 完成一次受控 initial owner claim；日常库十个业务集合与 `system_metadata` 在 Schema 6 接入前后摘要一致，initial owner claim 不属于日常启动步骤，不得再次自动执行或猜测其他账号归属。日常 API/H5 当前正常运行，health 为 `ready / knowledge_base / schemaVersion=6`，H5 `/index.html` 返回 200；`admin` 使用既有凭据登录成功，工作台正常挂载，“用户管理”入口可见并可进入。完整逻辑备份与摘要保存在项目和 Git 外 `维护相关/运行库备份/Schema6接入-daily-20260731-163920`。

**当前 UAT 数据库与凭据状态（2026-07-31）：** `knowledge_base_uat` 已使用既有 001–006 Migration 接入为 17 张 base table、`schemaVersion=6`；唯一既有 `admin` 拥有 `member + platform_admin`，初始管理员审计 1 条，真实 UAT health、登录、工作台和用户管理入口功能链此前通过。UAT app/migrator 继续使用已轮换的私有凭据及冻结权限，公开示例凭据不得使用。未复制日常数据，未执行 claim、Backup restore 或破坏性清理。UAT API/H5 当前停止，日常服务正常运行；后续 UAT 必须继续显式加载 `.env.uat` 并使用无痕窗口，不得复用或覆盖日常浏览器会话。

**平台角色与最小权限管理 V1—切片 1 产品验收通过（2026-07-30）：** 产品选择的方案一已落实：`last-platform-admin` 仅作为内部安全不变量，已从公开 Repository 错误联合与不可达实现分支移除；单管理员自撤稳定返回 `self-role-change`，双管理员并发互撤稳定为一次 `revoked`、一次 `actor-not-platform-admin`，最终至少保留一名管理员且仅有一条成功撤销审计。独立 QA 复测 Repository 1 文件 6 项、切片 1 定向 2 文件 8 项、完整 MySQL 13 文件 135 项、typecheck 与 `git diff --check` 均通过；两运行库前后只读深度摘要一致，均保持 15 表、schemaVersion 5、平台两表 0，临时库和账号零残留，未执行真实 006。QA 报告见 `docs/architecture/平台角色与最小权限管理-V1-切片1-独立QA报告.md`，产品验收见 `docs/product/平台角色与最小权限管理-V1-切片1-产品验收.md`。切片 1 现已完成产品验收，但不构成真实 006 Migration、运行库部署或切片 2–5 的授权。另因 `MySqlAuthRepository.createUser` 已依赖 `user_roles`，真实 006 部署前不得用当前源码重启或部署注册链路。后续如继续 V1，须先由架构师冻结切片 2“认证角色刷新与初始管理员 CLI”的精确契约，再由产品经理独立授权。

**平台角色与最小权限管理 V1—切片 2 产品验收通过（2026-07-30）：** `AuthUser.roles`、认证角色实时读取与失败关闭、结构化 `AuthCredentialRecord`、初始管理员 Application/Repository/CLI 及 Schema 6 启动保护已完成独立 QA 与产品验收。定向 6 文件 19 项、CLI 单文件 3 项、完整 MySQL 13 文件 135 项、既有 H5 认证 2 文件 18 项、typecheck、`build:h5` 与 `git diff --check` 均通过；两个运行库前后只读深度摘要一致，保持 15 表、schemaVersion 5、平台表 0，所有额外 `kb_*` 临时库和账号零残留。QA 报告见 `docs/architecture/平台角色与最小权限管理-V1-切片2-独立QA报告.md`，产品验收见 `docs/product/平台角色与最小权限管理-V1-切片2-产品验收.md`。切片 2 现已完成产品验收，但不授权真实 006、服务重启、运行库操作、管理 API/H5 或切片 3–5。当前两个运行库仍是 Schema 5，严禁用切片 1/2 新源码重启、部署或替换当前 API。后续如继续 V1，须先由架构师冻结切片 3“管理 API”的精确契约，再由产品经理独立授权。

**平台角色与最小权限管理 V1—切片 3 产品验收通过（2026-07-30）：** 三条管理路由、Application、Repository 及认证/授权与 body-size 门顺序已完成独立 QA 与产品验收。原 P1 已关闭：无 Cookie和无效 Cookie 的超限管理请求为 401，member 统一为 403，只有管理员通过授权门后才返回 413；四类均含 requestId、`X-Request-Id`、`Cache-Control:no-store` 且安全表零写入。切片 3 定向 4 文件 21 项、完整 MySQL 13 文件 135 项、typecheck 与 `git diff --check` 均通过；两运行库前后只读摘要一致，保持 15 表、Schema 5、平台表 0，临时库和账号零残留。QA 报告见 `docs/architecture/平台角色与最小权限管理-V1-切片3-独立QA报告.md`，产品验收见 `docs/product/平台角色与最小权限管理-V1-切片3-产品验收.md`。切片 3 现已完成产品验收，但不授权真实 006、API 重启/部署、运行库、H5 或切片 4–5。两个运行库仍为 Schema 5，严禁以新源码替换当前 API。后续如继续 V1，须先冻结切片 4 H5 管理入口的产品、设计与前端架构边界，再由产品经理独立授权。

**平台角色与最小权限管理 V1—切片 4 H5 管理入口产品编码授权（2026-07-30）：** 已依据 `docs/design/平台角色与最小权限管理-V1-切片4-H5管理入口设计冻结.md` 冻结 H5 API Client 三接口消费、当前会话角色入口门、管理 401/403 卸载、列表/搜索/分页代次、最终确认 UUID operationId、目标级写锁、旧响应保护，以及角色与会话两类不可抹平的 unknown-outcome。精确状态机、竞态和 mock 前端测试门见 `docs/architecture/平台角色与最小权限管理-V1-切片4-H5前端架构冻结记录.md`，产品编码授权见 `docs/product/平台角色与最小权限管理-V1-切片4-产品编码授权.md`。当前仅允许在五个前端源码文件、四个直接测试文件及必要记录内完成纯前端编码和隔离验证；不授权真实联调、服务启动、真实 006、运行库、后端或切片 5。两个运行库继续保持 15 表、Schema 5、平台表 0。完成后只转切片 4 独立 QA。

**平台角色与最小权限管理 V1—切片 4 独立 QA 不通过（2026-07-31）：** 独立复测的定向 4 文件 38 项、完整 H5 18 文件 125 项、typecheck、`build:h5` 与 `git diff --check` 均通过，但静态与时序审查确认 2 个 P1：写入使在途列表 GET 因事实代次过期后，页面可能永久停留在 `refreshing` 并持续禁用搜索、刷新和分页；多个角色 unknown 目标被整体绑定，单个已由新 GET 返回的目标仍会因另一目标缺失而无法独立解锁。报告见 `docs/architecture/平台角色与最小权限管理-V1-切片4-独立QA报告.md`。当前只允许回流前端在切片 4 已授权文件内做最小修复及直接交错时序测试；不授权产品验收、真实联调、真实 006、服务重启、运行库操作或切片 5。

**平台角色与最小权限管理 V1—切片 4 产品验收通过（2026-07-31）：** read owner 已保证写入只作废并结束当时在途旧读取状态，旧 GET 后到、失败、Abort 或过期不覆盖写入事实且不结束新读取；role-unknown 已按目标独立采用服务端摘要和解锁，未返回目标继续保留旧事实与锁。独立复测的定向 4 文件 42 项、完整 H5 18 文件 129 项、typecheck、`build:h5` 与 `git diff --check` 均通过，未发现新增 P0–P3。QA 报告已在 `docs/architecture/平台角色与最小权限管理-V1-切片4-独立QA报告.md` 保留初次不通过事实并标注由本次结论替代，产品验收见 `docs/product/平台角色与最小权限管理-V1-切片4-产品验收.md`。切片 4 现已完成产品验收，但不授权真实联调、真实 006、服务重启、运行库操作或切片 5。后续如继续 V1，须先由架构师冻结切片 5 随机 Schema 6 临时库全链路隔离 QA，再由产品经理独立授权。

**平台角色与最小权限管理 V1—切片 5 随机 Schema 6 临时库全链路隔离 QA 独立测试授权（2026-07-31）：** 已冻结随机临时 database/app/migrator 账号、随机 loopback API/fault API/H5 端口、隔离 Chromium context、Schema 5 启动拒绝与同库 006 后 Schema 6 启动、初始管理员 CLI、真实 Cookie/H5 管理闭环、错误/并发/unknown-outcome/503、owner 不扩权、敏感扫描、两运行库只读深度快照和 finally 零残留。精确任务书见 `docs/architecture/平台角色与最小权限管理-V1-切片5-随机Schema6临时库全链路隔离QA任务书.md`，产品独立测试授权见 `docs/product/平台角色与最小权限管理-V1-切片5-独立测试授权.md`。当前仅允许新增 `tests/platform-administration-v1-slice5.e2e.test.ts` 与 `tests/helpers/platform-administration-v1-slice5-harness.ts`、必要 QA/状态/验收记录及脱敏临时证据；禁止修改生产代码、既有测试、Migration、脚本、配置或运行库。测试必须显式满足双执行门并只使用随机临时资源；QA 通过不自动授权真实 006、真实管理员授予、服务重启部署或运行库接入。

**平台角色与最小权限管理 V1—切片 5 P1 最小修复完整 QA 复测仍不通过（2026-07-31）：** 搜索输入同步冻结 `event.currentTarget.value` 的修复已由真实 Chromium 证明有效，输入 `%_=` 后工作台保持挂载，原卸载 P1 关闭；但原样完整执行既有 E2E 仍为 1 文件 8 项、5 通过、3 失败。新确认 1 个生产 P1：管理页可见且 `disabled=false` 的“搜索”按钮被 `.platform-administration-search` 父容器拦截指针事件；新的管理页面中“刷新”按钮亦被 `.platform-administration-header` 父容器拦截，导致搜索提交及 unknown-outcome 显式重读不可通过鼠标完成；角色/会话项失败为首项筛选无法清除后的级联。Schema 5/6 启动门、初始管理员 CLI、旧 Cookie 角色刷新、403/404/409、actor 降级、并发互撤、owner/Backup 不扩权及敏感检查已有通过证据；最终 typecheck、`build:h5`、`git diff --check` 通过，项目外临时目录、随机临时库及账号均为 0，finally 的两运行库只读快照比较未报告变化。报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。当前必须再次回流架构师与产品经理冻结最小前端命中区域修复；不得进入 V1 产品最终验收，不授权真实 006、真实管理员授予、服务重启部署或运行库接入。

**平台角色与最小权限管理 V1—切片 5 P1 管理顶部按钮命中区域最小修复与完整复测授权（2026-07-31）：** 真实 H5 的 Taro `Button` 命中元素为 `taro-button-core`，而管理页顶部按钮布局规则主要面向原生 `button`，未完整约束 header/search 内实际元素的显示盒、flex 占位和指针命中层。产品现授权仅在 `apps/client/src/pages/index/index.scss` 为 `.platform-administration-header > taro-button-core.platform-administration-refresh` 与 `.platform-administration-search > taro-button-core` 补齐冻结的局部命中盒及 disabled 规则，并只在 `tests/platform-administration-h5-flow.test.ts` 增加直接断言；不得修改 TSX、状态、API Client、全局按钮、搜索/分页/权限/写锁或 unknown-outcome。前端工程门通过后，QA 必须原样重新执行既有切片 5 E2E/harness 全部 8 项，禁止 force/DOM click、坐标或键盘绕过及修改编排。产品同时重新签发一次性例外：本次复测可仅从根未跟踪私有 `.env` 读取本机 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD` 并只注入当前测试子进程内存，禁止读取 `.env.uat`、回显、持久化或写回，子进程结束后清除，且不得延续。架构裁决见 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1管理顶部按钮命中区域最小修复架构裁决.md`，产品授权见 `docs/product/平台角色与最小权限管理-V1-切片5-P1管理顶部按钮命中区域最小修复与复测授权.md`。切片 5 QA 仍不通过，真实 006、真实管理员授予、服务重启部署、运行库操作和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 管理顶部按钮 P1 完整 QA 复测仍不通过（2026-07-31）：** QA 已按授权重新构建 H5，并在未修改 E2E/harness、未使用 force/DOM click、坐标、键盘替代或延长 timeout 的前提下原样串行执行全部 8 项，结果仍为 5 通过、3 失败。搜索输入卸载问题继续保持关闭；但“搜索”按钮在真实 Chromium 中变为元素位于视口外，滚动后仍无法点击，“刷新”按钮仍被 `.platform-administration-header` 父容器拦截，说明本轮 `inline-flex`、局部层级与 `pointer-events` 修复未关闭 P1；角色/会话失败仍为搜索状态无法清除后的级联。最终 typecheck、`build:h5`、`git diff --check` 通过，项目外临时目录、随机临时库及账号均为 0，finally 的两运行库只读快照比较未报告变化。最新报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。当前须回流架构师基于真实渲染盒与命中元素重新诊断，不得继续盲改 CSS；V1 产品最终验收、真实 006、真实管理员授予、服务重启部署与运行库操作继续禁止。

**平台角色与最小权限管理 V1—切片 5 P1 只读真实渲染诊断完成（2026-07-31，尚未授权再次编码）：** 架构师使用当前 dist、随机 loopback 静态服务、内存 API 读模型和隔离 Chromium 完成不连接 API/MySQL 的真实 Taro DOM/CSS 几何诊断。两个启用按钮实际均为 `disabled="false"`，却被 `[disabled]` 存在选择器计算为 `pointer-events:none`；Taro 基础 `width:100%` 未被上一轮 flex 规则覆盖，使搜索/刷新按钮均宽 954px，挤压 header 至 1181.56px 并把中心推到视口外；桌面 `.app-main` 为 `overflow:clip` 且 `scrollHeight=1923/clientHeight=720`，`scrollIntoView` 无效。仅在内存覆盖按钮 `width:auto`、精确 disabled true 与可见管理页 `overflow-y:auto` 后，两按钮均为 62×40px、中心在视口内、`elementsFromPoint` 栈顶为自身，真实鼠标点击各只新增一条 GET。精确证据与第二次最小修复范围见 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1真实渲染诊断与第二次最小修复架构裁决.md`。当前仅完成诊断与冻结，禁止修改源码或复测；须由产品经理重新授权一个 SCSS、一个直接测试及完整 8 项 E2E，上一轮临时库/凭据例外不得延续。

**平台角色与最小权限管理 V1—切片 5 分页编排与管理控件可访问性诊断完成（2026-07-31，尚未授权修正）：** 第二次真实渲染修复后的完整 E2E 为 8 项中 6 通过、2 失败，搜索输入和顶部按钮两个既有 P1 已关闭。架构复核确认其一为测试编排缺陷：24 用户、固定每页 20 条、`createdAt DESC → userId ASC` 下，早期 `member_a` 在第 2 页，后续测试却停留第 1 页直接等待；最小测试修正必须通过既有完整 username 搜索把每个目标带入当前读模型，并在 unknown 测试开始显式清除前一测试 query/page，不改生产分页。其二为单点生产可访问语义缺口：隔离 Chromium 中目标与 currentUserId 不同、锁分支为 idle，`.platform-more-button` 存在、可见、pointer-events auto、中心命中栈顶为自身，CSS/getByLabel 均为 1；但宿主无 role，CDP 可访问树为 `generic`，`getByRole(button,name)` 为 0。最小生产修正仅为该控件增加 `role='button'`，不改状态或行为。精确诊断与范围见 `docs/architecture/平台角色与最小权限管理-V1-切片5-分页编排与管理控件可访问性诊断裁决.md`。当前禁止修改 E2E/生产源码或复测，须产品重新授权；切片 5 仍不通过。

**平台角色与最小权限管理 V1—切片 5 P1 搜索输入最小修复与完整复测授权（2026-07-31）：** 架构复核确认故障来自 `platform-administration.tsx` 搜索 `onInput` 在 React 状态 updater 内延迟读取已失效的 `event.currentTarget.value`。产品现授权仅在事件回调内同步捕获字符串并传入既有 updater/`acceptPlatformUserQueryDraft`，只可修改该生产文件与 `tests/platform-administration-h5-flow.test.ts`；不得改变输入不自动提交、显式搜索、API/DTO、权限、分页、旧请求保护或两类 unknown-outcome 语义。前端工程门通过后，QA 必须原样重新执行既有切片 5 E2E/harness 全部 8 项，不得修改测试编排。产品同时重新签发一次性例外：本次复测可仅从根未跟踪私有 `.env` 读取本机 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD` 并只注入当前测试子进程内存，禁止读取 `.env.uat`、回显、持久化或写回，子进程结束后清除，且不得自动延续。架构裁决见 `docs/architecture/平台角色与最小权限管理-V1-切片5-P1搜索输入最小修复架构裁决.md`，产品授权见 `docs/product/平台角色与最小权限管理-V1-切片5-P1搜索输入最小修复与复测授权.md`。切片 5 QA 仍不通过，真实 006、真实管理员授予、服务重启部署、运行库操作和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 `.env` 凭据读取一次性执行例外（2026-07-31）：** 用户已明确授权 QA 本轮仅从项目根 `.env` 读取本机 MySQL root 连接值并注入当前测试子进程，用于执行已冻结的随机 Schema 6 临时库 E2E；秘密不得输出、写回、持久化或进入报告，子进程结束后必须清除。该例外不允许读取 `.env.uat`，不允许修改 `.env`、Docker/MySQL 配置、既有账号或运行库，也不改变仅可对两个运行库执行只读深度快照的边界。

**本机启动器适配与 UAT Schema 5 重建授权（2026-07-30，已实施）：** 用户已明确将远端 Git、Docker 新镜像与云端部署暂缓，并授权先处理本机事项。日常启动器及本机启动说明的健康门已从 Schema 4 更新为当前日常 Schema 5，并完成直接测试和真实启动验证；UAT 已按上述受控边界完成空 Schema 5 初始化。Docker README 中的旧镜像版本与 Schema 4 说明继续作为暂缓发布事实保留，不得在新镜像发布前伪改为 Schema 5。

**Docker LAN 发布与环境变量边界（2026-07-29）**：Docker H5 宿主入口可由本机 `.env` 的 `KB_H5_BIND_IP` 与 `KB_H5_HOST_PORT` 控制，默认仍为 `127.0.0.1:10086`；局域网发布时才显式设为 `0.0.0.0`。应用镜像标签、API 宿主端口与 MySQL 宿主端口可由独立 `KB_*` 变量控制，但 API `32146` 与 MySQL `3306` 的监听地址始终固定为 `127.0.0.1`，不得对 LAN 开放。Nginx 内部 `/api` 与 `/health` 已移除浏览器 `Origin` 后再转发给 Node API，避免 LAN H5 同源代理被 API 的 loopback CORS 规则误拒绝；这不扩大 API CORS，也不开放 API 或 MySQL。修复已构建并推送 `dnf:1.1.2` 与覆盖后的 `dnf:latest`，两者均指向 `sha256:be4840aba2df5d4f871a7e1cc4d88677cbbfba8ce0237d578452428cf3f939f0`。虚拟机已完成运行冒烟：`/health` 实测为 `ready / knowledge_base / schemaVersion=4`，LAN H5 入口可访问，用户人工复验捕获与新增无可见延迟；该事实不替代后续完整部署 QA。数据库名、迁移、备份、业务功能与 API/H5 业务代码不在本项范围。

**Docker migrate 首次启动等待修复（2026-07-29，已授权实施）**：云宿主机空 volume 首次 `docker compose up -d` 已复现 `migrate` 在 MySQL 本地 healthcheck 通过后、Docker 网络 TCP 连接短暂未完全就绪时以 `ERROR 2002 ... mysql (115)` 退出；手动重跑 migrate 后 health 已确认 `ready / knowledge_base / schemaVersion=4`。本项仅允许在 `docker/app-entrypoint.sh` 的 `migrate` 路径添加既有 `MYSQL_HOST`、`MYSQL_PORT`、root 连接信息的 TCP 查询等待：每秒一次、最多 30 秒，成功后才执行既有账号收敛及 migration，超时明确失败。禁止修改 `docker-compose.yml`、Dockerfile、MySQL 镜像、migration、账号权限语义、数据库、备份、API、H5 或业务代码；不得自动删除、重建或清空 volume。产品授权见 `docs/product/Docker-migrate首次启动等待修复产品授权.md`，修复后须完成脚本验证、镜像重建与隔离宿主机复测。

**Docker 交付配置位置（2026-07-29）**：用于新机器交付的唯一配置目录已迁至 `C:\Users\Administrator\Desktop\mikey\维护相关\新机器需要的配置`，其中仅保留交付用 `docker-compose.yml` 与本机 `.env`；后续任何 Compose 相关交付调整必须优先同步至此目录，并与仓库根 `Knowledge_Base/docker-compose.yml` 保持一致。日常项目根 `.env` 现显式包含 `KB_APP_IMAGE_TAG`、`KB_H5_BIND_IP`、`KB_H5_HOST_PORT`、`KB_API_HOST_PORT`、`KB_MYSQL_HOST_PORT`，默认保持 H5 回环入口；`.env.example` 与日常 `.env` 的变量结构一致，连接池默认值为 10。真实 `.env` 密码不得提交、复制或输出。

**Schema 与测试隔离现场状态（2026-07-24）**：探索主线 S1 的正式 `004_add_exploration_tracks.sql` 已因历史 `tests/mysql-m1.integration.test.ts` 直接对 `.env` 的 `knowledge_base` 调用 migration runner 而实际执行；`knowledge_base` 当前 `schemaVersion = 4`。受控 UAT 004 部署已实际完成，`knowledge_base_uat` 当前亦为 `schemaVersion = 4`，且 004 的 migration record、checksum、表、列、索引、外键、幂等与 app DML-only 均已验证。UAT 004 双口径数据一致性后验已通过：冻结的离线 Profile 已验证为仅接受 `knowledge_base_uat` 的精确 v3 → v4 / 004 Schema 演进；受限真实快照复核确认 Items 八个既有字段 20 行逐行一致、新列全 NULL、`exploration_tracks` 为空、其余九个既有集合与 `system_metadata` 一致。完整 Items dump hash 的差异被保留为新增可空列的导出表示变化。禁止对任一运行库执行 DDL 回退、删除 migration record 或手工修表。H5 / API 可解除因 UAT 004 后验而施加的停止要求，但启动日常 API 前必须停止旧进程、显式加载 `.env`，并以 `/health` 确认 `database = knowledge_base`。

**P1 测试隔离闭合记录（2026-07-24）**：历史 `mysql-m2a.integration.test.ts` 已迁出 `.env` 的业务 database，改用随机 `kb_m2a_*` 临时 database、独立 app / migrator 用户，并在 finally 清理。QA 在 H5 `10086` 和 API `32146` 均无监听的停写窗口内，显式加载 `.env` 执行完整 `test:mysql:integration`（12 files / 110 tests passed）；对 `knowledge_base` 与 `knowledge_base_uat` 的 `schema_migrations`、十个业务集合、稳定排序全量内容 SHA-256、完整记录及 `system_metadata` 前后深度快照比较结果为 `SNAPSHOTS_IDENTICAL`。P1 集成测试零污染门已关闭。此前 `knowledge_base.items 459 → 534` 的历史增量仍无法可靠归因，保留为事实，禁止清理或改写。

`http://192.168.128.1:10086` 不属于本机可路由验收入口，禁止用于本项目 H5 验收。

## 当前数据源事实

```text
MySQL = 当前 H5 唯一运行主库
IndexedDB = 不参与当前 H5 运行时业务读写；保留为旧本地资产
SQLite = 实验 / 测试资产
```

禁止把 IndexedDB 表述为“当前唯一运行主库”。

## 当前阶段

**V1.1 阶段性封板（2026-07-27）**：本次仅对截至当前工作区的已授权、已实现前端体验与探索主线交互改动执行阶段性归档和 Git 快照，不等同于项目整体完成，也不改变已封板的探索主线 V1 历史事实。已纳入本次快照的范围包括：探索事实刷新、改名低摩擦保存、历史/已放弃历史切换、事项状态迁移后的真实定位、主线左侧九条分页及已授权的局部视觉密度调整；工程验证为定向前端测试 2 文件 22 用例、`typecheck`、`build:h5` 与 `git diff --check` 均通过。`build:h5` 保留既有入口体积和 webpack 缓存警告，未阻断构建。待架构评审的“本机迁移与一键启动”能力不属于本次封板，仍不得实施；任何新需求不得混入本次阶段性封板。

**新建事项标题最多 20 字（2026-07-27，已授权实施）**：架构已确认最小方案有条件可行，产品编码授权见 `docs/product/事项池-新建事项标题最多20字-产品编码授权.md`。所有既有新建事项入口统一限制标题最多 20 个用户可见 Unicode 字素簇，包含“加入想试试”“加入以后再说”“长期探索内加入想试试”“用此方法开始行动”及其他既有 `POST /api/v1/items` 创建路径。前端必须阻止继续输入并显示计数；Application 与三条实际 Repository 写入链必须在 INSERT 前拒绝超限，API 复用既有 `400 VALIDATION_FAILED + requestId`。既有超过限制的事项只读兼容保留，不迁移、不截断。本项禁止新增 DTO、路由、Contracts、Migration、数据库结构、备份格式、配置或业务语义；未经 QA 与产品验收不得宣称完成。

```text
阶段：探索主线 V1 已于 2026-07-26 完成产品最终验收、归档与封板；归档提交为 `d1f2381 feat(exploration): archive V1 MySQL workflow`。已封板范围不得插入新需求。
前置：MySQL 快速接入 Sprint 已于 2026-07-24 完成集中 UAT 并正式封板
```

**V1.1.1 前端体验热修复（2026-07-27，独立于已封板 V1）**：已定位探索主线左侧主线切换的轻微卡顿。原因是 V1.1 阶段快照 `ac8d50f` 引入的逐项 `getComputedStyle`、canvas `measureText` 与 `ResizeObserver` 在详情历史批量挂载/卸载时触发布局测量与重复渲染；不是 API、MySQL 或业务数据问题。现书面授权仅修复该前端渲染性能与由此引入的标题截断展示回归：允许修改 `apps/client/src/pages/index/exploration-prototype.tsx`、`apps/client/src/pages/index/exploration-session-state.ts`、`apps/client/src/pages/index/index.scss` 及其直接前端测试、必要 QA/产品记录和当天贡献记录。应移除逐项 canvas/observer 截断链，使用既有 CSS 截断；保留模块状态、旧详情后台更新、Abort、旧请求保护与 unknown-outcome 读写边界。禁止修改 API、Application、Repository、Contracts、migration、配置、数据库、业务规则、路由、DTO 或任何运行库；不得缓存或预取全部历史。验收仅覆盖必要切换体验与直接工程验证，用户最终人工复验；本热修复未经 QA 与产品验收不得宣称 V1.1 完成，也不改变 V1 封板事实。

**V1.1.2 探索事实同步热修复（2026-07-27，独立于已封板 V1）**：现书面授权修复已成功写入后探索主线仍展示旧读模型的问题。仅允许修改 `apps/client/src/pages/index/index.tsx`、`apps/client/src/pages/index/exploration-prototype.tsx` 及其直接前端测试、必要 QA/产品记录和当天贡献记录。成功完成事项通用状态迁移（含想试试/以后再说、已开始/已暂停等）、调整主线或移除主线后，已挂载的探索模块必须后台重读活跃主线列表与当前选中主线历史；保留旧内容和局部更新提示，失败不得伪装为成功或空态，unknown-outcome 不触发自动重读。不得新增接口、字段、状态、关系、缓存、预取、API/Application/Repository/Contracts/migration/配置/数据库修改或改变任何状态迁移语义。验收覆盖上述成功写入同步、快速连续操作的旧请求保护，以及直接工程验证；用户最终人工复验。

**V1.1.2 同步实现缺口修正（2026-07-27）**：已确认当前父页在事项状态迁移成功后递增探索事实版本，但探索模块仅重读主线列表；当前选中主线 ID 不变时未重读其历史，造成“想试试 ↔ 以后再说、已开始 ↔ 已暂停”须切换事项才更新的回归。该项属于上述既有授权的直接修正：版本变化后必须在列表真实读取成功后，对仍选中的当前主线显式重读历史；失败、Abort、503 或结果未知时保留旧历史和局部错误态，不自动重试或降级为空。直接测试须覆盖两组互相切换、旧请求保护与不自动重试；允许文件范围不扩展。

**探索主线 V1.1.5 子项改名低摩擦交互已授权实施（2026-07-27）**：架构轻量评审结论为“有条件可行”，产品现书面授权仅复用既有冻结 `PATCH /exploration-tracks/:id` 改名契约。点击“改名”进入编辑态，移除“取消”，仅保留“保存”；保存按钮和编辑区外 pointer 点击必须调用同一单次保存函数。编辑区外首次点击主线切换、导航或其他上下文控件时，必须被消费：仅保存当前冻结主线，不执行切换或原点击动作，也不得在成功后自动补执行；用户成功后需再次点击目标入口。保存失败、503、Abort、响应丢失或结果未知时，保留编辑态和草稿，不自动重试，失败后仅保存按钮或显式重读可继续。实现必须绑定 `editingTrackId` 与会话序号，旧响应不得影响新上下文；禁止 timer、单纯 blur、窗口关闭或卸载自动写入。仅允许修改 `apps/client/src/pages/index/exploration-prototype.tsx`、直接前端测试、必要 QA/产品记录与当天贡献记录；禁止修改 api-client、index.tsx、API、DTO、Application、Repository、Contracts、样式、migration、配置、数据库、备份或其他业务语义。未经 QA 与产品验收不得归档。

**探索主线 V1.1.6 历史列表合并切换已授权实施（2026-07-27）**：现有 `history.history` 与 `history.abandonedHistory` 读模型不变，不新增任何读取或写入能力。右侧原“探索历史”标题行右侧增加“查看已放弃记录”切换入口；两者共用同一个历史列表区域，默认显示探索历史，切换后只显示已放弃记录，切回后只显示探索历史。当前主线切换时重置为默认探索历史；任一列表为空时在同一列表区域显示相应空态。列表中的“查看”继续调用既有定位行为；当前关联事项、录入区、已删除主线只读展示及 unknown-outcome 边界不变。仅允许修改 `apps/client/src/pages/index/exploration-prototype.tsx`、`apps/client/src/pages/index/index.scss`、直接前端测试、必要 QA/产品记录与当天贡献记录；禁止 API、api-client、index.tsx、Application、Repository、Contracts、数据库、状态机、迁移、配置、备份、关系或任何写入语义改动。

**事项池 V1.1.7 状态迁移后定位已授权实施（2026-07-27，已扩展）**：修复当前事项在“想试试 ↔ 以后再说”“已开始 ↔ 已暂停”“以后再说 → 已放弃”“已暂停 → 已放弃”及“已放弃 → 想试试”七个成功迁移方向后仍停留在旧筛选的问题。进入已放弃须切换至“更多状态 → 已放弃”列表；从已放弃恢复则须切换至“想试试”列表，均选中该事项。仅在既有状态写入已由服务端明确成功、随后真实刷新结果包含同一事项时，左侧事项池才切换到目标状态筛选、计算该事项所在页并选中它；不得靠本地乐观状态或旧列表推断。写入失败、Abort、503、结果未知或刷新未确认时，保持原筛选、分页和选中，不自动重试或伪造定位。保留既有草稿离开保护及探索事实刷新。仅允许修改 `apps/client/src/pages/index/index.tsx`、直接前端测试、必要 QA/产品记录与当天贡献记录；禁止 API、Application、Repository、Contracts、数据库、状态机、迁移、配置、备份、探索主线关系或写入语义修改。

**本机迁移与一键启动能力（2026-07-27，已授权实施）**：架构已确认最小方案有条件可行，产品编码授权见 `docs/product/本机迁移与一键启动-产品编码授权.md`。仅允许实现 `scripts/kb-init.ps1`、`scripts/kb-start.ps1`、`scripts/kb-stop.ps1`、必要启动文档、直接脚本测试、QA 记录与当天贡献记录。初始化只在 `.env` 不存在时由 `.env.example` 生成本机配置，默认仅启动日常 `knowledge_base`，并以 `/health` 确认 `ready / knowledge_base / schemaVersion=4`；迁移仅用于新机器空库初始化。个人数据只允许由用户通过命名参数显式提供 Backup V3 恢复，禁止复制 `mysql-data`、实际 `.env`、密码或任何个人运行数据。若 32146 已有 UAT 或未知 API，启动器必须拒绝，不得停止、切换或覆盖。不得扩展为全栈 Docker 化，不得修改 compose、端口、API/H5 业务能力、Contracts、Application、Repository、备份格式、数据库结构、migration、远程访问或本机回环边界；未经 QA 与产品验收不得宣称该能力完成。

**全栈 Docker Compose 一键启动（2026-07-27，已授权实施）**：架构已确认最小方案有条件可行，产品编码授权见 `docs/product/全栈Docker-Compose一键启动-产品编码授权.md`。仅允许在 Dockerfile、docker-compose.yml、nginx.conf、.dockerignore、新增 `docker/app-entrypoint.sh`、必要运行文档、直接 Docker 测试、QA 记录与当天贡献记录内实施。推荐拓扑固定为：官方 `mysql:8.4` 加未提交本机命名 volume；复用应用镜像的一次性 `migrate` 服务仅连接 `knowledge_base`；同一 `app` 镜像内运行 Node API、H5 静态站点和内部 Nginx，Nginx 仅在容器内转发 `/api` 与 `/health` 到 `127.0.0.1:32146`。宿主机入口固定为回环 `127.0.0.1:10086`、`127.0.0.1:32146`、`127.0.0.1:3306`，保持既有入口。`.env` 仅由用户本机提供，mysql 数据、备份、导出和个人数据不得进入镜像、日志或 Git；不得自动恢复 Backup，仅能通过既有 API 显式恢复 Backup V3。若 UAT 或未知进程占用端口，Compose 必须失败且不得停止、替换或切换它；停止仅可使用 `docker compose stop`，禁止 `down -v`。禁止修改 apps/api、apps/client、packages、Contracts、Migration、备份格式、数据库结构、端口业务语义、远程/公网访问或任何业务功能；未完成 QA 与产品验收不得宣称能力完成。

**受控 LAN 前端发布边界（2026-07-28，已实施、待局域网设备复验）**：这是对既有“仅回环”限制的唯一例外：在保留本机 H5 入口 `http://127.0.0.1:10086` 的同时，已额外发布 H5 页面入口 `http://192.168.2.57:10086`，供同一局域网设备访问。API `32146` 与 MySQL `3306` 继续仅限本机回环或 Docker 内部，绝不向局域网设备开放；两个 H5 入口的 `/api` 与 `/health` 均仅经既有同源内部代理转发，API CORS 不扩张。实施范围仅限 `docker-compose.yml`、`.env.example`、新增 `scripts/kb-lan.ps1`、必要文档/直接测试/QA 记录；禁止修改 API、H5 业务、Nginx、CORS、数据库、配置实值、业务语义、Contracts、Application、Repository、Migration 或备份格式。局域网发布只允许本机已分配的 RFC1918 私网 IPv4，允许 Windows 网络类别为 Private 或 Public；必须拒绝 `0.0.0.0`、回环、非本机地址与非私网地址。仅放行该 IP 的 TCP `10086`，不放行 API 或 MySQL；本次复用同项目旧 MySQL 容器和网络，仅以 `--no-deps` 启动/重建 app，未启动、重建或修改旧 MySQL、migration 或业务数据。禁止公网、反向代理或隧道；日常/UAT health 核验与数据库隔离规则保持不变。

**本地 Taro + Vite LAN 测试（2026-07-28，已回退）**：Docker LAN 运行测试暂不继续。本地开发服务器已恢复仅监听 `127.0.0.1:10086`，不再提供 `0.0.0.0` 或 LAN H5 入口；API `32146` 与 MySQL `3306` 始终继续仅监听 `127.0.0.1`。API CORS 白名单未改变，仍仅允许 `http://127.0.0.1:10086`。

**探索主线 V1.1.8 左侧分页已授权实施（2026-07-27，已修订）**：以分页替代活跃探索主线左侧内部滚动列表；每页固定 9 条。已删除主线不再在探索模块内提供“查看已删除主线”入口、独立列表、分页或恢复入口，统一仅在“数据与设置 → 回收站 → 探索主线”查看和恢复，与 V1.2 授权一致。活跃主线在创建或删除后回到第 1 页。点击当前页某条主线仅切换详情；翻上一页或下一页时，自动选中目标页第一条主线，确保选中态与右侧详情一致。刷新时保留当前选中主线并定位到其所在页；若选中主线不再存在则选当前有效页第一条；若当前页超过总页数则收敛到最后有效页。空列表不显示分页控件。该项只改变前端展示和局部页面状态，不新增读取/写入请求，不改变改名的外部点击消费、unknown-outcome、旧请求保护或任何主线业务语义。仅允许修改 `apps/client/src/pages/index/exploration-prototype.tsx`、`apps/client/src/pages/index/index.scss`、直接前端测试、必要 QA/产品记录与当天贡献记录；禁止 API、api-client、index.tsx、Application、Repository、Contracts、数据库、状态机、migration、配置、备份、关系或写入语义修改。

**探索主线 V1.1.8 左侧密度视觉补充（2026-07-27，已修订）**：在既有分页授权内，探索主线单项最小高度由当前约 56px 提升至约 62px，以减少列表下方留白；分页栏保持左栏底部对齐，不要求紧跟列表。不得改变每页 9 条、按钮文案、禁用逻辑、分页与选中规则、排序、读取或任何业务交互。仅限既有 `index.scss` 与直接视觉测试范围。

**探索主线 Backup 恢复后事实同步与数据总览补全（2026-07-29，已授权实施）**：已确认这是 Backup 恢复成功后前端仍保留陈旧探索主线事实的 P1 修复，不涉及 API、DTO、Application、Repository、Contracts、Migration、Backup、数据库或删除恢复语义。仅在恢复 API 明确成功且事项、方法、回收站、既有 exploration-tracks 真实读取全部成功后，才更新探索主线事实；若恢复后活跃主线为空，必须清空列表、`selectedId`、历史及旧主线上下文，显示正常空态，不得显示“探索主线不存在”或继续使用旧主线。数据与设置须补充探索主线的真实活跃数量，已删除主线不计入，仍仅属于回收站。读取失败、503、Abort、响应丢失或 unknown-outcome 时保留旧事实和明确错误/锁定状态，不得伪造为空或自动重试。仅允许修改 `apps/client/src/pages/index/index.tsx`、`apps/client/src/pages/index/exploration-prototype.tsx`、直接前端测试、必要 QA / 产品记录与当天贡献记录；禁止修改 `api-client`、API、DTO、Application、Repository、Contracts、Migration、Backup、数据库、配置、运行库、状态机或任何业务写入/删除恢复语义。产品授权见 `docs/product/探索主线-Backup恢复后事实同步与数据总览补全-产品编码授权.md`；实施后必须经 QA 与产品验收。

**MaruMaru 品牌展示名称替换（2026-07-29，已授权实施）**：用户可见的英文产品名称冻结为 `MaruMaru`，中文名称冻结为“圈圈”。本项仅允许替换 H5 浏览器/导航标题、页面内既有产品展示文案、README 标题与产品介绍，及其直接视觉或文案回归测试、必要 QA / 产品记录与当天贡献记录。必须保持所有内部技术标识不变，包括 `knowledge-base`、`@knowledge-base/*`、`knowledge_base`、`knowledge_base_uat`、Docker Compose 项目及 volume 名称、镜像仓库路径、端口和现有启动命令；禁止修改 API、DTO、Application、Repository、Contracts、Migration、Backup、数据库、配置实值、Docker、启动脚本、路由、状态机或业务语义。产品授权见 `docs/product/MaruMaru-品牌展示名称替换产品授权.md`；实施后必须经 QA 与产品验收。

**数据与设置统计与恢复预览补全（2026-07-29，已授权实施）**：本项为纯前端展示补全，复用既有活跃探索主线读取事实与 Backup V3 已有 `explorationTracks` 数据。宽桌面下“本地数据状态”四张统计卡必须同一行、等宽展示有效事项、当前方法、探索主线与回收站；窄屏允许响应式降为两列或单列，但不得压缩、遮挡或截断文本。探索主线统计仅计活跃主线，已删除主线继续仅属于回收站。合法 Backup 的恢复确认信息必须展示 `X 条事项 · X 条复盘 · X 条方法 · X 条探索主线`，只说明既有将恢复内容，不改变恢复范围、覆盖语义、确认层或 unknown-outcome/失败边界。仅允许修改 `apps/client/src/pages/index/index.tsx`、`apps/client/src/pages/index/index.scss`、直接前端测试、必要 QA / 产品记录与当天贡献记录；禁止 API、DTO、Application、Repository、Contracts、Migration、Backup 格式、数据库、配置、运行库、状态机、恢复或删除语义、接口、字段、统计口径、缓存、自动重试或自动恢复改动。产品授权见 `docs/product/数据与设置-统计与恢复预览补全产品授权.md`；实施后必须经 QA 与产品验收。

**数据与设置统计与恢复预览补全（2026-07-29，产品验收通过）**：宽桌面四项统计已验证同排等宽，窄屏安全降列；探索主线仅统计真实活跃记录，已删除记录仍仅归入回收站；Backup V3 零条/多条探索主线及旧备份零条预览均已验证。恢复后的读取失败、503、Abort、响应丢失与 unknown-outcome 均保留旧事实和恢复锁定，不伪造成功或空态，也不自动重试。本结论仅覆盖该授权的统计与恢复预览补全，不自动验收、归档或提交同工作区的品牌替换、恢复事实同步及其他未归档改动；完整结论见 `docs/product/数据与设置-统计与恢复预览补全产品授权.md`。

**V1.1.3 / V1.1.4 已授权实施（2026-07-27）**：产品接受架构“有条件可行 / DTO 例外”的结论。V1.1.3 授权以 `docs/product/探索主线-V1.1.3-已放弃事项探索关联只读产品授权.md` 的修订版为准：前端按结构化 `status=abandoned` 只读展示，MySQL Repository 必须在事务锁定事项后拒绝关联调整与移除并复用既有 `400 VALIDATION_FAILED + requestId`；真实刷新为 `idea_to_try` 后才恢复入口；另仅允许在 `index.scss` 将“查看已放弃记录”字号与“探索历史”统一。V1.1.4 授权记录见 `docs/product/探索主线-V1.1.4-重新开始覆盖启动动作产品授权.md`：仅批准既有 StartItemExecutionInput 增加 `overwriteExistingStartAction` 确认参数；仅在旧动作存在、新旧非空动作不同且该参数为真时，允许单一事务原子覆盖动作、迁移到 doing 并写入唯一状态事件。未经确认稳定拒绝；取消零写入；unknown-outcome 不自动重试，只能重读确认。两项均禁止新增业务对象、持久字段、状态、关系、路由、migration、备份格式、配置或运行库 DDL；实施与验收文件范围以两份授权为准。

**方法模块纯视觉热修复已授权（2026-07-27，去外框化工作台方案）**：独立授权记录见 `docs/product/方法模块-V1.1-纯视觉热修复产品授权.md`。仅允许调整方法模块的前端布局密度：取消或显著弱化无信息的 `methods-panel` 外层白卡，双栏工作台成为唯一主容器；标题、有效方法数量和排序合并为工作台上沿紧凑工具栏；内容最大宽度 1180–1240px；左栏暖灰导航面、右栏近白阅读面，仅保留两层背景；工作台高度为 `clamp(440px, calc(100dvh - 220px), 560px)`，两栏独立滚动，小屏降为单栏；主行动维持强调，来源证据和版本历史降为低强调折叠行。保留搜索、列表、详情、更多操作、开始行动、来源证据和版本历史的现有结构与交互。仅允许修改 `apps/client/src/pages/index/index.scss`，必要时 `apps/client/src/pages/index/index.tsx` 的既有静态布局结构及直接视觉回归测试；禁止所有业务语义、API、Application、Repository、Contracts、数据与 unknown-outcome 修改。本段替代此前 32% / 68% 与 620px 的方法模块视觉规范。

**方法模块视觉 P2 回归（2026-07-27）**：当前实现截图呈现窄小内嵌滚动框、外层大片空白与仍可感知的外层白卡，未满足去外框化工作台授权，不得验收或归档。允许在同一纯视觉文件范围内纠正：`methods-panel` 仅作无卡片全宽承载层，双栏工作台填满可用主内容宽度，且高度 clamp 必须作用于整体工作台而非子区；左右栏仅在整体内部独立滚动。不得以固定短高、额外内层滚动或大片空白替代冻结布局。

**方法模块桌面双栏冻结补充（2026-07-27）**：当前方法页必须按“左方法列表 / 右方法详情”呈现，而非上下堆叠。左栏只承担搜索和方法列表；右栏承载选中方法的标题、版本/验证/更多、步骤/补充、开始行动、来源证据和版本历史。已定位 `.methods-panel > view:first-child` 覆盖 `.methods-workbench` 的 `display:grid`，导致双栏失效；必须在同一纯视觉范围内移除该冲突。其余边界不变。

**方法模块与探索主线视觉语言统一补充（2026-07-27）**：在既有“方法模块 V1.1 纯视觉热修复”授权内，方法页须与探索主线采用同族的“外部页头 + 内部柔和双栏工作台”节奏，但不得复制或改变探索主线业务结构。桌面端继续固定为左方法列表、右方法详情；工作台在圆角、克制边框与阴影、左暖灰导航面 / 右近白阅读面上与探索主线同族，且不得回退为上下堆叠、瀑布卡片或窄小居中框。仅方法模块局部按钮可改为柔和胶囊或大圆角；主行动保持深色强调、次级操作低强调，严禁通过全局按钮选择器影响行动或探索主线。搜索、列表、详情、更多操作、开始行动、来源证据、版本历史、加载失败与 unknown-outcome 均不得改变；不授权任何 API、Application、Repository、Contracts、数据、状态机、路由、DTO、配置或业务语义修改。

**探索主线 V1.2 统一回收站入口已授权（2026-07-27）**：产品授权记录见 `docs/product/探索主线-V1.2-统一回收站入口产品授权.md`。仅允许扩展既有 TrashFilter / TrashEntry、TrashApplicationService、既有 `GET /api/v1/trash` 的 filter 参数和返回条目，以及对应 H5 接入；探索主线恢复继续调用既有 `POST /api/v1/exploration-tracks/:id/restore`，不得新增 trash 恢复路由。筛选固定为 all/item/method/exploration-track，排序固定为 `deletedAt DESC → item/method/exploration-track → id ASC`；全部筛选必须包含三类事实。事项/方法保留既有 30 天文案；探索主线文案固定为“当前不自动清理，可在此恢复”，全部筛选不得显示统一剩余天数。恢复后重读当前回收站、事项数据和活跃主线列表；不重建或改写关联。禁止 Repository、migration、数据库、配置、备份格式、状态机及删除/恢复语义改动，禁止永久删除、批量操作、搜索或路由。unknown-outcome 单次请求、无自动重试，仅显式重读确认。

**探索主线 V1 解封板与 Repository 查询优化切片已授权实施（2026-07-28，产品所有人裁决）**：产品所有人裁决解除探索主线 V1 于 2026-07-26 的最终封板，V1 范围重新开放由产品所有人主导的后续工作；本裁决不改变 V1 历史验收与归档事实，仅取消“未经新的产品立项不得改动”的限制。首个切片授权修复 `packages/storage-mysql/src/exploration-track-repository.ts` 中的 N+1 查询与 `SELECT *` 脆弱性：`listActive`（行 92-100）与 `historyEntries`（行 268-277）的逐行子查询改为批量查询或 JOIN；该文件内 `SELECT *` 顺手替换为显式列名。仅允许修改 `packages/storage-mysql/src/exploration-track-repository.ts` 及其直接测试；不得新增业务对象、字段、状态、关系、路由、migration、备份格式；不得改变任何业务语义、状态机、软删除/恢复语义、事务边界或锁顺序；不得双写、回填、fallback 或浏览器直连。验收须证明查询结果与改前逐行等价、事务与锁语义不变，并通过 `typecheck` / `test` / `build:h5` / `git diff --check`。

**API 错误分类穷尽性守卫收尾修正已授权实施（2026-07-29，产品所有人裁决）**：`apps/api/src/api-errors.ts` 中 `mapBusinessFailure` 与 `shouldReportBusinessFailure` 的穷尽性守卫以 `return unhandledCategory` 收尾，运行时若业务错误分类超出联合类型将返回非约定类型值而静默走错分支。授权仅把这两处收尾改为显式抛出，保留既有 `never` 编译期检查、既有分类映射与上报判定不变。仅允许修改 `apps/api/src/api-errors.ts` 及其直接测试与当天贡献记录；不得新增错误码、分类、路由、DTO、业务语义或运行库操作。

**错误契约基础切片已授权实施（2026-07-29，产品所有人裁决）**：为后续后端按业务域拆分与 Hono RPC 接入做基础准备，授权两项纯结构改动。其一，`BusinessErrorCode` 的单一大联合按业务域拆到 `packages/contracts/src/errors/` 下的独立模块文件（item / exploration-track / method / review / backup），聚合联合与 `BusinessErrorCategory` 保持同名同值导出，`packages/domain` 继续对外 re-export，所有既有调用点不改。落点选 `contracts` 而非 `domain`，因 `domain` 已依赖 `contracts`，反向引用会形成包级循环，且 `businessCode` 自本切片起属于对外 API 契约。其二，`ApiErrorCode` / `ApiErrorStatus` / `ApiErrorBody` 统一到 `packages/contracts`，`ApiFailure` 与 API 失败响应体增加可选 `businessCode`，供前端按业务错误码分支而非匹配文案；`internal` 分类按未分类故障呈现，不透出 `businessCode`。仅允许修改 `packages/contracts/src/**`、`packages/domain/src/index.ts`、`apps/api/src/api-errors.ts`、`apps/api/src/index.ts`、`apps/api/src/hono/{types,errors,app}.ts` 及其直接测试与当天贡献记录。本切片为纯结构与可选字段新增：不得新增或删除任何错误码取值、不得改变既有错误码到 HTTP 状态的映射、不得改变上报判定、不得新增业务对象/字段/状态/关系/路由/migration/备份格式，不得改动前端代码。

**错误契约目录收缩已授权实施（2026-08-02，产品所有人裁决）**：当前业务错误码体量不足以支撑按域一文件拆分，观感过度模块化。授权将 `packages/contracts/src/errors/` 下 item / exploration-track / method / review / backup / category 的类型定义收回 `business.ts`，保留各域独立类型名与 `BusinessErrorCode` / `BusinessErrorCategory` 同名同值导出；`api.ts` 继续独立。仅允许修改 `packages/contracts/src/errors/**`、必要删除上述过碎文件，以及当天贡献记录与本锚点条目。不得新增或删除任何错误码取值、不得改变对外导出面与 HTTP/上报语义，不得改动 domain / api / 前端 / 运行库。

**错误契约已统一（2026-08-02，产品所有人裁决）**：认证、平台管理、备份归属、初始归属及既有业务失败共用唯一 `BusinessErrorCode` 与穷尽的 `businessErrorCategoryByCode`；`BusinessError` 的 category 只能由中央码表派生，调用点不再重复指定。Repository 与 Application 对预期业务失败直接抛 `BusinessError`，不再保留层级私有码、平行 Error 类或手动翻译表。API 对业务失败只识别 `BusinessError`，统一按 category 映射 HTTP，并仅通过 `publicBusinessErrorCodes` 白名单返回 `businessCode`；认证凭据、平台管理及内部归属类错误码不对外暴露。MySQL 连接、Schema 未就绪等非业务基础设施故障仍走独立边界。历史契约冻结过程与最终实施记录分别见 `docs/architecture/错误契约统一-切片1-契约冻结.md`、`docs/architecture/错误契约统一-切片2-消灭翻译层.md`；本次未改 H5 业务页、Migration、运行库、路由或业务规则。

**Application 模块结构拆分已实施（2026-08-02，产品所有人裁决）**：`packages/application/src/index.ts` 已按账户与权限、备份、事项与探索主线、复盘与方法、读模型、回收站六个内聚职责拆分；根 `index.ts` 仅保留稳定公共导出，现有 `@knowledge-base/application` 导入方式和全部公开符号保持不变。该切片仅修改 `packages/application/src/**`、本锚点与当天贡献记录，未改变业务规则、错误码、事务或 Repository 调用语义，未修改 API、Contracts、Repository、H5、Migration 或运行库。

**Application 纯规则函数化已实施（2026-08-02，产品所有人裁决）**：重复的主线名称规范化已收敛为唯一 Domain 函数；备份解析校验、Dashboard 报告计算与回收站排序已提取为可直接测试的函数，备份函数可显式注入 ID 生成器以固定旧版本补齐结果。现有 Application Service 继续作为依赖注入和业务编排外壳，公开 Service API、业务规则、错误码、排序、备份兼容与 Repository 调用保持不变；未机械改写 class，未修改 API、H5、Contracts、Repository、Migration 或运行库。

**Hono 路由前缀收拢切片已授权实施（2026-07-29，产品所有人裁决）**：`apps/api/src/hono/app.ts` 将 `/api/v1` 在挂载点摊平重复 10 次（全文件 24 次），版本前缀无单一声明处，升版本需逐条修改。授权把 10 个 `/api/v1/*` 挂载改为先组装 v1 子应用再整体挂到单一 `apiV1BasePath` 常量上，前缀声明收敛为一处。该切片实施时 `apps/api/src/hono/**` 尚未被 `main.ts` 接入；后续完整接入见下方 2026-08-02 条目。当时验收以路由表等价为准并固化为 `tests/hono-route-table.test.ts`。

**Hono 完整接入与 Scalar OpenAPI 文档已实施（2026-08-02，产品所有人裁决）**：日常入口与测试用 `createApiServer` 均改为 `@hono/node-server` 适配的唯一 Hono 实现；补齐 Cookie 会话 `kb_session`、auth/admin 路由、登录后按 `userId` 的 scoped services，以及 admin 先鉴权再 413 的门顺序。业务路由迁至 `@hono/zod-openapi` 的 `createRoute`，挂载 `GET /openapi.json` 与 Scalar `GET /docs`（中文 pageTitle）；CORS 对允许 Origin 补 `credentials` 与 `PUT`。不开放 `0.0.0.0`、不扩大 CORS 白名单、不改 Migration/运行库/H5 业务页，不把 `AUTH_INVALID_CREDENTIALS` 放入对外 `businessCode` 白名单。架构记录见 `docs/architecture/Hono接入与Scalar文档冻结.md`。

MySQL 快速接入 Sprint 已完成以下浏览器级与 UAT 验收：

```text
H5 完整主流程：创建事项 → 保存补充说明 → 开始执行 → 完成复盘 → 刷新读取
JSON 导出 → 仅清理 knowledge_base_uat → 恢复 → H5 / API 核验
MySQL 重启期间 MYSQL_UNAVAILABLE 诚实降级 → 服务恢复后 H5 读取与最小写入持久化
写入失败时草稿保留 → API 恢复后同会话重提 → 重启后持久化一致
```

集中 UAT 全程在 `knowledge_base_uat` 执行，未触及 `knowledge_base`、`mysql-data`、IndexedDB 或 SQLite，且未发现 P0 / P1。

因此当前允许表述：

```text
MySQL = 当前 H5 唯一运行主库
```

探索主线 V1 的 S1“Schema 004 与基础 Contracts”与 S2“Repository / Application 与原子工作流”均已于 2026-07-24 经产品验收正式封板。S2 已通过固定 13 文件真实 MySQL 回归入口、事务与可信读模型验证、已删除 / unavailable 关联拒绝零副作用验证，以及两个运行库 `SNAPSHOTS_IDENTICAL` 零污染门。S2 封板不等于探索主线 V1 整体完成。

## S3 切片授权状态

产品已基于 `docs/architecture/探索主线-V1-S3-最小架构任务书.md` 书面授权并验收 S3 切片 1“Backup V3（后端）”。验收记录见 `docs/product/探索主线-V1-S3-切片1-BackupV3产品验收结论.md`；本结论不等同于 S3 整体授权。

```text
允许修改：
- packages/contracts/src/**
- packages/application/src/**
- packages/storage-mysql/src/**
- 与 Backup V3 直接对应的 tests/**
- 必要的真实架构/验收记录，以及完成工程验证或 H5 人工验收后当天的贡献记录

禁止修改：
- apps/api/src/**、apps/client/src/**，以及任何 API 路由、HTTP 或 H5 真实接入
- migrations/004_add_exploration_tracks.sql 或任一既有 migration
- 任一运行库 DDL、手工修表、migration record、mysql-data、MySQL 容器、端口或权限配置
- IndexedDB / SQLite 运行时资产、状态机、复盘、方法、回收站、ItemLink 语义
- 双写、同步、回填、fallback、浏览器直连 MySQL，及任何新业务语义
```

切片 1 验收门已满足：随机临时 MySQL database 已证明 V1/V2 安全降级、V3 导出/恢复等价、非法 V3 在业务写入前整体拒绝、十个业务集合零写入与末端失败整体回滚，以及 `system_metadata` 隔离；`knowledge_base` 与 `knowledge_base_uat` 前后只读深度快照均为 `SNAPSHOTS_IDENTICAL`。

产品现书面授权 S3 切片 2“冻结 API 与 HTTP 契约（后端）”。授权记录见 `docs/product/探索主线-V1-S3-切片2-API与HTTP契约产品授权.md`。本切片仅允许修改 `apps/api/src/**`、与冻结 API 契约直接对应的 `tests/**`，以及必要的真实架构/验收记录和当天贡献记录；API 只可调用既有 Application 能力。严禁修改 `packages/application/src/**`、`packages/storage-mysql/src/**`、`packages/contracts/src/**`、任一 migration、运行库 DDL/DML、`apps/client/src/**`，或新增/变更任何冻结路由、DTO、状态、关系与业务语义。API 集成测试仅使用随机临时 MySQL database；任何运行时测试均须先以 `/health` 确认目标库。验收须证明冻结路由、DTO、排序、body/query 边界、错误映射、脱敏、`requestId`、`Cache-Control: no-store` 与无额外路由。未获得 QA 与产品对切片 2 的通过结论，不得开始切片 3 或任何后续 S3 切片；前端原型继续仅限纯展示、无真实数据读写，控件持续禁用。

切片 2 验收门已满足并完成产品验收，记录见 `docs/product/探索主线-V1-S3-切片2-API与HTTP契约产品验收结论.md`：定向 API 测试 1 文件 7 用例与完整 MySQL 集成回归 13 文件 127 测试均通过，`git diff --check` 通过；冻结路由、DTO、排序、错误映射、`requestId`、`Cache-Control: no-store`、无额外路由及 64 KiB / 16 MiB body 边界均已覆盖。测试仅使用随机临时 MySQL database。

产品现书面授权 S3 切片 3“H5 API Client / Adapter 真实接入”。授权记录见 `docs/product/探索主线-V1-S3-切片3-H5真实接入产品授权.md`。本切片只允许修改 `apps/client/src/**`、与真实接入直接对应的前端 `tests/**`，以及必要的真实设计/验收记录和当天贡献记录；只能消费已冻结 API 的结构化 DTO。严禁修改 API、Application、Repository、Contracts、migration、运行库、`mysql-data`，或新增/变更任何业务语义、路由、DTO、状态与关系。必须移除探索主线真实读写对 fixture 的依赖；禁止 IndexedDB / SQLite / localStorage 持久化、浏览器直连 MySQL、双写、同步、回填和 fallback。unknown-outcome 端到端实现仍不在本切片范围。涉及实际 API 运行时的测试，必须先以 `/health` 确认目标库；浏览器破坏性 UAT 继续留待切片 5。未获得 QA 与产品对切片 3 的通过结论，不得开始切片 4 或任何后续 S3 切片。

切片 3 验收门已满足并完成产品验收，记录见 `docs/product/探索主线-V1-S3-切片3-H5真实接入产品验收结论.md`：Adapter 的 GET/PUT/DELETE、available / track-deleted / unavailable / no-association 边界、读取状态、草稿保护与旧请求竞态已通过定向复核；typecheck、2 文件 11 用例、`build:h5` 与 `git diff --check` 均通过。设计冻结已修订为 track-deleted 只读且调整/移除稳定拒绝、零副作用。浏览器破坏性 UAT 留待切片 5，不构成本切片未完成项。

产品现书面授权 S3 切片 4“unknown-outcome 端到端保护”。授权记录见 `docs/product/探索主线-V1-S3-切片4-unknown-outcome产品授权.md`。仅允许修改 `apps/client/src/**`、与 unknown-outcome 验证直接对应的 `tests/**`，以及必要的真实验收记录和当天贡献记录；API 仅作为既有 503 与冻结读接口的验证对象，未经产品再次裁决不得修改。超时、断线、页面关闭或响应丢失后，前端必须保留草稿和明确选择，不伪造成功或失败、不自动重发，只能重新读取冻结 API 确认事实。严禁新增幂等键、结果查询、后台补偿、自动重试、双写、fallback 或本地成功推断。MySQL 不可用验证和任何故障注入只允许在显式 `.env.uat`、`/health` 确认 `knowledge_base_uat`、且具备前后快照和恢复条件时执行；日常 `knowledge_base` 不得触及。未获得 QA 与产品对切片 4 的通过结论，不得开始切片 5 或最终封板。

切片 4 验收门已满足并完成产品验收，记录见 `docs/product/探索主线-V1-S3-切片4-unknown-outcome产品验收结论.md`：unknown-outcome 锁仅在所需真实读取全部成功后解除；读取中、失败、Abort 或过期请求持续锁定，并保留草稿、改名输入、确认层与明确关联选择。Track 写入、关联写入及扩展 `POST /items` 的响应丢失和写入 Abort 均无自动重发，503 读取不降级为空数据；typecheck、2 文件 21 用例、`build:h5` 与 `git diff --check` 均通过。真实 API 写入、故障注入和浏览器破坏性 UAT 保留为切片 5，不得以本结论替代。

产品现书面授权 S3 切片 5“隔离浏览器 UAT、架构复审与最终产品验收输入”。授权记录见 `docs/product/探索主线-V1-S3-切片5-隔离UAT产品授权.md`。仅允许 QA 在显式 `.env.uat` 会话中、先以 `/health` 实测并记录 `database=knowledge_base_uat` 与 `schemaVersion=4` 后执行浏览器 UAT、受控清库/恢复、故障注入与必要的 UAT 数据写入；任何一步不满足即停止。执行前后须保留 `knowledge_base` 与 `knowledge_base_uat` 的只读深度快照，日常库必须为 `SNAPSHOTS_IDENTICAL`；禁止 DDL、修改 migration record、删除 `mysql-data`、触及 IndexedDB/SQLite 或使用非 loopback 入口。覆盖主线生命周期、existing/new 原子捕获与取消零写入、关联调整/移除、deleted/unavailable、历史与定位、V1/V2/V3 备份、unknown-outcome、MySQL 不可用、无 fallback、桌面/小屏与两行截断。QA 完成后转架构师复审；未经架构复审与产品最终验收，不得宣称探索主线 V1 完成或封板。

切片 5 架构复审结论为不通过，不得封板。产品现书面授权一次性切片 5.1“最小合规修复”，授权记录见 `docs/product/探索主线-V1-S3-切片5.1-最小合规修复产品授权.md`。仅允许：以既有 Backup V3 合法空文档的恢复替代 UAT 的 DROP/CREATE DATABASE 清理，使清理仅在单一 DML transaction 中作用于十个业务集合且保留 `system_metadata` 与 `schema_migrations`；将 V3 断裂 `explorationTrackId` 以既有错误语义映射为 `400 VALIDATION_FAILED` 并补零写入回归；仅移除或改正“尚未接入真实数据能力”的失实 P2 文案。不得新增对象、字段、状态、关系、migration、路由或其他 API/前端能力。修复后必须重建合规 UAT 基线并完整重做浏览器 UAT；未经新的 QA 通过、架构复审和产品最终验收，不得封板。

产品同时书面授权最终 UAT 故障启动器的实现与直接测试，记录见 `docs/product/探索主线-V1-S3-最终UAT故障启动器产品授权.md`：仅允许修改 `scripts/uat-api-fault.ps1`、其直接测试、架构/QA 记录与当天贡献记录。启动器只能编排本地 loopback API 子进程，故障验证只接受既有 HTTP `503` 和冻结 GET 错误 DTO 的 `code=MYSQL_UNAVAILABLE`、`requestId`；不得新增 health 字段、诊断字段、路由或 DTO。此授权不包括浏览器 UAT 执行；完整 UAT 须待切片 5.1 三项修复完成并通过 QA 后，重建合规基线再一次性执行。

切片 5 QA 已报告通过：最终 UAT 已覆盖 health、两库只读深度快照、V3 恢复、unknown-outcome、真实 `503 MYSQL_UNAVAILABLE`、恢复后的显式重读、浏览器证据与故障启动器直接测试。当前仅等待证据归档完整性与架构复审；该状态不构成探索主线 V1 封板或产品最终验收。

架构复审确认唯一技术阻断为故障 `/health` 响应含未冻结 `diagnosticId=MYSQL_UNAVAILABLE`。产品现书面授权最小修复，记录见 `docs/product/探索主线-V1-S3-health-diagnosticId移除产品授权.md`：仅移除该既有 `/health` 503 响应字段及其直接测试、QA/架构记录与当天贡献记录；不得新增字段、DTO、路由、业务能力或运行库操作。修复后须定向证明 fault health 为脱敏 HTTP 503 且不含 `diagnosticId`，冻结 GET 仍为 `code=MYSQL_UNAVAILABLE` 且含 `requestId`，`restore-normal` 后 health 为 `ready / knowledge_base_uat / schemaVersion=4`。随后转 QA 定向复测与架构复审；探索主线 V1 仍不得封板。

health `diagnosticId` 最小修复已通过定向 QA 复测与架构技术复审：fault health 为不含该字段的 HTTP 503，冻结 GET 仍为 `code=MYSQL_UNAVAILABLE` 并含 `requestId`，恢复后及当前 health 为 `ready / knowledge_base_uat / schemaVersion=4`；直接测试 2 文件 11 用例与 `git diff --check` 通过。旧含 `diagnosticId` 的 `health-fault.json` 保留为修复前历史事实。

**最终封板事实（2026-07-26）**：S3 产品功能验收、QA 最终 UAT、架构技术复审与 Git 归档均已完成。产品已据此签发探索主线 V1 最终封板，归档提交为 `d1f2381 feat(exploration): archive V1 MySQL workflow`（105 files）。此前“等待产品最终验收 / 独立提交裁决 / 不得封板”的表述均为已完成的历史阶段，不再适用。探索主线 V1 已封板；未经新的产品立项，不得安排任何该范围内的业务代码、运行库、配置或数据库改动。

**V1 解封板裁决（2026-07-28）**：产品所有人已裁决解除上述 V1 最终封板，V1 范围重新开放由产品所有人主导的后续工作；详见本节“探索主线 V1 解封板与 Repository 查询优化切片已授权实施（2026-07-28，产品所有人裁决）”条目。本条不重写 2026-07-26 的封板历史事实，仅取消其“未经新的产品立项不得改动”的限制。

**全栈 Docker Compose 一键启动实施状态（2026-07-27）**：部署层实现已完成，应用镜像已实际构建；Compose 配置校验、容器内 Nginx 语法校验以及 `.env`、`mysql-data` 排除检查均已通过。当前 `127.0.0.1:32146` 实测仍为 `ready / knowledge_base_uat / schemaVersion=4`，且 10086、32146、3306 均已有受保护监听进程；为遵守端口/UAT 不干扰约束，本会话未启动或停止 Compose，未创建命名 volume、未执行 migration、未写入任何运行库。全链路干净目录 Docker QA（空 volume、health、三入口、端口冲突、显式 Backup V3、stop 后 volume 保留与回归）仍待执行；在 QA 与产品验收前不得宣称该能力完成或归档。

**全栈 Docker Compose 自包含镜像修订（2026-07-28）**：用户已明确要求新机器不携带项目目录。现应用镜像已内置既有 MySQL 账号初始化规则；`migrate` 服务在官方 MySQL 就绪后，以同一镜像先完成账号/权限与两库初始化、再执行既有日常库 migration，Compose 已不再挂载本地 `docker/mysql/`。镜像已重新构建，容器内 Nginx、入口脚本和内置初始化脚本均通过直接语法校验；当前受保护 UAT 端口仍未触碰。此修订不改变 API/H5/Contracts/Application/Repository、Migration 内容、数据库结构、Backup V3 或业务语义。仍待干净机器/空 volume QA，未验收前不得推送镜像仓库、宣称一键部署完成或归档。

**阿里云镜像引用准备状态（2026-07-28）**：Compose 的 `app` 与 `migrate` 已固定引用 `crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:latest`，本地同一构建结果已具备 `:latest` 与 `:1.0.0` 两个标签。新机器的目标部署包已收敛为 `docker-compose.yml` 与本机 `.env`；首次登录私有仓库后，Compose 可拉取应用镜像，官方 MySQL 仍由 Compose 拉取。尚未执行 `docker login`、`docker push` 或全链路运行测试；必须先完成隔离 QA 后才可推送和发布。

**阿里云候选镜像上传例外（2026-07-28）**：用户已直接授权将当前本地构建上传至指定私有仓库，并同时推送固定候选标签 `:1.0.0` 与默认候选标签 `:latest`。此次上传仅用于后续隔离 QA 和拉取验证，不构成 Docker 部署能力验收、正式发布、产品封板或对新机器可用性的宣称；隔离 QA 未通过前不得将该镜像用于个人业务数据迁移或正式运行。

**阿里云候选镜像已上传（2026-07-28）**：`crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:1.0.0` 与 `:latest` 均已推送成功，并已完成只读远端 manifest 核验。两标签当前指向同一候选构建；仍仅可用于隔离 QA，不得替代完成验收或正式部署结论。

**阿里云候选镜像 QA 阻断（2026-07-28）**：隔离 Docker QA 已判定当前 `:latest` 与 `:1.0.0` 不通过：运行镜像因复制完整构建工作区而包含 `@tarojs/cli` 的两个 `node_modules` 测试夹具 `.env`。现书面授权仅在 Docker 最终产物范围内移除这些非运行时夹具与任何 `.env`，严禁修改 API、H5、Contracts、Application、Repository、migration、Backup、数据库或业务语义。修复后必须使用新的不可变候选标签，严禁覆盖本次 QA 证据对应的 `:latest`、`:1.0.0`；仍待独立宿主机复测。

**Docker 修复候选已发布（2026-07-28）**：已按 QA 最小修复范围重建并上传不可变候选 `crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:1.0.1-qa.1`，未覆盖失败证据对应的 `:latest`、`:1.0.0`。镜像内容直接检查已确认 `/app` 内无 `.env`，Taro CLI `__tests__` 夹具已移除，Nginx 与两个入口脚本语法、Compose 配置和 `git diff --check` 均通过。QA 复测时须在本机 `.env` 设置 `KB_APP_IMAGE_TAG=1.0.1-qa.1`；默认 `latest` 仍指向失败候选，禁止用于复测或正式运行。仍待独立宿主机全链路复测。

**Docker 修复候选交付包修订（2026-07-28）**：Compose 的 `KB_APP_IMAGE_TAG` 默认值及 `.env.example` 已固定为 `1.0.1-qa.1`，不再要求复测者手工补写该变量；在当前干净交付配置上执行 `docker compose config --images` 已精确渲染一次 `mysql:8.4` 和两次 `dnf:1.0.1-qa.1`。失败的 `latest`、`:1.0.0` 仍被明确禁止。此修订仅涉及 Docker 交付配置与文档，尚待独立宿主机空 volume 全链路 QA。

**Docker 镜像 1.1 发布授权（2026-07-28）**：用户已明确授权基于当前工作区构建并推送同一应用镜像的 `:1.1` 与 `:latest` 两个标签；`:latest` 以本次构建覆盖旧失败候选，仅用于后续部署验证，不等同于独立 Docker 宿主机全链路 QA 已完成，也不改变正式迁移、运行验收与产品封板仍待完成的事实。构建与推送不得触碰任何运行中的 API、H5、MySQL、数据库、迁移或备份。

**Docker 镜像 1.1 构建阻塞（2026-07-28）**：已按授权尝试构建 `:1.1` 与 `:latest`，但均在 Docker BuildKit 的 `pnpm install --frozen-lockfile` 阶段失败；首次 `--pull` 被 Docker 配置的不可达代理 `192.168.1.2:7890` 阻断，取消拉取后 npm 依赖下载仍以 `ECONNRESET` 失败。未生成新镜像、未覆盖任何已有标签、未执行 `docker push`，且未触碰运行服务或数据库；待 Docker 构建环境恢复可访问 npm 后重试。

**Docker 镜像 1.1 已发布（2026-07-28）**：Docker 构建网络恢复后，当前工作区已成功构建并上传 `crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:1.1` 与 `:latest`；两标签均指向摘要 `sha256:b9351d4f9dcf5e469c1f7922beda21af85acc3f725f54e928520d851afcd108f`。镜像内 `/app` 的 `.env` 文件检查无输出，证明未包含该类文件；本次仅完成构建与私仓发布，独立 Docker 宿主机空 volume 全链路 QA、正式迁移与部署验收仍未完成。

**Docker 镜像入口脚本换行修复并重发（2026-07-29）**：虚拟机实际拉取 `:latest` 后，`migrate` 容器以 `127` 失败；日志确认镜像内 `/app/docker/app-entrypoint.sh` 虽存在但为 Windows CRLF，Linux 将解释器识别为不存在的 `/bin/sh\r`。已将该脚本规范为 LF，并新增 `.gitattributes` 固定所有 `.sh` 使用 LF；新镜像已构建、在容器内通过入口脚本 LF 字节与 `sh -n` 检查，并推送 `crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:1.1.1` 与覆盖后的 `:latest`，两标签指向 `sha256:dcb8857df3dda408125b5f8b259a661f37193ecfbce56db811952dfe35f210d5`。本次未启动或修改本地运行库；虚拟机仍须拉取新 `:latest` 后继续完成运行验证，未完成前不得宣称一键部署已验收。

**新机器交付目录同步修复（2026-07-28）**：实际交付目录 `C:\Users\Administrator\Desktop\mikey\新机器需要的配置` 已同步修正后的 `docker-compose.yml`，其本机 `.env` 已设置 `KB_APP_IMAGE_TAG=1.0.1-qa.1`，目录仅保留这两个文件。另为防止中文目录名导致 Compose 自动项目名为空，Compose 已固定顶层项目名 `knowledge-base`。在该真实交付目录执行 `docker compose config --images` 已精确输出 `mysql:8.4` 一次与 `dnf:1.0.1-qa.1` 两次；仍待独立宿主机运行复测，不得宣称一键部署完成。

**平台角色与最小权限管理 V1—切片 5 P1 真实渲染第二次最小修复与完整复测授权（2026-07-31）：** 产品已依据真实 Chromium 几何诊断签发新的独立授权，并以本授权替代上一轮针对同一 P1 的 `z-index`、`position` 与 `[disabled]` 存在选择器方案。生产修改仅限 `apps/client/src/pages/index/index.scss`：两个直接 `taro-button-core` 增加 `width:auto`，删除局部 `position/z-index`，disabled 仅精确匹配 `[disabled='true']`，并仅在桌面可见管理页存在时给予 `.app-main` 纵向滚动资格，同时保留小屏 `width:100%`；直接测试仅限 `tests/platform-administration-h5-flow.test.ts`。工程门通过后，QA 必须原样串行执行既有 E2E 全部 8 项并重新采集真实几何、滚动容器与命中栈证据。产品同时重新签发一次性例外：QA 仅可从根未跟踪私有 `.env` 读取本机 `MYSQL_HOST`、`MYSQL_PORT`、`MYSQL_ROOT_PASSWORD` 并只注入本次测试子进程内存，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-P1真实渲染第二次最小修复与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、服务重启部署、运行库写入和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 第二次真实渲染修复完整 QA 仍未通过（2026-07-31）：** 原样 8 项随机 Schema 6 E2E 本轮提升为 6 通过、2 失败；搜索输入卸载与顶部搜索/刷新按钮命中两个既有 P1 已关闭，包含真实鼠标搜索、清除、分页和刷新的完整场景通过。剩余第一项失败已确认是 E2E 编排与冻结分页冲突：总用户 24、固定 20 条且停留第 1 页时，测试直接等待早期创建、按排序位于第 2 页的 `member_a`。剩余第二项在新目标行已可见后仍无法通过 `getByRole(button, 管理用户名)` 定位管理控件，尚不能区分 Taro 可访问角色/名称、页面锁状态或测试假设问题，暂列 P1 候选并阻断角色/会话及 unknown-outcome 完整 H5 证据。最终 typecheck、`build:h5`、`git diff --check` 通过，临时目录、数据库和账号均为 0，finally 两运行库只读快照未报告变化。最新报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。下一步须由架构师冻结最小 E2E 分页修正与只读管理控件诊断；V1 产品最终验收、真实 006、真实管理员授予、服务重启部署和运行库操作继续禁止。

**平台角色与最小权限管理 V1—切片 5 分页编排与管理控件可访问性最小修正及完整复测授权（2026-07-31）：** 产品已依据只读诊断签发独立授权。E2E 仅可在 `tests/platform-administration-v1-slice5.e2e.test.ts` 中使用完整 username 通过既有搜索定位角色与会话目标，并在 unknown-outcome 场景开始前显式清除上一场景 query/page，同时保留 24 用户、固定每页 20 条、两页覆盖与稳定排序；生产仅可在 `apps/client/src/pages/index/platform-administration.tsx` 为 `.platform-more-button` 增加 `role="button"`；直接测试仅可在 `tests/platform-administration-h5-flow.test.ts` 断言动态 aria-label、button role 与既有显示条件。禁止修改 harness、SCSS、状态、API Client、后端或其他测试。工程门通过后，QA 必须原样串行执行全部 8 项，并以 `getByRole(button,name)` 唯一解析及真实鼠标点击验证，不得使用 CSS/getByLabel/force/DOM/坐标绕过。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-分页编排与管理控件可访问性最小修正与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 分页/可访问性修正完整 QA 仍未通过（2026-07-31）：** 修正后的原样 8 项 E2E 结果仍为 6 通过、2 失败；24 用户、20 条分页、两页、稳定排序及顶部搜索/刷新真实鼠标场景通过。两项失败均由新 helper 在进入角色或 unknown-outcome 流程前调用 `getByRole(button, 搜索)` 导致：本轮仅为 `.platform-more-button` 补充真实 button role，搜索 Taro 宿主并无该 role；此前已验证的搜索提交入口为 `.platform-administration-search-button` 的真实鼠标点击。因此本轮失败属于 E2E 修正引入的定位假设错误，不构成新的生产 P0–P3，也不能证明管理控件 role 修复失败。最终 typecheck、`build:h5`、`git diff --check` 通过，临时目录、数据库和账号均为 0，finally 两运行库只读快照未报告变化。最新报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。须另行授权仅修正两个 helper 的搜索提交定位并再次完整运行 8 项；V1 最终验收及所有运行库/部署动作继续禁止。

**平台角色与最小权限管理 V1—切片 5 搜索 Helper 定位单点修正及完整复测授权（2026-07-31）：** 产品已依据架构复审签发独立授权。本轮唯一工程修改为 `tests/platform-administration-v1-slice5.e2e.test.ts`：仅将 `locateManagementUser()` 与 `clearManagementSearch()` 的搜索提交改为 `.platform-administration-search-button` 普通 Playwright `click()`。搜索输入仍用真实 textbox，管理控件仍必须由 `getByRole(button,动态管理用户名)` 唯一定位和真实点击；完整 username、24 用户、每页 20 条、两页覆盖、稳定排序及 query/page 清理均保持不变。禁止修改生产代码、SCSS、状态、API Client、harness 或其他测试，禁止 force/DOM/坐标/键盘/CSS 注入/延时绕过。修正后 QA 必须原样串行执行全部 8 项。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-搜索Helper定位最小修正与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 搜索 Helper 修正完整 QA 仍未通过（2026-07-31）：** 原样 8 项 E2E 仍为 6 通过、2 失败。搜索 Helper 单点修正有效；24 用户/20 条/两页、完整 username 搜索、管理控件 `getByRole(button,动态名称)` 唯一解析与真实点击均已进入真实链路，角色授予、撤销、旧 Cookie 刷新及 member 管理 GET 403 已完成。第一项失败仅因 `getByText(行动)` 同时匹配导航和全局标题两个元素，属于 E2E strict-mode 定位歧义。第二项在服务端写入完成后由代理 `response.destroy()` 丢弃响应，但 8 秒内未出现 role-unknown 提示，尚不能区分代理未及时形成 fetch rejection、前端错误分类/状态采用或提示渲染问题，暂列 P1 候选并阻断两类 unknown-outcome、503、Abort 与显式恢复的完整证据。最终 typecheck、`build:h5`、`git diff --check` 通过，临时目录、数据库和账号均为 0，finally 两运行库快照未报告变化。最新报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。须先做响应丢失只读诊断并冻结唯一定位修正；V1 最终验收及所有真实运行库/部署动作继续禁止。

**平台角色与最小权限管理 V1—切片 5 行动定位与响应丢失只读诊断完成（2026-07-31，尚未授权修正）：** 架构复核确认“行动”失败仅为测试同时匹配导航文字和 `.global-module-title` 的 strict-mode 歧义，最小修正是限定唯一模块标题并保留管理卸载、入口隐藏和 403 断言。随机 loopback、纯内存 HTTP 与隔离 Chromium 诊断进一步确认，harness 在零响应头时直接 `response.destroy()` 会触发 Chromium 透明重发相同 PUT；drop 模式只消费一次，重发请求随后取得正常 200，故页面采用成功结果而不进入 unknown。等价实验为两条 PUT、首次 `ERR_EMPTY_RESPONSE`。先发送既有 200 与完整 `Content-Length`、仅交付部分响应体后断开则稳定为单条 PUT、`response 200`、`ERR_CONTENT_LENGTH_MISMATCH` 和 JSON 解析 TypeError；现有 API Client 会将该解析失败转换为 `ApiClientUnknownOutcomeError`，组件既有状态链会进入目标级 role/session unknown。当前证据关闭生产 P1 候选，归类为 QA 故障代理缺陷；精确范围见 `docs/architecture/平台角色与最小权限管理-V1-切片5-行动定位与响应丢失故障代理诊断裁决.md`。当前禁止修改测试或源码，须由产品经理另行授权仅修正切片 5 E2E 与 harness 并完整复测 8 项；私有凭据例外不得自动延续，V1 最终验收及所有真实运行库/部署动作继续禁止。

**平台角色与最小权限管理 V1—切片 5 行动定位与响应丢失故障代理最小修正及完整复测授权（2026-07-31）：** 产品已依据只读诊断签发独立授权，唯一工程修改为 `tests/platform-administration-v1-slice5.e2e.test.ts` 与 `tests/helpers/platform-administration-v1-slice5-harness.ts`。E2E 仅可将“行动”断言限定到唯一 `.global-module-title`，并补充单次目标写、Chromium response/requestfailed 及实际 unknown DOM 证据；harness 仅可令 drop 模式在上游写入完成后发送既有状态和完整 `Content-Length`、交付严格短于完整长度的响应体前缀再断开，并删除冲突的 `Transfer-Encoding`，不安全时必须明确失败。禁止修改生产代码或其他测试。修正后 QA 必须原样串行执行全部 8 项，证明单条 PUT、上游 200、Chromium `ERR_CONTENT_LENGTH_MISMATCH`、`ApiClientUnknownOutcomeError`、两类目标级 unknown 及仅显式 GET 后恢复。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-行动定位与响应丢失故障代理最小修正与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 故障代理修正复测仍未通过（2026-07-31）：** 行动唯一标题定位与截断响应故障代理均已验证有效：目标写仅一条、上游 200、operationId 脱敏，Chromium 收到 response 200 与 `ERR_CONTENT_LENGTH_MISMATCH`，页面进入目标级 unknown 且无透明重发。完整 8 项连续两轮均为 7 通过、1 失败，并稳定暴露新的生产 P1：B 保持 `role-unknown` 时，显式搜索 A 的 GET 虽返回 A，读协调仍保留 B 的旧快照，导致 A 行不可见；这违反已冻结的多目标逐项采用与独立解锁语义。`build:h5`、typecheck、`git diff --check` 通过；项目外临时目录、随机数据库和账号均为 0，harness finally 两运行库只读快照未报告变化。切片 5 继续不通过，须回流架构与前端冻结最小状态协调修复；真实 006、真实管理员授予、运行库写入、服务重启部署及 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 多目标 Role Unknown P1 架构冻结完成（2026-07-31，尚未授权修复）：** 根因是 `reconcileRoleUnknownRead` 在存在未返回目标 B 时以旧 `previous.items` 为合并底表，导致当前成功 GET 返回的 A 若不在旧页中便无法进入可见结果。架构冻结要求将当前可见页与未解析目标事实分离：每次有效 GET 的 `items/page/pageSize/total/order` 必须精确采用服务端结果，不得拼接旧页；每个 role-unknown 目标以仅前端内存 Map 独立保存形成代次及写入前最后一次服务端确认摘要，未返回目标继续保留记录、提示和锁但不得污染当前 query/page，返回目标只解析并解锁自身。sessions-unknown 继续不由列表 GET 确认。精确范围见 `docs/architecture/平台角色与最小权限管理-V1-切片5-多目标RoleUnknown读模型独立协调修复裁决.md`；当前禁止编码与复测，须产品经理另行授权仅修改两个管理前端状态文件和两个直接测试文件，并在工程门后原样完整复测 8 项。私有凭据例外不得自动延续；真实 006、真实管理员授予、运行库写入、服务重启部署及 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 多目标 Role Unknown P1 最小修复及完整复测授权（2026-07-31）：** 产品已依据架构冻结签发四文件独立授权。仅可修改 `platform-administration-state.ts`、`platform-administration.tsx` 及两个直接 H5 state/flow 测试：当前有效 GET 的可见 `PlatformUserPage` 必须精确采用服务端 `items/page/pageSize/total/order`，不得拼接 `previous.items`；每个 role-unknown 目标以仅前端内存 Map 独立保存形成事实 generation 和写入前最后一次服务端确认摘要，返回目标只解析自身，未返回目标继续保留记录、notice 和 lock 但不得污染当前 query/page；sessions-unknown 继续不由列表 GET 确认。禁止修改 E2E/harness、API Client、SCSS 或后端。前端工程门通过后，QA 必须原样串行执行全部 8 项，不调整 A/B 顺序或预先恢复 B。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-多目标RoleUnknown最小修复与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 多目标 Role Unknown 修复 QA 仍未完整通过（2026-07-31）：** 修复后原样 8 项 E2E 为 7 通过、1 失败；A/B 多目标真实链路已证明当前 GET 精确采用服务端页面、A 与 B 分别解析解锁且互不污染，原生产 P1 关闭。唯一失败转移至后续 503 恢复编排：测试在 `.platform-target-notice` 内以非精确文本定位“刷新用户列表”，实际点击提示正文文本；真实刷新按钮位于行内 actions，故未发起 GET，随后管理按钮等待超时。该项归类为 E2E locator 缺陷，未发现新生产 P0–P3，但完整 503、Abort、sessions-unknown 后半链路仍缺本轮通过证据。`build:h5`、typecheck、`git diff --check` 通过，临时目录、数据库和账号均为 0，harness finally 两运行库快照未报告变化。须由架构与产品另行授权仅修正该恢复按钮定位并重新完整执行 8 项；真实 006、真实管理员授予、运行库写入、服务重启部署及 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 503 恢复按钮定位最小修正架构冻结完成（2026-07-31，尚未授权修正）：** 架构核对确认生产 DOM 将 role-unknown 正文/requestId 放在 `.platform-target-notice`，真实“刷新用户列表”控件放在同一目标行 `.platform-user-actions`；当前 E2E 在 notice 内以非精确文本点击，只命中正文文本且未发出 GET。最小修正仅允许在 `tests/platform-administration-v1-slice5.e2e.test.ts` 的 503 恢复一步中冻结唯一目标行，notice 继续只断言文案/requestId，并在该行 actions 内以 exact 文本普通点击真实恢复控件；点击只允许新增一条列表 GET、零写入，GET 返回目标后才恢复管理按钮。精确裁决见 `docs/architecture/平台角色与最小权限管理-V1-切片5-503恢复按钮定位最小修正裁决.md`。当前禁止修改测试或复测，须产品经理另行授权单文件单点修正及完整 8 项；私有凭据例外不得自动延续，真实 006、真实管理员授予、运行库写入、服务重启部署与 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 503 恢复按钮定位单点修正及完整复测授权（2026-07-31）：** 产品已依据架构冻结签发单文件独立授权。唯一工程修改为 `tests/platform-administration-v1-slice5.e2e.test.ts` 的 503 恢复点击：以 `target503.username` 冻结唯一目标行，notice 仅断言 unknown 正文和 requestId，在该行 `.platform-user-actions` 内 exact 定位“刷新用户列表”并普通 Playwright 点击，随后等待同行动态管理按钮恢复。点击只允许新增一条列表 GET、零写入，只有 GET 返回目标后才采用服务端事实并解锁。禁止修改生产代码、harness、其他测试或调整后续场景。修正后 QA 必须原样串行执行全部 8 项。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-503恢复按钮定位最小修正与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 503 恢复定位通过、完整 QA 仍未通过（2026-07-31）：** 目标行 actions 内 exact 点击已证明 503 恢复满足点击前 role-unknown/管理按钮不可用、仅新增一条列表 GET、零新增 PUT、GET 返回目标后提示清除并恢复管理按钮。完整 8 项仍为 7 通过、1 失败，唯一失败推进到后续 Abort 恢复：E2E 在目标行内使用非精确 `getByText(刷新用户列表)`，同时命中按钮与 unknown 正文，strict mode 在点击前失败；该问题与 503 原 locator 缺陷同型，不构成新生产 P0–P3。本轮授权仅含 503 位置，未修改 Abort 步骤。`build:h5`、typecheck、`git diff --check` 通过，临时目录、数据库和账号均为 0，harness finally 两运行库快照未报告变化。须由架构与产品另行授权仅修正 Abort 恢复按钮定位并重新执行全部 8 项；真实 006、真实管理员授予、运行库写入、服务重启部署及 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 Abort 恢复按钮定位最小修正架构冻结完成（2026-07-31，尚未授权修正）：** 架构核对确认 Abort 写入已进入目标级 role-unknown，失败仅因 E2E 在整个目标行内非精确定位“刷新用户列表”，同时匹配 `.platform-user-actions` 的真实控件与 `.platform-target-notice` 的正文。最小修正仅允许在 `tests/platform-administration-v1-slice5.e2e.test.ts` 冻结唯一 `abortTargetRow`，notice 只精确断言正文，并在同行 actions 内 exact 普通点击真实恢复控件；点击前管理按钮不可用，点击只允许新增一条列表 GET、零新增 PUT，GET 返回目标后才清除 notice、解锁并恢复管理按钮。精确裁决见 `docs/architecture/平台角色与最小权限管理-V1-切片5-Abort恢复按钮定位最小修正裁决.md`。当前禁止修改测试或复测，须产品经理另行授权单文件单点修正及完整 8 项；私有凭据例外不得自动延续，真实 006、真实管理员授予、运行库写入、服务重启部署与 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 Abort 恢复按钮定位单点修正及完整复测授权（2026-07-31）：** 产品已依据架构冻结签发单文件独立授权。唯一工程修改为 `tests/platform-administration-v1-slice5.e2e.test.ts` 的 Abort 恢复点击：以 `abortTarget.username` 冻结唯一目标行，notice 仅精确断言 unknown 正文，点击前确认同行管理按钮不存在并记录列表 GET/目标 PUT 数量，在同行 `.platform-user-actions` 内 exact 定位“刷新用户列表”并普通 Playwright 点击，随后等待管理按钮恢复，断言仅新增一条 GET、PUT 不变且 notice 清除。禁止修改生产代码、harness、其他测试或后续 sessions-unknown 流程。修正后 QA 必须原样串行执行全部 8 项。产品同时重新签发本轮 `.env` root 连接值内存注入一次性例外，禁止读取 `.env.uat`、回显、持久化或写回，且不得延续。授权见 `docs/product/平台角色与最小权限管理-V1-切片5-Abort恢复按钮定位最小修正与复测授权.md`。切片 5 QA 仍不通过；真实 006、真实管理员授予、运行库写入、服务重启部署和 V1 最终验收继续禁止。

**平台角色与最小权限管理 V1—切片 5 独立 QA 通过（2026-07-31）：** Abort 恢复已按目标行 actions 内 exact 普通点击完成单点修正，并证明点击只新增一条列表 GET、目标 PUT 不变、GET 返回目标后 notice 清除且管理按钮恢复。修正后随机 Schema 6 全链路 E2E 全部 8 项通过，覆盖 Schema 启动门、初始管理员、真实 Cookie/H5、分页搜索、角色/会话管理、403/404/409、actor 降级、并发互撤、A/B 多目标 role-unknown、503、Abort、截断响应、sessions-unknown、显式恢复、owner/Backup 不扩权及敏感扫描。`build:h5`、typecheck、`git diff --check` 通过；临时目录、数据库和账号均为 0，浏览器/端口/进程由 finally 清理；`knowledge_base` 与 `knowledge_base_uat` 前后只读深度快照分别 `SNAPSHOTS_IDENTICAL`。最新报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。当前仅允许转产品经理执行 V1 最终产品验收；QA 通过不自动授权真实 006、真实管理员授予、运行库写入、服务重启部署或封板。

**平台角色与最小权限管理 V1 最终产品验收与 Git 独立归档完成（2026-07-31）：** 切片 1–5 已满足冻结产品范围与最终验收门。随机 Schema 6 全链路 E2E 1 文件 8 项全部通过，覆盖 Schema 启动门、初始管理员 CLI、真实 Cookie 与角色刷新、24 用户分页搜索、角色/会话管理、403/404/409、自操作、actor 降级、并发互撤、A/B 多目标 role-unknown、503、Abort、截断响应、sessions-unknown、显式恢复及 owner/Backup 不扩权；`build:h5`、typecheck、`git diff --check` 通过，临时资源为 0，两运行库前后分别 `SNAPSHOTS_IDENTICAL`，敏感扫描通过且无新增 P0–P3。产品验收见 `docs/product/平台角色与最小权限管理-V1-最终产品验收结论.md`，QA 报告见 `docs/architecture/平台角色与最小权限管理-V1-切片5-独立QA报告.md`。V1 已以独立提交 `5c1c52f feat(auth): add platform role administration V1` 完成受控 Git 归档（74 files），但尚未封板。两个运行库仍为 15 表、Schema 5、平台表 0；真实 006、真实管理员授予、运行库写入、服务重启部署与运行验收继续禁止。

**平台角色与最小权限管理 V1 真实 Schema 6 接入与运行验收授权（2026-07-31）：** 用户已书面授权本机 `knowledge_base_uat` 与 `knowledge_base` 按“UAT 先行验证、日常库随后接入”的顺序执行既有 006 Migration，各自仅对唯一既有 `admin` 账户使用显式 `userId` 和 expected database 执行初始管理员 CLI，并完成 API/H5 运行验收。执行前必须停止当前 API/H5、保存两库完整逻辑备份和前置摘要；006 失败不得回退 DDL或篡改 migration record，必须保留现场并回流架构；CLI commit 后结果未知只能重读角色与审计判定。禁止 claim、Backup restore、业务数据改写、账号/grants/密码修改、Docker 应用镜像、云端或公网部署。精确授权见 `docs/product/平台角色与最小权限管理-V1-真实Schema6接入与运行验收授权.md`。在本授权实际完成前，两个运行库仍按 15 表、Schema 5、平台表 0 事实处理。

**平台角色 V1 真实 Schema 6 首次接入被 checksum 安全门阻断（2026-07-31）：** 写入前已停止 API/H5，并在仓库外 `维护相关/运行库备份/Schema6接入-20260731-145204` 保存两库完整压缩备份、十集合业务快照及 SHA-256 清单。UAT migration 在任何 006 新增 DDL 前发现既有 005 checksum 不一致并停止：数据库登记值与 Git 归档 LF 内容一致，但 Windows 工作树将 005 检出为 CRLF，当前 migration loader 按原始换行计算出不同哈希。失败后 `knowledge_base` 与 `knowledge_base_uat` 的十集合业务压缩快照分别前后一致，均保持 15 表、Schema 5、平台表 0；日常 migration、两个初始管理员 CLI 均未执行。MySQL 继续健康，API/H5 当前停止。不得改写 migration record、手工执行 006 或临时绕过 checksum；须先冻结并授权最小跨平台 checksum 处理，再从 UAT 重新开始。

**平台角色 V1 UAT Schema 6 功能接入通过、H5 视觉验收不通过（2026-07-31）：** Migration loader 已以“既有记录兼容原始或 LF checksum、新记录固定 LF checksum”的最小策略关闭 Windows 换行误报，随机临时库定向 1 文件 3 项、typecheck 与 `git diff --check` 通过，临时资源为 0。`knowledge_base_uat` 随后成功执行既有 006，当前为 17 张表、Schema 6，唯一既有 `admin` 拥有 `member + platform_admin`，初始授予审计 1 条；真实 health 为 `ready / knowledge_base_uat / schemaVersion=6`，H5、登录、工作台、用户管理入口与未认证 401 功能链通过。真实页面截图同时确认多个 Taro 按钮文字不可见或对比不足、用户行/注册时间/分页字号失控、搜索输入尺寸与层级不协调，故只能判定功能通过，H5 视觉产品验收不通过。日常 `knowledge_base` 仍为 15 张表、Schema 5、平台表 0，日常 006 与初始管理员均未执行；须先完成最小设计冻结与前端修复复验，再决定是否继续日常接入。

**平台角色 V1 H5 用户管理页视觉 P1 最小前端修复授权（2026-07-31）：** 产品已依据 `docs/design/平台角色与最小权限管理-V1-H5用户管理页视觉P1设计冻结.md` 签发独立授权。生产修改仅限 `apps/client/src/pages/index/index.scss`，直接测试仅限 `tests/platform-administration-h5-flow.test.ts`，只可修复冻结的字号、按钮可见性、搜索布局、四列表格密度、分页层级及页面内原生 `button`/Taro `taro-button-core` 视觉一致性；当前无 TSX 修改授权。工程门包括直接视觉测试、既有用户管理与完整 H5 回归、typecheck、`build:h5`、`git diff --check`，并须在隔离 mock 环境完成 `1440×900`、`1280×720`、`390×844`、`360×800` 的 100% 缩放截图验收。禁止读取私有环境、连接 API/MySQL、恢复 UAT、继续日常 006，或修改任何业务、权限、请求、状态与后端边界。授权见 `docs/product/平台角色与最小权限管理-V1-H5用户管理页视觉P1最小前端修复授权.md`；完成后只转独立 QA 与产品视觉验收。

**平台角色 V1 H5 用户管理页视觉 P1 独立 QA 不通过（2026-07-31）：** 字号、按钮文字、搜索布局、桌面四列/64px 行、小屏卡片和确认层的截图复核通过，直接视觉 1 文件 9 项、用户管理 5 文件 53 项、前端广域 17 文件 121 项以及 typecheck、`build:h5`、`git diff --check` 通过；但桌面第 1/2 页截图中应禁用的“上一页”与可用“下一页”均呈正常背景 `#FFFDF9`，未采用冻结禁用背景 `#EEEAE4`，肉眼不可区分，定级 P2。报告见 `docs/architecture/平台角色与最小权限管理-V1-H5用户管理页视觉P1-独立QA报告.md`。当前回流前端仅修复真实分页宿主禁用视觉并补 computed-style 证据；产品视觉验收、日常 Schema 6 接入、服务恢复、归档和封板继续暂停。

**平台角色 V1 分页禁用展示 class 最小扩展授权（2026-07-31）：** 前端真实 DOM 诊断确认，Taro 将首页业务禁用与末页业务启用的“上一页”均可能输出 `disabled=""`，CSS 无法可靠区分且不得匹配 `[disabled]`、空属性、位置或文案推断。产品现仅授权在 `platform-administration.tsx` 为两个分页按钮增加基础 `platform-pagination-button`，并分别直接复用既有 `page <= 1 || refreshing`、`page >= pageCount || refreshing` 表达式附加 `platform-pagination-button-disabled`；原 `disabled`、`onClick`、`changePage` 和全部分页/请求语义必须保持不变。SCSS 只可在分页作用域按该 class 应用冻结禁用视觉，直接测试只验证标记来源与样式边界。精确授权见 `docs/product/平台角色与最小权限管理-V1-H5用户管理页分页禁用展示标记最小扩展授权.md`。完成后必须原四视口完整复测首页、末页和中间页的 computed style、命中与单次 GET；未经 QA 与产品视觉验收，不得继续日常 Schema 6、恢复服务、归档或封板。

**平台角色 V1 H5 用户管理页视觉 P1 独立 QA 通过（2026-07-31）：** 分页禁用展示单点修复已关闭上一轮 P2。隔离最终构建与 mock 数据下，1440×900、1280×720、390×844、360×800 四视口的首页、中间页、末页共 12 张截图确认：首/末页对应按钮采用冻结禁用颜色且不可点击，中间页按钮均启用，启用点击每次仅新增一条列表 GET，页码单行且无横向溢出。直接视觉 1 文件 10 项、用户管理 5 文件 54 项、当前 H5 去重回归 18 文件 137 项以及 typecheck、`build:h5`、`git diff --check` 均通过，无新增 P0–P3。报告见 `docs/architecture/平台角色与最小权限管理-V1-H5用户管理页视觉P1-独立QA报告.md`。当前仅转产品经理执行视觉验收；日常 Schema 6 接入、服务恢复、归档和封板仍暂停。

**平台角色 V1 H5 用户管理页视觉 P1 产品验收通过（2026-07-31）：** 产品已只读复核 1440×900、1280×720、390×844、360×800 四视口首页、中间页和末页共 12 张原图。桌面四列表格、搜索与操作层级清晰，小屏卡片降级无横向溢出；首/末页禁用态与中间页启用态可明确辨认，页码保持单行，未发现新的 P0–P3。验收记录见 `docs/product/平台角色与最小权限管理-V1-H5用户管理页视觉P1产品验收.md`。上一轮视觉阻断已关闭，可恢复既有真实 Schema 6 接入与运行验收流转；`knowledge_base_uat` 已为 17 表、Schema 6 且功能链此前通过但 API/H5 当前停止，`knowledge_base` 仍为 15 表、Schema 5、平台表 0，日常 006 与初始管理员尚未执行。本轮视觉修复的 Git 独立归档和 V1 封板仍须等待真实运行验收完成后另行裁决；既有 V1 功能提交 `5c1c52f` 的归档事实保持不变。

**平台角色 V1 日常 Schema 6 接入与服务恢复已完成（2026-07-31）：** 执行前确认 API `32146` 与 H5 `10086` 均停止，`.env` 明确指向 `knowledge_base`，唯一既有 `admin` 为 `9f9d3111-fe83-465e-9f56-9d5150c204ca`；完整逻辑备份及前置摘要保存在项目外 `维护相关/运行库备份/Schema6接入-daily-20260731-163920`。已使用仓库既有 migration runner 执行 006，随后仅以显式 userId、expected database 和 `--apply` 执行一次初始管理员 CLI，返回 `granted`；只读复核确认日常库 17 表、Schema 6、唯一 admin 同时拥有 `member` 与 `platform_admin`，初始管理员审计 1 条，001–006 migration 记录完整。API/H5 已恢复，health 为 `ready / knowledge_base / schemaVersion=6`，H5 `/index.html` 返回 200，未认证 API 与管理入口均返回既有 401。该步骤完成时认证后的登录、工作台及用户管理入口尚未验证，实际 admin 登录密码未被读取、猜测、重置或写入；此阶段状态已由下一条产品运行验收事实替代。未执行 claim、Backup restore、业务数据改写、账号/grants/密码修改、Docker 应用镜像、云端或公网部署。

**平台角色 V1 日常 Schema 6 与认证基础运行产品验收通过（2026-07-31）：** 用户使用既有凭据完成 `admin` 登录，登录后工作台正常挂载，“用户管理”入口可见并可进入；结合已确认的 17 表、Schema 6、`member + platform_admin`、初始管理员审计 1 条、`ready / knowledge_base / schemaVersion=6`、H5 200 及十个业务集合与 `system_metadata` 前后摘要一致，产品确认真实 Schema 6 接入与认证基础运行验收通过。未输出、持久化或重置密码，未执行 claim、Backup restore、业务写入或其他账号操作。产品裁决见 `docs/product/平台角色与最小权限管理-V1-真实Schema6运行验收与最终归档封板裁决.md`：现授权将跨平台 checksum 修复、视觉 P1 修复和最终状态记录拆成三个精确 Git 提交；提交号登记前 V1 尚未正式封板，不得使用 `git add -A` 或混入私有配置、备份、运行数据及其他脏改动。

**平台角色与最小权限管理 V1 最终归档与封板完成（2026-07-31）：** 产品功能、随机 Schema 6 QA、UAT/日常真实 Schema 6、初始管理员、认证基础运行和 H5 用户管理页视觉 P1 均已通过验收。跨平台 Migration checksum 安全修复已以 `f14ce64 fix(db): normalize migration checksums across platforms` 归档，视觉 P1 已以 `ffd2544 fix(h5): polish platform administration visuals` 归档，真实运行授权、验收与最终状态记录随本封板记录完成独立文档归档；全部候选均按精确文件清单暂存，未使用 `git add -A`，未混入私有配置、备份、运行数据或其他脏改动。封板裁决见 `docs/product/平台角色与最小权限管理-V1-真实Schema6运行验收与最终归档封板裁决.md`。V1 现正式封板；后续新增角色、权限、协作、部署或视觉需求必须重新立项。

## 持续禁止

**在线账户与单用户数据隔离 V0 最终封板（2026-07-30）：** 切片 0–5 的 Schema、认证 API、全业务 owner 隔离、初始归属 claim、用户范围 Backup、H5 登录门和独立临时库端到端 QA 均已通过，UAT 凭据 P1 亦已关闭。真实 H5/API 临时库端到端 1 文件 3 项、账户 Schema/认证/owner 隔离/claim/Backup 4 文件 11 项、完整 MySQL 回归 13 文件 135 项、H5 去重回归 15 文件 97 项以及 typecheck、`build:h5`、`git diff --check` 均通过；临时资源零残留，两个运行库前后只读摘要一致。产品最终验收与 Git 独立归档均已完成，归档提交为 `1ac6021 feat(auth): add single-user data isolation V0`（50 files）。品牌、探索主线、Docker、其他历史任务、构建产物、运行数据与私有 `.env`、`.env.uat` 均未纳入该提交。V0 现已封板；未经新的产品立项，不得向该封板范围插入新需求。Tauri、安卓、同步、协作、HTTPS/域名及云端部署仍不属于 V0；本机 UAT Schema 5 重建属于封板后的独立运行授权，不回写或扩张 V0 范围。

```text
IndexedDB / MySQL 双写、同步、回填、fallback 或合并展示
浏览器直连 MySQL
真实 IndexedDB 历史数据迁移（除非用户重新明确提出）
远程 / 公网 API、反向代理或隧道（Docker H5 LAN 与本地 Vite H5 测试的已授权例外除外）
团队、协作共享、自定义或动态权限、JWT 与其他未立项认证扩展；平台角色 V1 已完成产品验收、真实运行接入、受控 Git 独立归档并正式封板，后续扩展必须重新立项；云端或公网部署仍禁止
Kubernetes、云端同步、协作
```

**fast-ui H5 信息架构重构（2026-08-10）**：当前 `fast-ui` 分支已将 H5 主导航收敛为首页、行动工作台、圈圈 AI 助手、数据管理和我的五项；行动工作台保留行动/长期探索/方法页签，管理员能力收拢至“我的”管理区域。首页上半区当前仅放置本地主题轮播，支持自动轮播、点击切换和 `prefers-reduced-motion` 降级；其余首页内容等待后续产品定义。Framer Motion 页面壳层动效已撤回，当前保持静态页面布局；本轮未修改 API、数据库、Prompt、SSE、AI/Rive 业务逻辑，尚未完成完整人工验收，也未合并 main 或触发生产 CI/CD。
**Tauri 桌面端第一阶段（2026-08-10）**：`fast-ui` 分支新增 Tauri v2 壳层配置，桌面端加载现有 H5 构建产物；API Client 支持通过 `TARO_APP_API_BASE_URL` 指向云端 API，未设置时继续使用同源 `/api`。API CORS 增加 Tauri 本地来源白名单。当前机器尚未安装 Rust/Cargo，尚未生成 Windows 安装包；本阶段不引入 SQLite、不修改 API 契约、数据库结构或 AI 业务逻辑。
**Tauri 桌面端第一阶段（2026-08-10）**：`fast-ui` 分支新增 Tauri v2 壳层配置，桌面端加载现有 H5 构建产物；API Client 支持通过 `TARO_APP_API_BASE_URL` 指向云端 API，未设置时继续使用同源 `/api`。API CORS 增加 Tauri 本地来源白名单。当前开发机已安装 Rust MSVC、Visual C++ Build Tools 和 WebView2，并成功生成 Windows MSI 与 NSIS 安装包；安装包尚未使用实际云端 API 地址进行最终连接验收。本阶段不引入 SQLite、不修改 API 契约、数据库结构或 AI 业务逻辑。

**桌面端静态资源内置（2026-08-10）：** `fast-ui` 分支的 H5 构建已将当前桌宠 Rive、Markdown Worker、首页轮播 SVG 和静态帮助文档纳入 `apps/client/dist`；Tauri NSIS 构建已成功生成 `src-tauri/target/release/bundle/nsis/MaruMaru_0.1.0_x64-setup.exe`。当前没有自定义字体文件，继续使用系统字体栈。固定资源随版本发布，账户、会话和业务数据仍通过 API 与 MySQL 获取；`src-tauri` 继续被 Docker 构建上下文排除。

**fast-ui 当前开发边界（2026-08-10）：** 本分支当前专注 Tauri 桌面应用的窗口布局、桌面端交互和本地开发联调；暂不处理 H5 独立页面的视觉改造、H5 专项验收或无关页面需求。桌面端开发仍复用 H5 编译产物作为 WebView 内容来源，但桌面端验收以 Tauri 窗口行为为准。

## 2026-08-10 fast-ui 桌面端原生化事实

- 当前开发分支为 `fast-ui`，本阶段专注 Tauri 桌面窗口布局与系统交互，不进行 H5 独立视觉验收。
- 桌面端继续复用 React/Taro 编译后的 WebView 内容，不使用 SQLite；业务数据、会话和 AI 请求仍通过 API 获取。
- 已加入无框透明窗口、自定义标题栏、关闭隐藏到托盘、托盘恢复/退出和桌面快捷键。
- 工作台已按行动、长期探索、方法使用三栏结构，中栏宽度当前支持 280px～420px 调整，默认 340px。
- 生产 NSIS 构建使用 `apps/client/dist` 内置静态资源，不依赖开发端口 `10086`；开发模式仍允许使用 `10086` 热更新。
- AI 页面继续保持现有双栏、流式回复、桌宠和 SSE 逻辑；本轮未修改 API、数据库、Prompt、SSE 或业务语义。
- Windows 11 多屏/125%～150% 高 DPI、托盘 Alt + Tab、最大化边缘和安装后业务链路尚未完成实机人工验收。
- `Alt + Space` 若与 Windows 或其他软件冲突会静默降级，不再阻断桌面端启动。
- 本地桌面联调可执行 `corepack pnpm desktop:api`，由 `scripts/desktop-api.mjs` 加载 `.env`、确保 MySQL 可用并启动 API；桌面客户端仍使用内置前端资源。
- `corepack pnpm --filter @knowledge-base/client tauri:dev` 先构建 `apps/client/dist` 再启动 Tauri，不启动 10086 或 10087；Web 端仍通过 `corepack pnpm dev` 使用 10086。
- `fast-ui` 分支的默认 `corepack pnpm dev` 仅启动桌面端所需的 MySQL/API 联调服务，不启动 10086；Web 端开发改用 `corepack pnpm dev:web`。

## 使用规则

**平台管理员与普通管理员权限分层（2026-08-09）**：** 源码已增加 `member`、`ordinary_admin`、`platform_admin` 三级角色契约。平台管理员是唯一最高层级；普通管理员仅可管理普通成员，不能查看或操作其他管理员；AI 参数配置仍仅平台管理员可用。Migration 016 负责初始平台管理员不可变标识与平台管理员唯一约束；本轮仅修改代码、契约、Migration、测试和文档，不执行生产库迁移。

**本地 Docker 应用镜像发布（2026-07-31）：** 已基于 `main` 提交 `68202d4` 本机构建并推送既有阿里云镜像仓库的 `dnf:1.1.3` 与 `dnf:latest`；两标签均对应 digest `sha256:e2b322df2ef9924bd7e9624bd8d5698b7321dda26d6356ab34e92ae1ad926c1e`。构建内 `build:h5` 通过，仅保留既有 Sass legacy API 弃用警告。本次未执行 Docker Compose、migrate、reconcile-users、数据库写入、服务重启或部署；版本号标签保留用于镜像追溯。

**阿里云测试机首次受控试运行授权（2026-07-31）：** 用户已明确授权仅在其测试服务器使用既有根目录 `docker-compose.yml` 与私有 `.env` 拉取并启动 `dnf:latest`，用于通过 SSH 隧道查看效果；不开放公网端口、不执行 `down` 或删除数据卷。启动会按既有 Compose 行为执行账号权限收敛和 Migration；脚本须在检测到既有 `knowledge_base_mysql_data` 数据卷时要求显式确认。部署后管理员仍必须由正常注册用户经受控初始管理员 CLI 显式授予，禁止默认管理员或默认密码。

1. 每次开始产品、设计、架构、前端、后端或 QA 工作前，先读取本文。
2. 任务书和交接稿只引用本文中的当前事实；不得复制旧 M 文档中的运行主库口径。
3. 当前运行路径、端口、数据源、阶段或禁止事项变化时，先更新本文，再实施或流转。
4. 历史文档发现冲突时，追加“历史结论，已被当前运行事实替代”的说明；不重写历史验收事实。

**Tauri 桌面端云端连接版构建事实（2026-08-10）**：`fast-ui` 分支已成功生成 Windows MSI 与 NSIS 安装包。构建使用客户端忽略文件 `apps/client/.env.production` 注入 `http://47.97.69.175:10086`，并已在 `apps/client/dist` 产物中确认该地址；桌面端不使用 SQLite，仍通过 API 访问云端 MySQL 业务服务。云端 `GET /health` 返回 `ready / knowledge_base / schemaVersion=15`，未认证的 `GET /api/v1/auth/session` 返回 401，说明入口和认证门正常响应。尚未完成安装包安装后的完整人工业务验收；当前公网入口仍为 HTTP，正式使用前应配置 HTTPS 域名。此前“尚未使用实际云端 API 地址打包”的 Tauri 记录已被本条替代。

桌面端补充事实：H5/Tauri 标题区域已增加浏览器在线状态提示，断网时显示“网络已断开”，恢复联网后自动隐藏；未增加 SQLite、离线业务数据、自动更新或原生文件存储能力。

**本地桌面联调事实（2026-08-10）**：当前桌面测试包通过客户端忽略环境文件连接 `http://127.0.0.1:32146`。本机 `knowledge_base` 已在明确授权后执行 Migration 016，当前 API health 为 `ready / knowledge_base / schemaVersion=16`；`Origin: tauri://localhost` 的未登录会话请求返回 401 且 CORS 来源头正常。云端 API 未被本轮操作。

本地桌面联调补充：桌面测试包 API 地址已切换为 `http://localhost:32146`；Tauri 来源的登录 Cookie 使用跨站会话属性，普通 H5 Cookie 行为保持不变。最新 NSIS 包尚未完成人工登录验证。
- **Docker 应用镜像发布（2026-08-06）**：已基于当前工作区构建并推送 `crpi-v8ex1zrhoe87bb3d.cn-hangzhou.personal.cr.aliyuncs.com/my-acr-demo/dnf:1.1.4` 与 `:latest`；两标签均指向 digest `sha256:4dbdc2e6be80888480f12b8561ed14210ae0cb21c598b37973c1ffd172165cf8`。本次包含文件型 AI SecretStore、命名卷持久化和 Dockerfile 路径修复；尚未在新机器执行容器重建后的 API Key 持久化人工验收。

**GitHub Actions 镜像发布配置（2026-08-08）**：仓库 `liukai-weibo/muramura` 的 `main` 与 `v*` 标签已配置自动构建并推送阿里云镜像仓库的工作流 `.github/workflows/docker-image.yml`。每次构建同时向阿里云 ACR 的 `my-acr-demo/dnf` 和 GHCR 的 `ghcr.io/liukai-weibo/muramura` 推送 `latest` 与 `build-{github.run_number}` 两个标签；工作流依赖 GitHub Secrets `ACR_USERNAME` 与 `ACR_PASSWORD`，GHCR 使用内置 `GITHUB_TOKEN`。Secrets 尚未完成首次云端运行验证。工作流不读取或提交本地 `.env`、`.env.uat` 及其他私有凭据。

**GitHub Container Registry 镜像发布配置（2026-08-08）**：同一工作流同时推送 `ghcr.io/liukai-weibo/muramura`，使用 GitHub Actions 内置 `GITHUB_TOKEN` 和 `packages: write` 权限，不需要额外账号密码。GHCR 首次发布仍需等待工作流运行，并可能需要在 GitHub Packages 页面调整镜像可见性。

**H5 AI 会话操作菜单层级修复（2026-08-09）**：打开会话的重命名/删除菜单时，为当前会话行提升局部层级，并将菜单定位到行下方安全间距，避免相邻会话内容覆盖菜单；未修改会话命名、删除、切换和持久化逻辑。

**GitHub Actions 云服务器 CD 配置（2026-08-09）**：Docker 镜像构建推送成功后，工作流 `.github/workflows/docker-image.yml` 会使用 GitHub Secrets 中的 SSH 连接信息登录云服务器，在 `/root` 目录执行既有 `./start.sh --ci`。服务器启动脚本的 `--ci` 仅在显式传入时跳过已有数据卷的交互确认，手动执行仍要求输入 `DEPLOY`；服务器地址、用户名、密码和端口均不写入仓库。首次自动部署仍待修订后的脚本同步到服务器并完成验证。部署脚本、服务器 `.env`、数据库和运行数据不纳入 Git。

**Migration CRLF 历史兼容修复（2026-08-09）**：云服务器 `knowledge_base.schema_migrations` 中的 `001_initial_schema.sql` 校验和确认对应同一文件的 CRLF 历史内容，而当前 Linux 镜像使用 LF 内容；已在 `packages/storage-mysql` 增加同一规范化内容的 CRLF 兼容校验和，并补充回归测试，不修改数据库记录、不放宽实际内容漂移检查。
**回收站永久删除与批量操作（2026-08-09）：** H5 回收站默认收起，可展开/收起；事项、方法、探索主线支持单条永久删除，当前筛选结果支持跨分页全选与批量永久删除，单条和批量均需二次确认。批量由同一 MySQL 事务编排，任一失败整批回滚；永久删除探索主线会在同一事务中清理其关联事项。微信小程序暂不纳入本轮验收。
**探索主线级联删除与回收站详情（2026-08-09）：** 探索主线软删除会在同一事务中软删除当时仍有效的关联事项，并写入级联删除时间；恢复仅恢复与主线原删除时间匹配的级联事项，不恢复用户此前单独删除的事项。回收站点击已删除探索主线可读取并展示全部仍绑定事项及其删除状态；永久删除探索主线会在同一事务中永久清理关联事项。删除、恢复和永久删除后由前端重新读取事项、回收站和探索事实，避免依赖手动刷新。Migration 015 新增可空级联标记字段；微信小程序不纳入本轮验收。
**fast-ui 桌面顶轨收敛（2026-08-10）：** 桌面端顶栏现为全窗口贯穿的深色顶轨，工作台页签、搜索和快速捕获集中在顶轨；一级侧栏从顶轨下方开始。行动日历、数量和紧凑状态筛选位于事项中栏，桌面工作台不提供常规主动刷新按钮，数据由进入页面和业务操作后的自动刷新维护。桌面端布局未修改 API、数据库、业务状态或 Web 端行为；Windows 多屏、高 DPI、托盘 Alt + Tab 和最大化边缘仍待人工验收。

**MaruMaru 品牌图片统一（2026-08-11）：** `src-tauri/Gemini_Generated_Image_p4vo21p4vo21p4vo.png` 作为品牌图片母版；已生成去除背景的全身透明猫咪图与方形透明头像图，桌面安装包、窗口、托盘及静态客户端品牌图片使用同源透明资源。首页轮播统一使用透明猫咪图；AI 桌宠 `.riv` 保留动画功能，不替换为静态图片。该调整不涉及 API、数据库、业务状态或会话数据。

**