# 平台角色与最小权限管理 V1—H5 用户管理页视觉 P1 产品验收

日期：2026-07-31

状态：产品视觉验收通过；恢复真实 Schema 6 接入与运行验收流转，本轮视觉修复尚未独立归档，V1 尚未封板

## 结论

平台角色 V1 H5 用户管理页视觉 P1 产品验收通过。上一轮分页禁用视觉 P2 已关闭，本轮未发现新的 P0–P3 问题。

本结论仅确认用户管理页局部视觉满足冻结设计和可用性要求，不改变角色、权限、搜索、分页、请求、写锁、operationId 或 unknown-outcome 语义，也不替代真实日常库接入与运行验收。

## 产品复核范围

产品只读复核了隔离 mock 环境最终构建的 12 张原始截图：

- 桌面：1440×900、1280×720；
- 小屏：390×844、360×800；
- 每个视口均覆盖首页、中间页和末页。

截图目录：`%TEMP%\marumaru-platform-admin-pagination-20260731`。

## 验收结果

- 桌面端标题、说明、搜索输入、刷新/搜索按钮、四列表格、角色标签、当前账号标识及分页形成清晰层级；字号和行高未出现失控或异常断行。
- 小屏正确降级为卡片，搜索输入与操作按钮可读，长用户名安全省略，页面无横向溢出。
- 首页“上一页”和末页“下一页”采用明确禁用视觉；中间页两个分页按钮均呈启用视觉。
- 禁用态与启用态可由背景、文字和边框层级直接辨认，页码在四个视口均保持单行。
- QA 已证明禁用按钮不可点击且产生零列表 GET；启用按钮每次真实点击仅新增一条列表 GET。
- 未观察到文字不可见、按钮命中盒异常、搜索布局失衡、表格密度失控或分页层级混淆。

## QA 与工程证据

- 直接视觉测试：1 文件 / 10 项通过。
- 用户管理回归：5 文件 / 54 项通过。
- 当前 H5 去重回归：18 文件 / 137 项通过。
- typecheck、`build:h5`、`git diff --check` 通过；构建仅有既有 Sass legacy API 弃用警告。
- QA 报告：`docs/architecture/平台角色与最小权限管理-V1-H5用户管理页视觉P1-独立QA报告.md`。

## 边界与后续

- 本轮未读取 `.env`、`.env.uat`，未启动或连接 API/MySQL，未执行 Migration 或运行库写入。
- `knowledge_base_uat` 的 Schema 6 功能链此前已通过；其 API/H5 当前保持停止。
- `knowledge_base` 仍为 15 张表、Schema 5、平台表 0，尚未执行 006 或初始管理员授予。
- 视觉验收通过后，可恢复既有“真实 Schema 6 接入与运行验收”流转；任何日常库写入仍必须严格按原独立授权和安全门执行。
- 本轮视觉修复的 Git 独立归档与 V1 封板仍须在真实运行验收完成后另行裁决；既有 V1 功能提交 `5c1c52f` 的历史归档事实保持不变，本次不自动授权新提交或封板。

## 当前状态

- H5 用户管理页视觉 P1：产品视觉验收通过。
- 平台角色 V1：真实日常 Schema 6 接入与运行验收尚未完成。
- 当前不允许借本验收扩大到其他视觉、业务、权限或部署范围。
