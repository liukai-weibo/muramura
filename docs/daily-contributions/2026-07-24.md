# 2026-07-24

## 增加项

- 增加单一 MySQL 容器内 `knowledge_base` 与 `knowledge_base_uat` 双库、四个独立 app / migrator 最小权限初始化与收敛机制。
- 增加同容器 UAT 启动、独立迁移与仅重建 `knowledge_base_uat` 的受限 reset 流程。
- 增加执行中事项的纯 UI “开始复盘”入口、历史待复盘兼容入口与“更多状态”导航。
- 增加 UAT 乱码合成事项清理、H5 创建干净中文状态导航验收样本、执行—复盘唯一入口与 MySQL Sprint 封板前浏览器闭环验证记录。
- 增加 004 migration 在执行任何 DDL 前的同连接、advisory lock 内对象预检及临时库回归证据。
- 增加 MySQL 集成测试的显式串行文件调度入口，隔离单容器 migration 与临时用户资源竞争。
- 增加 `mysql-m2a` 集成测试的随机临时数据库、独立 app / migrator 用户与清理夹具，隔离日常运行库。
- 增加 Schema 004 UAT 部署前后的跨库容器 Unix socket 只读保护快照脚本。
- 增加 Schema 004 UAT 的固定 Items 既有字段投影与完整快照双口径比较证据。
- 增加 Schema 004 UAT 双口径比较的离线 Profile eligibility 门与合成快照负向回归。
- 增加探索主线 S1 的基础结构化 Contracts：主线、显式选择、Item 关联上下文与冻结读模型类型。
- 收紧探索主线关联上下文的生命周期类型边界：可用主线拒绝删除时间字符串，已删除主线必须保留删除时间字符串。
- 增加探索主线 S2 的 MySQL 主线 Repository、结构化读模型与单一事务创建/关联工作流。
- 增加探索主线 S2 的 Application 名称规范化、原子 workflow 委派与随机临时库真实 MySQL 回归。
- 增加 `test:mysql:integration` 对探索主线 S2 临时库集成回归的固定串行覆盖。

增加流转强命令格式；
增加项目最新状态统一文档，对齐各个会话；
数据库完美迁移搞定；
探索主线文档搞定；
执行态，执行态-复盘态完成
由双容器数据库改为单容器双库形式搞定；

## 修复项

- 恢复根目录 `node_modules/` 的锁文件依赖安装，修复 `html-webpack-plugin` 缺失导致的 H5 编译失败。
- 完成 UAT 浏览器端复盘提交失败草稿保留与 API 恢复后重提的 P1 复验。
- 完成 MySQL 快速接入 Sprint 集中 UAT、产品验收与封板，并切换至探索主线 V1 的 S1 实施阶段。
- 完成探索主线 V1 S1 Schema 004 与基础 Contracts 的产品验收封板。
- 授权探索主线 V1 S2 的 MySQL Repository、Application 与原子工作流限定编码。
- 修复已软删除探索主线关联仍可被改派或移除的问题，收敛为只读历史事实并拒绝关联调整。
- 授权探索主线 V1 前端可视化原型与 S2 并行实施。
- 完成探索主线 V1 S2 Repository / Application 与原子工作流的产品验收封板。

- 修复 UAT 清库误用 `docker compose down -v` 无法清除 bind mount 历史数据的问题，收敛为同一 MySQL 容器内仅重建 `knowledge_base_uat` 的隔离流程。
- 修复新流程经公共状态迁移创建 `waiting_review` 的旧路径，收敛为 `doing` 仅由复盘工作流原子完成至 `reviewed`。
- 修复复盘提交失败后草稿被清空的问题：仅在 `POST /api/v1/reviews/complete` 成功响应后关闭编辑器并清空输入。
- 修复复盘提交在服务恢复后读取默认状态快照的问题：提交改为使用与 UI 输入同步的最新草稿快照。
- 修复状态导航的人工分组与右上角更多状态布局，收敛为桌面端固定 3×2 网格并提供小屏两列降级。
- 修复历史待完成复盘事项详情缺少删除入口的问题，复用既有软删除与状态保留行为。
- 修复复盘失败恢复后提交仍使用默认值的问题，改为提交输入事件同步的当前草稿快照。
- 修复执行中事项详情仍显示“执行完成”旧入口的问题，收敛为“开始复盘”作为唯一主入口。
- 修复详情底部相邻操作栈与删除操作的纵向间距不一致：按 Taro H5 换算调整为实际可见 9px 间距，并以浏览器实测复验。
- 修复无补充内容时“暂无说明。”被误读为正文的问题，使其与“补充：”使用同级浅色标签层级。
- 修复已复盘事项补充说明因弹性最小高度导致详情留白的问题，收敛为最多两行的浏览态摘要，并让复盘证据与操作区回归自然文档流。
- 撤销补充说明正文颜色覆盖规则，恢复至两行摘要任务完成时的视觉版本。
- 调整详情底部操作组视觉密度：将同组按钮间距从 22.5px 收窄至 12px。
- 修复已复盘详情将未填写的“有效 / 舒服”“阻力 / 不舒服”默认占位文案展示为复盘证据的问题，并将补充说明前置到复盘证据之前。
- 修复补充说明浏览态折叠显式换行与编辑框提前滚动的问题，保留两行摘要并支持编辑态最多十行自适应高度。
- 修复补充说明保存提示占据详情文档流并推动操作按钮的问题，改为无交互的固定轻提示。
- 调整桌面端事项详情的末尾状态操作组到底部，小屏保持自然文档流。
- 修复“开始复盘”作为独立操作组未随其他状态操作到底部的问题，合并为唯一底部操作组。
- 修复复盘完成请求未固化当前草稿的问题，使 `result` 与 `actualAction` 始终来自同一提交快照。
- 修复 UAT API 故障注入恢复依赖旧 PID / 不稳定 detached shell 的问题，改为按当前监听 PID 停止并以 `.env.uat` 直接恢复。

## 未完成项

- S2 已通过架构总体稳定复审，等待产品经理封板裁决；封板前不得进入 S3、Backup V3、API 或真实 H5 数据接入。
