# 增加项
- 手记 AI 复盘助手改为按日期持久化的独立对话面板，支持空态居中输入、消息流式展示和底部 Composer。
- 新增手记自由对话流式接口，服务端自动注入当前草稿与上海时区当日行动事实。
- 新增同源 `/deepseek/` 代理配置、独立 Harness 启动脚本和管理员专用 Harness Provider 配置入口。

# 修复项
- 保留现有 AI SecretStore 文件路径，同时将 Harness 凭据隔离到独立命名空间。

# 未完成项
- Harness WebUI 当前将 `/api` 和 WebSocket 路径硬编码为根路径；需要对外部 Harness workspace 增加 `/deepseek/` base-path 适配后，才能完成真实页面、会话隔离和业务工具联调。
- 保留并整合手记情绪梳理、待办提取、候选创建、AI 追加/替换和日期切换中断保护。
- 优化手记 AI 首 token 延迟，跳过主 AI 全量知识库上下文预处理，避免情绪梳理等待过久。
- 移除每条 AI 回复下方的“追加到手记”和“替换正文”按钮，保持对话区简洁。
- 修复提取待办候选卡片确认按钮文字被 Taro 样式覆盖而不可见的问题。

# 未完成项
- 浏览器人工截图验收与真实 AI 对话联调待运行环境中的 API 服务重启后进行。
# 修复项
- 合并“我的”页面的账户与安全分区，将当前会话信息固定展示在账户页并移除冗余安全标签。
- Scoped daily-note AI Markdown spacing so one-line replies no longer show an extra blank line above the text.
- 修复手记 AI 输入框回车发送，普通 Enter 发送、Shift+Enter 换行，并兼容中文输入法组合状态。
- 修复导航离开时中断主 AI/手记 AI 流，并让手记发送后自动滚动到聊天底部。
- 修复页面切换误终止 AI 生成，改为保留 AI 页面挂载并在返回后继续显示回复。
- 修复保留挂载层导致 AI 输入框被挤出视口的问题，恢复主 AI 与手记输入区域全高布局。
- 统一账户与安全页面卡片的系统字号、行高和字重，修复正文异常放大。
- 移除长期探索列表的手动刷新按钮和方法列表搜索框入口。
- 修复首页快速记录与手记编辑器之间的内容同步，避免旧草稿自动保存覆盖已追加记录。
- 修复手记正文全高点击区域和回车输入，避免空白区域无法聚焦及回车清空正文。
- 修复首页快速记录后手记编辑区高度回退与空内容同步问题，保持正文和全高编辑状态。
- 强化 Taro 手记编辑器内部容器的全高约束，修复单行内容时仅首行可点击的问题。
- 处理快速记录后 Taro 多层 textarea 容器的高度耦合，统一内外层编辑区域尺寸。
- 将手记正文从 Taro Textarea 改为原生 textarea，消除快速记录触发重渲染后的单行高度回退。
- 在账户页增加当前密码、新密码和确认密码三项修改表单，成功后清除当前会话并回到登录页。
- 将账户页密码修改表单收纳为居中模态框，避免展开表单挤压账户信息。
- 统一密码修改弹窗输入框与登录页系统样式，修正控件高度、内边距、对齐和焦点状态。
- 完成 DeepSeek Harness 外部运行时的 `/deepseek/` base path、动态 API 路径和 Windows 启动命令适配，修复 Harness 构建类型错误与 Windows 子进程启动失败。
- 完成 Harness Provider 管理员配置接口、独立 SecretStore 命名空间、H5/Nginx 代理配置和独立启动脚本的工程回归验证。
- 未完成：Harness 尚未接入 Knowledge_Base 认证适配、用户级会话命名空间、业务工具桥接和写入确认令牌；当前仅验证独立页面与内部服务可启动。
- 增加 DeepSeek Gateway 入口，对 Harness API 与 WebSocket 请求校验 Knowledge_Base 当前登录态，并将 Harness 上游收敛到内部端口。
- 未完成：Gateway 目前只负责登录态门禁，尚未完成按用户绑定 Harness 会话、业务工具桥接和写入确认流程。
- 增加按 Knowledge_Base 用户 ID 懒启动独立 Harness 上游的 Gateway 运行隔离，每个用户使用独立 `DSH_HOME` 数据目录与端口。
- 未完成：事项、手记读取和写入确认工具尚未接入 Harness Agent。
- 增加受认证保护的 DeepSeek Harness 只读上下文桥接，复用 Application 服务返回当前用户、事项和手记，Gateway 通过 `/kb-bridge/context` 转发。
- 未完成：桥接当前仅提供只读上下文，写入确认令牌和事项/手记写入工具仍未开放。
- 增加一次性、两分钟有效、绑定当前用户的 Harness 写入确认令牌，支持创建/更新事项、变更事项状态、更新手记和追加今日手记。
- 未完成：Harness WebUI 尚未注册这些 bridge 工具的前端操作面板，当前需由受控工具调用 `/kb-bridge/confirmations` 与 `/kb-bridge/writes`。
- 为每个用户运行时生成独立 bridge token，并通过 Gateway 注入 Harness 进程；后续工具调用无需暴露用户 Cookie。
- 在外部 Harness workspace 注册 `kb_context`、`kb_request_write`、`kb_confirm_write` 三个 Agent 工具，并完成 Harness 构建与 H5 回归验证。
- 未完成：真实登录态下的端到端工具调用与确认卡片人工验收，需 API 与测试账户同时运行。
- 修复未登录访问 `/deepseek/` 直接显示 401 的入口体验，改为跳转主应用登录页；API 与 WebSocket 仍保持未授权拦截。
- 修复 Harness 插件绝对 `/plugins/*` 资源未经过 10086 代理导致插件加载失败的问题，增加 H5/Nginx 插件资源转发。
