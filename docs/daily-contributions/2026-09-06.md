# 2026-09-06

## 增加项
- 「今日饮食推荐 · 个人档案」新增「AI 提示词」可编辑项（Migration `033_diet_profile_ai_prompt_weight.sql`：`user_diet_profiles` 增 `ai_prompt VARCHAR(2000) NULL`；Contracts/Application/Hono API/Repository 全链路支持 `aiPrompt`，最长 2000 字）。默认＝内置提示词（`daily-diet-auto.ts` 的 `DIET_PROMPT`）；保存自定义文本后提示词旁显示「自定义」徽标，可点「恢复默认」回到内置那一套（恢复后回到「默认」状态）；文本清空按默认处理。AI 生成饮食推荐时优先使用档案 `ai_prompt`，留空回退内置提示词。
- H5 重新构建（`pnpm --filter @knowledge-base/client build:h5`）并部署到 10086 静态服务（新 bundle 实测含 AI 提示词/恢复默认文案）。
- 日常库已执行 Migration 033：`/health` 实测 `ready / knowledge_base / schemaVersion=33`，`SHOW COLUMNS FROM user_diet_profiles` 确认 `ai_prompt varchar(2000)` 与 `weight_kg decimal(5,1) unsigned`。

## 修复项
- 修复：个人档案「保存失败，请稍后重试」（保存报 400 `VALIDATION_FAILED`）。根因：`apps/api/src/hono/routes/daily-diet.ts` 动态路由 `GET/PUT /{entryDate}` 注册在静态 `GET/PUT /profile` 之前，Hono 先匹配先注册，`/profile` 被 `/{entryDate}` 捕获、日期正则校验失败返回 400。修复：`/profile` 移至动态日期路由之前注册。
- 修复：弹窗保存只提交本次改动的字段，未改字段会被服务端覆盖清空（如只改体重后身高/性别/健康状态丢失）。修复：保存前先读服务端当前档案，与本次改动合并后整行 PUT。
- 修复：体重栏位 `SMALLINT UNSIGNED` 截断小数，81.5 被存成 81。修复：Migration 033 将 `weight_kg` 改为 `DECIMAL(5,1) UNSIGNED NULL`，Repository SELECT 以 `CAST(weight_kg AS DOUBLE)` 保持数值契约。
- API 已以新源码重启（单条 `node --import tsx src/main.ts`，进程树 1924→5908，监听 127.0.0.1:32146）；经 10086 代理全链路 E2E 通过：注册→登录→PUT 完整档案（含 aiPrompt）→合并保存体重 81.5→GET 读回字段全部保留（身高 178/性别/目标/活动量/健康状态/AI 提示词）→清空 aiPrompt 读回 undefined；未认证访问 `/daily-diet/{date}` 与 `/profile` 均 401（路由正常解析，不再 400）。

## 未完成项
- 说明：UAT `knowledge_base_uat` 尚未执行 033（032 亦未执行），未迁移不得视为可用；本项变更未发布桌面新版本（未提交/推送，按会话要求）。

## 发布项
- 发布桌面版 v0.1.34（2026-09-07）：含个人档案「AI 提示词」可编辑项（默认/自定义徽标 + 恢复默认，空文本按默认，AI 生成优先用档案提示词）+ 修复档案保存失败（路由顺序 400 / 体重小数截断 / 部分编辑丢字段）；版本 0.1.33 → 0.1.34（`src-tauri/Cargo.toml`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.lock`），以仓库公开签名材料（`src-tauri/signing-material/`）签名构建，`MaruMaru_0.1.34_x64-setup.exe`（6,408,121 字节）+ `.sig`（420 字节）上传 GitHub Release v0.1.34（id 383661860，https://github.com/liukai-weibo/muramura/releases/tag/v0.1.34），updater.json 更新指向 v0.1.34（gh-proxy 实时代理，实测 HEAD 200 + raw 拉取通过）。功能提交 ce4ae04 + 发布提交 d9dbce6 已直推 main。