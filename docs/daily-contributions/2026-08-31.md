# 2026-08-31 每日贡献

## 增加项
- 桌面端登录页新增「最近账号」下拉框与「记住密码」复选框：成功登录/注册后自动记录最近账号（最多 8 个、最新在前）；勾选记住密码后，从下拉框选中该账号会自动填入已存密码；取消勾选或在未勾选状态登录会清除该账号已存密码；账号列表与已存密码在桌面端分别存于本地 localStorage 与系统钥匙串（keyring），浏览器端降级 localStorage。

## 修复项
- 全量 vitest 误报基线治理：better-sqlite3 原生绑定重新编译适配当前 Node（NODE_MODULE_VERSION 137），sqlite 系测试恢复通过；Playwright 浏览器流程测试（*-h5-flow.test.ts、*.e2e.test.ts）从单元测试通道分离至独立 vitest.e2e.config.ts（新增 test:e2e 脚本、需真实 Chromium/MySQL 环境按需启用）；单元通道失败数由 131 降至 89，余下均为历史断言过时（旧状态机语义、旧源码字符串校验、路由表快照过期），与本次登录记住密码功能无关，另行跟踪。

## 发布项
- 发布桌面版 v0.1.30（签名版）：登录页最近账号 + 记住密码（桌面端钥匙串 / 浏览器端降级 localStorage）；版本 0.1.29 → 0.1.30（Cargo.toml/tauri.conf.json/Cargo.lock），`MaruMaru_0.1.30_x64-setup.exe`（6,405,162 字节）+ `.sig` 上传 GitHub Release v0.1.30，updater.json 更新指向 v0.1.30。详见下方修复项（更换签名密钥说明）。

## 修复项
- 修正 v0.1.30 发布为签名版：旧 updater 签名私钥口令丢失（穷尽检索会话日志/凭据/git 历史/常见口令均失败，密钥本身 rsign 加密无法解密），已更换新签名密钥对并安全存储新口令（Windows 凭据管理器 MaruMaruTauriSigning + `src-tauri/.signing-password.key` + 用户 Documents 副本，均不入库）；`MaruMaru_0.1.30_x64-setup.exe`（6,405,162 字节）+ `.sig` 已重签重传 Release v0.1.30，updater.json 更新指向 v0.1.30（gh-proxy 实时代理），tauri.conf.json pubkey 同步更新。已装 0.1.29 及更早的存量桌面端需手动安装一次（内置旧公钥），此后自动更新恢复。
