# 2026-09-18 每日贡献记录

## 增加项
1. 发布桌面端安装包 `v0.1.39`：GitHub Release 上传 `MaruMaru_0.1.39_x64-setup.exe`（6,412,666 字节）与配套 `.sig` 签名，安装包内置接口地址由旧服务器改为新服务器 `47.116.6.111`。
2. `updater.json` 更新到 `0.1.39`：版本号、发布说明、发布时间、签名与下载地址全部指向 `v0.1.39`，桌面端自动更新通道切换到新安装包。
3. `src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 版本统一升至 `0.1.39`，与 `updater.json` 保持一致。
4. 新增 `deploy/mysql-tuning.cnf`，并在 `docker-compose.yml` 的 mysql 服务上挂载为 `/etc/mysql/conf.d/mysql-tuning.cnf:ro`，用于压缩 MySQL 运行时内存占用。

## 修复项
1. 桌面端安装包在构建时把云端地址编译进前端产物，已安装的桌面端仍指向旧服务器 `47.97.69.175`，服务器迁移后无法登录与同步；本轮重新构建并发布 `0.1.39`，实测构建产物中只含 `47.116.6.111`，旧地址零命中。
2. 构建前发现 Rust 包版本与安装包版本不同步：`tauri.conf.json` 已升到 `0.1.39`，而 `src-tauri/Cargo.toml` 仍停留在 `0.1.38`，会使 Rust 侧版本与安装包版本不一致；已同步升级并重新构建。
3. 新服务器 MySQL 容器常驻 550.8MiB，而业务数据仅 2.19MiB，内存占用与数据量严重不匹配；根因是镜像跑原厂默认配置（`performance_schema` 开启、`max_connections=151`、`table_open_cache=4000`、`innodb_log_buffer_size=64M`）；已通过新增调优配置将其降至 147.1MiB。
4. 失效的 `setup-new-server.yml` 仍留在仓库中：该 workflow 引用的 `NEW_DEPLOY_*` Secrets 在新服务器初始化后已删除，再次触发必然失败；已删除该文件。

## 未完成项
1. 加密下载通道的私钥 `~/.kb-backup-keys/backup-key.pem` 只存在于本机，尚未做离线备份。
2. 旧服务器 `47.97.69.175` 尚未停用，两台机器的数据会各自继续变化，需尽快完成切换并下线旧机。
3. 新服务器以迁移数据启动后尚未做人工业务验收（登录、写入、备份恢复闭环）。
4. 桌面端 `0.1.39` 安装包尚未在本机做人工验收：安装、自动更新提示、登录后写入与同步。

> 说明：前一轮未完成项中「桌面端安装包需重新构建并发布后才会连接新服务器」已在本轮完成——`v0.1.39` 已发布并经代理下载校验，哈希与本地构建产物一致。
