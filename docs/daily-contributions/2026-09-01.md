# 2026-09-01

## 发布项
- 发布桌面版 v0.1.31：验证更换签名密钥后的自动更新链路（功能同 v0.1.30：登录页最近账号 + 记住密码）；版本 0.1.30 → 0.1.31（Cargo.toml/tauri.conf.json/Cargo.lock），用仓库公开签名材料（`src-tauri/signing-material/`）签名构建，`MaruMaru_0.1.31_x64-setup.exe`（6,403,708 字节）+ `.sig` 上传 GitHub Release v0.1.31，updater.json 更新指向 v0.1.31（gh-proxy 实时代理）。已装 0.1.30 本机可自动升级验证。

## 修复项
- 修复桌面端「记住密码」不生效：keyring crate 原未启用 windows-native feature（仅 mock store，凭证存进程内存、重启即失）；Cargo.toml 改为 `keyring = { version = "3", features = ["windows-native"] }` 后真实写入 Windows 凭据管理器（keyring Entry service=com.marumaru.knowledgebase / user=login-password:{username}，cmdkey 可核对）。

## 发布项
- 发布桌面版 v0.1.32（修复版）：0.1.31 → 0.1.32（Cargo.toml/tauri.conf.json/Cargo.lock），签名构建 `MaruMaru_0.1.32_x64-setup.exe`（6,407,411 字节）+ `.sig` 上传 GitHub Release v0.1.32，updater.json 指向 v0.1.32（gh-proxy 实时代理）。
