# 2026-08-26

## 增加项
- 安全审计中心二期：扩展 activity_audit_events 枚举并经 Migration 027 更新 CHECK 约束（module 5→14、action 4→11），MYSQL_REQUIRED_SCHEMA_VERSION 26→27；新增 9 个模块（探索轨道/方法/复盘/状态小结/饮食推荐/首页AI卡片/AI偏好/AI会话/AI配置）与 7 个动作（分配/移除/恢复/清空/归档/复盘/发送）。
- 新增 13 类审计捕获点（best-effort 追加、失败不阻断业务）：探索轨道新建/改名/改描述/软删/恢复、事项分配/移除探索轨道、复盘提交、方法应用创建事项、方法回收/恢复、回收站 purge（按类型记）、近期状态小结保存、今日饮食推荐保存、首页 AI 卡片新建/更新/删除/缓存写入、AI 偏好新建/更新/删除、AI 会话发消息/新建/改名/归档/恢复/删除/清空、AI 配置修改/清空（路由层按 actor 补录）、手记绑定 AI 会话。
- 前端同步：审计中心新增 9 个模块勾选与 7 个操作勾选、列表与 CSV 中文标签补齐、api-client 枚举白名单扩展。
- 扩展自动测试：activity-audit-application 新增 10 组捕获断言（15 个全绿）、audit-center-api-client 新增新枚举解析断言（6 个全绿）、mysql-activity-audit.integration 新增 027 迁移后新枚举插入与筛选断言。

## 增加项（补充）
- 真实库 Migration 027 已执行：日常 knowledge_base 经 pnpm db:migrate 应用（迁移前逻辑备份 knowledge_base-before-027-2026-08-26-13-34-43.sql 于项目外 维护相关/运行库备份/），schema_migrations 含 27，/health 实测 ready / knowledge_base / schemaVersion=27。

## 修复项
- 审计中心模块/操作筛选按钮（audit-center-chip 及筛选按钮全部状态）文字显示为白色像透明：根因是 Taro 按钮默认 `-webkit-text-fill-color: #fff` 覆盖 `color`，WebKit 渲染取 text-fill 值导致白字；修复为在未激活/激活 chip 及筛选按钮各状态同时显式双写 `color` 与 `-webkit-text-fill-color`（含 text/taro-text-core 子元素），playwright 加载 dev server 计算样式实测 chip 文字色 #413c35 / 激活白字 #fffdf9、text-fill 同值。

## 未完成项
- 审计中心二期真实运行验收需平台管理员账号人工执行（新增捕获点实际触发并与列表/CSV 中文标签对照）；未 commit/push（需用户确认）。
