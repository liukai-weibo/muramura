# 2026-08-26

## 增加项
- 安全审计中心二期：扩展 activity_audit_events 枚举并经 Migration 027 更新 CHECK 约束（module 5→14、action 4→11），MYSQL_REQUIRED_SCHEMA_VERSION 26→27；新增 9 个模块（探索轨道/方法/复盘/状态小结/饮食推荐/首页AI卡片/AI偏好/AI会话/AI配置）与 7 个动作（分配/移除/恢复/清空/归档/复盘/发送）。
- 新增 13 类审计捕获点（best-effort 追加、失败不阻断业务）：探索轨道新建/改名/改描述/软删/恢复、事项分配/移除探索轨道、复盘提交、方法应用创建事项、方法回收/恢复、回收站 purge（按类型记）、近期状态小结保存、今日饮食推荐保存、首页 AI 卡片新建/更新/删除/缓存写入、AI 偏好新建/更新/删除、AI 会话发消息/新建/改名/归档/恢复/删除/清空、AI 配置修改/清空（路由层按 actor 补录）、手记绑定 AI 会话。
- 前端同步：审计中心新增 9 个模块勾选与 7 个操作勾选、列表与 CSV 中文标签补齐、api-client 枚举白名单扩展。
- 扩展自动测试：activity-audit-application 新增 10 组捕获断言（15 个全绿）、audit-center-api-client 新增新枚举解析断言（6 个全绿）、mysql-activity-audit.integration 新增 027 迁移后新枚举插入与筛选断言。

## 增加项（补充）
- 真实库 Migration 027 已执行：日常 knowledge_base 经 pnpm db:migrate 应用（迁移前逻辑备份 knowledge_base-before-027-2026-08-26-13-34-43.sql 于项目外 维护相关/运行库备份/），schema_migrations 含 27，/health 实测 ready / knowledge_base / schemaVersion=27。
- 审计中心筛选交互调整：① 关键词与按用户名/ID 搜索合并为一个搜索框（placeholder“搜索用户名 / 用户 ID / 内容快照”），后端新增 search 查询参数（actor_user_id / actor_username / snapshot 任一模糊匹配 OR 语义），Contracts/MySQL repository/audit routes（list+export）/api-client 同步透传；② 时间筛选由手填 YYYY-MM-DD 文本框改为 `<input type='date'>` 日期选择控件（开始/结束），日期输入框宽度 118px→150px。
- 扩展测试：audit-center-api-client 新增 search 参数传递断言（列表 URL 与导出 URL 编码）、mysql-activity-audit.integration 新增 search OR 语义断言（按 actor 3 条 / 按快照内容命中）。
- 审计内容最小化：近期状态小结 / 今日饮食推荐 upsert 审计快照由 {entryDate, content} 收敛为仅 {entryDate}（不再入库 AI 生成的回复原文，AI 提示词本就为前端常量不经审计）；首页自定义 AI 卡片 create/update 审计快照由 {cardTitle, cardTheme} 扩充为 {cardTitle, aiPrompt, cardSize, cardTheme, refreshMode}（只记用户配置的标题/指令/尺寸/底色/刷新方式，AI 回复缓存 upsertCache 仍只记 cacheId 不含 aiOutput）。
- 审计中心内容快照交互：快照单元格由纯文本改为可点击按钮，点击弹出详情弹窗（title 内容快照详情，副标题含时间/用户/模块/操作/目标ID，正文以格式化 JSON 完整展示快照原文并保留换行）；点击弹窗外部（backdrop 自身）或 Esc 或 ✕ 均可关闭，弹窗内部点击不会误关闭。
- 审计快照列表可读化：原 snapshotPreview 只提取 content/query/title 三种键，其余全部 fallback 为无缩进压缩 JSON（如 `"entryDate":"2026-08-26"`）；现抽离为独立模块 audit-snapshot-preview.ts 并按优先级生成可读摘要（标题类文本 title/cardTitle/name/query/content → 餐次 meals 组合 → 日期 entryDate/cacheDate → 标量键值串联），真实库实测 daily_summary/home_ai_card/meal/item/mood 等全部显示可读文本，弹窗仍未变展示完整格式化原文；新增 snapshot-preview 单元测试 6 个（含优先级与兜底断言）。
- 审计快照弹窗可读化：原详情弹窗展示 JSON.stringify(parsed, null, 2) 的原始 JSON 缩进格式；现改为逐行“键：可读值”展示（嵌套对象/数组/餐次平铺为可读串，纯文本原样保留换行），真实库实测 AI 卡片配置/缓存等弹窗均为可读文本；快照预览单元测试扩至 8 个（含弹窗逐行与数组餐次断言）。
- 审计快照展示中文化：新增字段名→中文标签映射（cardTitle→卡片标题、aiPrompt→AI提示词、cardSize→卡片尺寸、cardTheme→卡片底色、refreshMode→刷新方式、entryDate→日期、cacheDate→缓存日期、meals→餐次、mealType→餐次类型、moodLevel→情绪等级等约 26 个）与常见枚举值→中文映射（breakfast→早餐、lunch→午餐、dinner→晚餐、small→小、medium→中、large→大、cream→奶油色、green→绿色、beige→米色、daily→每天、manual→手动、item→事项、daily_note→手记、search→搜索等约 20 个），列表摘要与详情弹窗统一应用（仅展示层翻译，不改变业务数据）；原 mealType 值将显示“餐次类型：早餐”而非“mealType:breakfast”。
- 审计搜索中文等价命中：原合并搜索仅按快照 JSON 原文 LIKE（搜“早餐”无法命中存储值为 breakfast 的 meal 快照，搜“fast”反而命中）；现新增共享映射 AUDIT_SNAPSHOT_VALUE_LABELS（contracts 单一来源，前端展示翻译与后端搜索展开共用），仓库 search 过滤将查询词中含有的中文枚举标签展开为对应存储值（如“早餐”→“breakfast”、“绿色”→“green”）追加 LIKE 条件，中文与原文均能命中；mysql 集成测试新增“搜早餐命中 mealType=breakfast”断言（本地跳过、CI 覆盖）；H5 真实链路实测搜“早餐/绿色”命中对应快照，“fast/喝粥”仍命中。
- 审计搜索中文字段名等价命中：原展开只覆盖枚举值（早餐→breakfast）不覆盖字段名，搜“餐次类型/卡片标题/日期”无结果；现 contracts 新增 AUDIT_SNAPSHOT_KEY_LABELS（字段名→中文，与展示层共用单一来源，前端本地定义删除改为导入），仓库展开函数同时匹配中文字段名→存储键名（如“餐次类型”→“mealType”）追加 LIKE；mysql 集成测试新增“搜餐次类型命中 meal 快照”与未知字段 0 结果断言；H5 真实链路实测搜“餐次类型/卡片标题/日期”均命中对应记录。
- 审计复盘快照补全与展示修复：原复盘完成审计只记 itemId/actualAction/result 三个字段（中文键未映射导致非中文展示，且前端提交时 actualAction 与 result 同值造成“结果”重复，缺乏【有效 / 舒服】【阻力 / 不舒服】【产生新想法】内容）；现 application 层快照补充 effective/incompatible/newIdeas（哨兵默认“暂未标记…”与空值不记，只录用户实际填写），contracts 键标签新增 actualAction→做了什么、result→复盘结果、effective→有效 / 舒服、incompatible→阻力 / 不舒服、newIdeas→产生新想法，展示层新增 reviewEntries 去重（“做了什么”与“复盘结果”值相同时只显示复盘结果）；真实 API 建事项→完成复盘（含有效/新想法）→审计快照实测含全部用户填写字段，旧记录（同值）界面显示为“事项ID…复盘结果：结果”单项；应用层测试新增完整断言（含哨兵不记），preview 测试新增复盘去重与中文标签断言。
- 近期状态小结与今日饮食推荐弹窗支持 Markdown 渲染：原两处均按 summary.content.split('\n') 纯文本逐行展示，AI 输出中的 ## 标题、**加粗** 等原样显示；现复用既有 ExperimentalAiMarkdown 组件（experimental-ai 模块，daily-note-ai-panel 同款），标题/加粗/列表/引用/代码/删除线均正确渲染；移除 modal-content 的 white-space: pre-wrap 避免与 markdown 布局冲突；build:h5 与 typecheck 通过，真实用户内容（kkk 账号 1154 字符 AI 小结）实测粗体与有序列表渲染、无裸露 **/## 标记。
- 审计精简：完成复盘审计快照移除 itemId（事项ID 对审计无信息量），只保留做了什么/复盘结果/有效 / 舒服/阻力 / 不舒服/产生新想法；近期状态小结与今日饮食推荐模块不再产生审计事件（内容为纯 AI 生成、无用户结构化输入，快照仅有日期无信息量），DailySummary/DailyDiet 应用服务移除 auditRecorder 依赖与 safeAuditRecord 调用，API services 同步去掉 auditRecorder 传参；应用层测试改为断言两模块不产生审计、复盘快照无 itemId，preview 测试同步去 itemId 断言；真实 API 实测新复盘快照 {actualAction,result,effective,newIdeas} 无 itemId，upsert 小结后审计列表无 daily_summary/daily_diet 事件。
- 封板发布 0.1.10：fast-ui 四笔提交（审计中文展示/搜索/精简 + 小结/饮食推荐 Markdown 渲染 + 每日贡献 + 版本 0.1.10/README）合并 main（8b3bfa9）并推送；Docker 镜像 CI run 32961094090 成功；Tauri 桌面构建 MaruMaru_0.1.10_x64-setup.exe（5.35 MB）并上传 GitHub Release v0.1.10（https://github.com/liukai-weibo/muramura/releases/tag/v0.1.10）。
- 审计中心模块精简：删除 activity_audit_events 中 daily_summary/daily_diet 历史遗留记录（快照仅有日期、无信息量，剩余 0 条）；审计中心模块筛选器移除「状态小结 / 饮食推荐」选项（audit-center.tsx moduleOptions，moduleLabel 保留以维持类型完整性）；client typecheck、audit 相关 16 测试与 build:h5 通过。
- 修复：情绪凌晨记录报 validation 的时区 Bug——后端 mood/meals/daily-summaries/daily-diet/home-ai-cards 五处「不能晚于今天」校验由服务器进程时区 todayLocal() 改为 UTC 上界 utcDatePlusDays(1)（新增 packages/application/src/date-utils.ts；本地“今天”在任意时区 ≤ UTC今天+1，覆盖东八区凌晨窗口=UTC 前一天；本地明天及以上仍拒绝）。实测：API 以 UTC今天+1 保存情绪 200、+2 返回 400 MOOD_ENTRY_INVALID；新增 date-utils 测试与五服务 +1/+2 时区无关断言，全量受影响测试 42 项与 client typecheck/build:h5 通过，测试记录已清理（8-28~8-29 无残留）。
- AI 提示词同步：ai-policy.ts AI_BUSINESS_SEMANTICS 历史状态码补全（idea_to_try/waiting_review/archived_no_review 与 paused/idea_later/abandoned 并列，与 contracts itemStatuses 8 枚举对齐）。
- 安全审计中心筛选区布局再调整：导出 CSV 按钮移至标题右侧（header space-between 右上角，距容器右缘 25px）；搜索框由独占一行改为与时间选择/筛选按钮同一行（flex 0 1 460px / min 260 / max 560 纠正原 38% 限宽 + 独占 1070px 的问题），模块、操作标签组依次各占一行（实机 top 190/238/276）；删除 row-1/row-2 两行容器与对应 scss，media query 简化；client typecheck、build:h5 与 Playwright 实机（搜索/时间/筛选同 top、搜索宽 460、导出在 header 右）通过。
- 安全审计中心去掉「目标ID」列（列表表头/行、快照弹窗 meta 同步移除；数据仍存储，仅界面隐藏）：表格改 5 列 时间/用户/模块/操作/内容快照，列宽重分配 15:12:12:10:51（快照最大）；响应式 nth-child(5) 整行规则改为快照列；删除 .audit-event-entity 样式；client typecheck、build:h5 与 Playwright 实机（表头 5 列、行 5 单元格、entity 0、弹窗 meta 无 ID、grid 比例）通过。
- 安全审计中心排版重构（视觉层，不改业务/契约）：标题区主标题加大加粗、副标题弱化；筛选区拆两行（第一行搜索框约 1/3 屏宽 + 右侧轻量化「导出 CSV」（⬇图标），第二行 模块：/操作：/时间：+筛选按钮 分组，组间留白）；模块与操作标签统一浅底色 999px 圆角 pill 样式、选中深色白字；表格列宽按权重 时间15/用户10/模块10/操作8/目标ID15/快照42、行内边距加大、整行 hover 浅底色、表头浅灰底、保留无线表细横线；目标 ID 灰字截断 + title hover 显示完整；快照列表改富文本（新增 snapshotPreviewSegments 纯函数，字段前缀加粗、正文常规、段间 · 分隔，taro title 经 as never 透传运行时生效）；删除操作去红改浅灰；分页区加细分隔线、「共 X 条 · 第 X/Y 页」靠左、页码数字按钮（≤7 全显否则首/省略/±1/省略/尾）当前页深色填充、禁用灰化；≤720px 保持单选列降级；新增 8 个 snapshotPreviewSegments 单测（label/value 拆分、review 去重、餐次/日期/空）；client typecheck、preview+client 18 测试、build:h5 与 Playwright 实机（两行筛选/999px chip/列宽比例/快照加粗/entity title/删除灰/hover 底色/页码高亮/分隔线）全部通过。

## 修复项
- 审计中心模块/操作筛选按钮（audit-center-chip 及筛选按钮全部状态）文字显示为白色像透明：根因是 Taro 按钮默认 `-webkit-text-fill-color: #fff` 覆盖 `color`，WebKit 渲染取 text-fill 值导致白字；修复为在未激活/激活 chip 及筛选按钮各状态同时显式双写 `color` 与 `-webkit-text-fill-color`（含 text/taro-text-core 子元素），playwright 加载 dev server 计算样式实测 chip 文字色 #413c35 / 激活白字 #fffdf9、text-fill 同值。
- 审计中心【按用户名】【关键词】【时间筛选】输入框输入任意字符即白屏（整页 React 卸载）：根因是 `onInput` 在函数式 setState 的 reducer 内读取 `event.currentTarget.value`，该回调延迟到下一次渲染才执行，此时 React 已把合成事件 currentTarget 置为 null，读 `.value` 抛 TypeError；修复为在事件回调内先同步提取 `const value = event.currentTarget.value` 再传入函数式更新，4 个输入框（用户名/开始日期/结束日期/关键词）全部改写，playwright 实测输入后页面存活、筛选与清除均正常。

## 未完成项
- 审计中心二期真实运行验收需平台管理员账号人工执行（新增捕获点实际触发并与列表/CSV 中文标签对照）；未 commit/push（需用户确认）。
