# 2026-08-28 每日贡献

## 增加项
- AI 全局时间感知：客户端新增 buildTimeAnchor()（Intl 本地时刻 + IANA 时区），4 个 AI 流请求（AI 聊天 / 首页卡片 / 手记续写 / 手记对话）统一上报 timeAnchor + timeZone；服务端 buildAiSystemMessage 注入「现在是 …（用户本地时区 …）」锚点，full / daily-note / ephemeral 全模式生效
- 记录时刻上屏：AiKnowledgeOverview moodEntries/mealEntries 增补 createdAt 契约；formatKnowledgeContext 的情绪/三餐/手记渲染增加 HH:mm（createdAt/updatedAt 按用户时区转换，timeZone 非法或缺失回退服务器本地，缺字段保持旧格式）；手记续写/对话的当天情绪三餐事实同样带 at 时刻
- 新增 packages/application date-utils 导出 formatInTimeZone / formatAnchorPrompt（经 application 入口导出）
- 长期探索归档能力：Migration 028（exploration_tracks/items 加 archived_at）；探索大项归档级联隐藏旗下子行动（显示收拢不删除）；行动 tab 列表不再展示已归档子行动（服务端 listItems 过滤）；归档列表入口（探索页 footer，可返回）；详情操作区新增归档/取消归档（在改名左侧）+ 确认弹窗；归档大项不进可选下拉；getHistory 与搜索深度访问保留；备份恢复携带归档标记；审计记录 archive/restore 动作
- 工作台 Tab 顺序调整：长期探索移至首位，默认进入长期探索页（灵感待办归入长期探索）

## 修复项
- items 仓储 mapItem 补 exploration_track_id / archived_at 映射（此前 listItems 返回对象无探索关联字段，归档过滤条件永远放行，归档大项子行动仍显示在行动列表）
- 探索页归档/取消归档成功后触发 onItemsChanged 刷新行动池（此前行动 tab 显示归档前的旧快照）
- 归档大项禁止继续写入：创建子行动（existing 归档主线）与把已有行动分配进归档主线均返回 409（EXPLORATION_TRACK_ALREADY_ARCHIVED）；前端归档详情隐藏捕获表单并显示「已归档，不能再添加新的行动」（原始逻辑缺服务器校验与表单隐藏）
- 自定义 AI 卡片 Markdown 渲染：卡片预览与详情弹窗改用 ExperimentalAiMarkdown 渲染（此前 Text 直出导致 ** 等原始符号可见）；预览截断保留换行结构并剥离孤立 ** 标记避免残留（此前沿用旧逻辑 \s+ 扁平化导致卡片预览挤成一行），弹窗复用官方卡片同款渲染；卡片内容容器改为限高溢出隐藏
- 归档验证数据已清理（含回收站与关联子表）

## 未完成项
- 无
