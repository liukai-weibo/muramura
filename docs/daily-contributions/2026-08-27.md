# 2026-08-27 每日贡献

## 增加项
- 首页卡片生成后台化：近期状态小结/今日饮食推荐从弹窗内部生成提升为页面级任务（index.tsx），点击刷新后无论弹窗是否关闭都持续生成，生成完成自动落库并刷新卡片；卡片与弹窗共享流式 draft（未落库前实时预览 + 光标动画）
- 卡片与弹窗流式生成预览：`generation-cursor` 闪烁动画、卡片 `streamPreview`、弹窗 `daily-summary-modal-live` / `daily-diet-modal-live` 实时草稿区
- 弹窗支持有内容后「重新生成」（之前仅空态按钮）：summary/diet 弹窗在已有内容时也显示生成按钮（force 强制覆盖）
- 服务端 ephemeral 轻量生成模式（experimental-ai.ts）：buildAiSystemMessage 新增 brief 版（去掉概念分类/猎人人设），stream() 新增 `ephemeral` 模式（跳过 search/conversation/preferences/全量手记，仅注入 overview 且上限 6,000 字符）；stream-ephemeral 路由接入，卡片类生成 prompt 由 ~30k 字符降至 ~10k 内
- 新增 `tests/experimental-ai-ephemeral.test.ts`（3 项：ephemeral 跳过搜索/会话/偏好、仍注入 overview、brief system 不含人设与概念段、full 模式行为不变）

## 修复项
- 「点击刷新后取消弹窗即停止生成」感知：生成状态提升到页面级后，关闭弹窗不再中断后台生成，卡片持续显示生成中/流式预览并在完成后自动更新
- 首页打开自动生成等待感：生成前准备从全量上下文降至轻量（实测 ephemeral 请求 654ms 内返回首个 chunk；此前受 24k 上下文 + 会话/搜索/全量手记拖慢）

## 未完成项
- 本地 AI 渠道（gpt-5.6-sol，聚合渠道）当次验证时返回 503 无可用通道，真实渐进流式效果未能本地录制；已用 Playwright 拦截 mock SSE 全链路验证（token→draft→落库→卡片自动刷新）
- 全量 vitest 中 sqlite（better-sqlite3 原生绑定 Node 24 ERR_DLOPEN_FAILED）与 Playwright 浏览器流测试为环境基线失败，与本次改动无关（改动相关测试 42+ 项全过）
