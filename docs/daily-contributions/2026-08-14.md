# 2026-08-14
- 日记页外层 `.page` 增加专用全宽壳层，清除通用左右内边距，让 AI 复盘背景铺到窗口最右侧，修复页面内部白边。
- 修复今日小记三栏 Grid 最小宽度导致右侧内容溢出、页面背景露出的白边；统一日记页及各栏宽度、盒模型和最大宽度约束。
- 今日小记中间编辑区恢复上一版书写卡片布局，并补齐页面内部宽高与盒模型铺满规则，修复编辑区白边。
- 今日小记时间线改为按日期稳定分配随机 Emoji；中间编辑区移除多余图标与操作按钮并铺满可用高度；Tab 输入拦截改为捕获阶段并默认插入两个空格。
- 今日小记中间编辑区改为紧凑顶部栏加全高输入面板，空白引导不再挤占书写空间；Tab 输入固定在编辑框内并继续 Markdown 行续写。
- Tauri Windows 桌面窗口使用原生区域裁剪实现约 24px 大圆角；最大化取消裁剪、恢复及尺寸变化后重新应用，完成实际屏幕截图验证。
- 统一不透明窗口与应用根背景，修复透明桌面漏底导致的绿色外露和右侧白条；保留无边框窗口操作。
- 修复窗口视觉回归：恢复透明窗口以保留圆角，应用根节点裁剪背景，避免关闭原生阴影后出现直角。
- 完成 Tauri 窗口边缘像素验收，确认关闭原生阴影后四角无纯白 1px 边框，普通窗口截图验证通过。
- 关闭无边框 Tauri 窗口默认阴影，移除 Windows 为 undecorated 窗口添加的 1px 白色边框。
- 修复认证页根容器未铺满窗口导致的右侧白边，统一 html、body、#app、page 与认证根节点的宽高和背景。
- 关闭 Tauri 透明窗口并恢复普通窗口启动，修复非最大化状态下的白边；前端拦截 F12、开发者工具快捷键和右键调试入口。
- 桌面窗口默认最大化启动，避免透明窗口未覆盖屏幕时露出后方白边。
- 认证页支持密码框 Enter 登录；新增桌面认证标题栏窗口控制，登录按钮补充主题化悬浮交互。

- Tauri 开发版启动时主动恢复、显示并聚焦主窗口，修复窗口进程存在但界面未置前的问题。

## 增加项
- 今日小记改为纯 Markdown 文本编辑模式，支持标题、无序列表和有序列表行首按 Tab 继续书写。
- 桌面端接入 Tauri 单实例启动保护，重复打开时唤醒已有窗口而不创建新副本。

## 修复项
- 移除中间编辑工具栏及弹层，避免非必要操作占用书写区域。

## 未完成项
- 无。
# 增加项
- 强化桌面端今日小记页面根层全宽规则，统一页面盒模型、尺寸和背景铺满行为。
# 修复项
- 修复今日小记右侧 AI 面板与窗口边缘之间因外层页面约束产生的白边。
# 未完成项
- 无。
# 修复项
- 修复桌面端全局内容区按父容器百分比计算导致首页、行动工作台和今日小记右侧未铺满的问题。
- 修复浏览器 H5 宽屏页面 `.page` 的 1400px 最大宽度限制导致右侧背景未铺满的问题。
- 解除前端 F12、Ctrl/Cmd+Shift+I 和右键菜单的全局拦截，恢复浏览器开发调试入口。
- 覆盖 Taro `.taro_page` 默认滚动条槽位，移除其右侧永久预留宽度并保持内容面铺满视口。
- 将浏览器今日小记编辑器顶部日期、自动保存状态和操作按钮压缩为紧凑单行，并扩大备忘录输入区高度。
- 修复今日小记右上角操作按钮受纵向父级样式影响逐个换行，桌面宽屏改为同一行显示。
- 调整今日小记顶部操作组为紧跟日期与自动保存状态排列，避免按钮被推到标题栏最右侧。
- 今日小记编辑器顶部改用日期稳定 Emoji 与按上海日期每日轮换的状态 Emoji，显示偏好仅保存在本机。
- 修复首页“继续推进”未切换行动工作台的问题，点击后会按事项状态筛选并选中对应事项。
- 今日小记顶部两个 Emoji 统一改为按上海日期每天随机切换，当天刷新保持一致且两个位置不重复。
- 移除编辑器日期标题左侧 Emoji，仅保留自动保存状态旁的每日随机 Emoji。
- 移除编辑器顶部日期与标题显示块，保留自动保存状态和操作按钮。
- 将今日小记三个操作按钮移动到编辑器顶部右侧，保存状态保留在左侧。
- 将手记 AI 快捷指令精简为“梳理今日情绪”和“提取待办”，并保持两列等宽同排展示。
- 修复手记 AI 两个快捷按钮受默认宽度影响而大小不一致的问题，统一按钮填充各自网格列。
- 修复手记 AI 快捷按钮高度被 Taro 默认样式拉伸的问题，统一外层与内部按钮的固定高度和行高。
- 进一步锁定手记 AI 按钮网格行高、对齐和外边距，修复两枚按钮垂直位置不一致。
- 修复 AI 结果底部“追加到手记 / 替换正文”按钮文字透明和高度不统一，补齐内外层文字颜色及固定尺寸。
- 将 AI 结果操作区改为两列等宽 Grid，并覆盖所有内部文字节点，修复按钮不对称和文字不可见。
- 修复提取待办候选项的确认/重试按钮文字透明与尺寸异常，统一候选按钮的主题文字色和高度。
- 将今日小记“重置编辑器”改为“Markdown 转换”，保守整理行空白与连续空行并保留原有正文语法。
- 统一三个顶部操作按钮的固定高度、行高和垂直对齐，修复按钮视觉错位。
- 修复今日小记 AI 面板禁用指令按钮与提示文字透明度过低的问题，保持禁用状态但恢复可读性。
- 提高 AI 面板禁用按钮文字的 CSS 优先级，覆盖 Taro 默认禁用透明样式。
- 左侧导航将“行动工作台”改名为“灵感todo”，将“今日小记”改名为“手记”，并交换两者顺序。
- 纠正左侧导航顺序，调整为“手记”在上、“灵感todo”在下。
- 修复桌面导航 Flex 排序，使实际渲染顺序稳定为“手记”在“灵感todo”上方。
- 将速记面板和复盘写入提示中的“今日小记”统一改为“手记”。
# 增加项
- 统一桌面窗口根节点奶油背景与圆角边界，补充 Tauri 原生窗口背景色配置。

# 修复项
- 修复登录页和工作区窗口边缘圆角处露白、根背景色不一致的问题。

# 增加项
- 优化手记右侧 AI 复盘助手的空态、生成态和结果态布局，增加全高面板、独立滚动与轻量状态过渡。

# 修复项
- 修复 AI 结果或待办候选内容撑破右侧面板的问题，保持真实流式请求和待办创建流程不变。
