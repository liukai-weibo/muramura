# 增加项
- 收敛圈圈 AI 业务上下文，明确三种当前事项状态、首页快捷入口和历史状态兼容规则。
- 圈圈 AI 接入当前用户全部历史手记（含当天）作为按问题使用的服务端只读上下文。
- 更新圈圈 AI 的当前业务规则和首页布局说明，优先使用想试试、已开始、已复盘三种事项状态。
- AI 会话删除改用系统风格确认弹窗，支持取消、提交中和失败提示。
- 用户管理列表改为按当前筛选合并加载并在自适应高度区域滚动浏览。
- 用户账号软删除成功后立即刷新正常用户列表，避免已删除账号残留在当前视图。

# 修复项
- 修复圈圈 AI 删除会话弹窗操作按钮继承错误配色导致文字不可见的问题。
- 将手记 AI 复盘助手的输入容器、发送按钮和用户消息气泡限定为透明毛玻璃样式，避免继承全局按钮配色。
- 修复手记 AI 发送按钮贴底，改为输入区内垂直居中并补齐悬停、按下和键盘焦点反馈。
- 让手记 AI 输入容器复用手记编辑区的纸张样式，聚焦时显示金色描边和外发光。
- 统一首页快速记录与继续推进操作区的底部位置和按钮高度，修复两组按钮上下不齐平。

# 增加项
- 完成 DeepSeek Harness 真实登录链路验收，确认插件资源、页面入口、用户上下文和一次性写入确认流程可用。
- 移除圈圈 AI 参数页的普通 API Key 配置入口，保留 DeepSeek Harness 独立配置。

# 修复项
- 恢复“我的 > AI 参数”原有圈圈 AI 配置入口，避免误隐藏非 Harness 配置。
- 隐藏“我的 > AI 参数”中的 DeepSeek Harness 配置卡片，Harness 配置继续在独立 WebUI 中管理。
- 将圈圈 AI 的“模型生成参数”改为默认收起的展开/收起区域，保留原有参数编辑能力。
- 用户管理增加正常用户/已删除用户列表切换，默认只读取正常用户，已删除列表独立分页展示并支持返回。
- 用户管理操作结果改为列表内“操作成功/操作失败”短提示，1 秒后自动消失。
- 修复 Gateway 首次启动 Harness 上游过早返回 502 的问题，改为等待用户独立运行时真正监听后再转发。
- 修复 10086 页面加载 Harness 绝对 `/plugins/*` 资源的代理链路。
- 修复同端口下 Harness 未版本化 `/api/*` RPC 被主 API 接管导致设置和 Agent Preset 404；开发代理与 Nginx 现按 `/api/v1/*` 和 Harness RPC 分流。
- 修复 Gateway 转发时 Origin 与用户 Harness 上游 Host 不一致导致的 403，HTTP 与 WebSocket 上游均改为 loopback Origin。

# 未完成项
- 尚未进行真实浏览器中的确认卡片点击录屏验收；后端令牌与写入闭环已通过临时账户验证。

# 增加项
- 将回收站记录改为紧凑响应式卡片网格，宽屏最多五列并适配移动端。

# 修复项
- 移除回收站分页，改为限定高度的上下滚动列表，保留筛选、选择、恢复和永久删除操作。
- 观察仪表盘隐藏方法复利及方法细分卡片，改为展示长期探索数量卡片。
- 回收站子项的类型和删除状态改为中文展示。
- 撤除 DeepSeek Harness 的活动代理、脚本、API、服务层和管理员配置代码。

# 增加项
- 交换侧栏“灵感todo”和“手记”的显示顺序。

# 修复项
- 修复用户管理正常用户列表在旧 API 进程下因携带默认状态参数而报错的问题。

# 修复项
- 将仪表盘对应记录中的事项状态标识统一转换为中文显示。

# 修复项
- 用户管理顶部刷新、回收站切换按钮统一使用全局系统按钮样式并修复文字可见性。

# 修复项
- 移除用户管理顶部刷新按钮，保留正常用户与已删除用户列表切换。
- 重启 API 后恢复已删除用户列表查询。

# 修复项
- 将回收站卡片复选框固定到事项右上角，并统一为奶油主题复选框样式。
- 固定批量操作预留槽，避免选择记录时工具栏和卡片布局抖动。

# 增加项
- 仪表盘长期探索卡片改为展开最近探索记录摘要，不再直接跳转工作台。

# 修复项
- 修复长期探索指标卡与其他指标卡交互不一致的问题。
- 缩小回收站卡片及批量操作按钮，降低操作区视觉比例并保持布局稳定。
- 修复回收站卡片“恢复”和“永久删除”按钮宽度不一致，改为等分布局并适配窄屏。
- 将长期探索页“新建长期探索”按钮向内收回，避免贴近窗口右边缘。
- 固定工作区一级导航顺序为“首页、灵感todo、手记、圈圈 AI 助手”。
- 修复长期探索历史增多时详情工作区被内容撑高的问题，改为固定可用高度并在面板内滚动。
- 移除长期探索页头重复标签，并为页头两侧内容增加安全边距。
- 修复回收站恢复与永久删除按钮受内容宽度影响而不等宽的问题。
- 统一首页快速记录、继续推进和快速捕获按钮高度。
- 修复 H5 Taro 按钮内部尺寸未同步的问题，统一回收站按钮宽高。
# 修复项
- 将灵感事项详情的“开始执行”恢复为黑底白字，并隐藏“流转历史”入口但保留历史数据逻辑。
# 修复项
- 对齐首页快速记录与继续推进、快速捕获按钮的底部基线，消除卡片内边距差异造成的上下错位。
# 修复项
- 修复首页“快速记录”按钮文字未在按钮内部垂直居中的问题，统一 Taro 按钮宿主与原生按钮行高和对齐方式。
# 修复项
- 提高首页“快速记录”按钮的火龙果玫红色亮度与饱和度，并限定在手记快捷入口。
# 修复项
- 将首页“快速记录”调整为参考图一致的纯火龙果粉色，仅影响该快捷入口及其悬停反馈。
# 修复项
- 将首页“继续推进”主按钮同步为火龙果粉色，保持与“快速记录”一致。
# 修复项
- 统一灵感 todo 桌面详情工作区、详情面板和内容区域的奶油背景，填充顶部、分栏和右侧残留白边。
# 修复项
- 临时隐藏灵感 todo 的“更多”状态入口。
- 将灵感事项详情“开始复盘”和“完成复盘”恢复为黑底白字。
- 将快速记录弹窗“保存”按钮同步为火龙果粉色。
# 修复项
- 快速记录新增内容的时间行改为普通 `HH:mm`，不再写入 Markdown 二级标题标记。
# 修复项
- 将“开始推进”确认弹窗中的“开始！”主按钮恢复为黑底白字。
# 修复项
- 将手记编辑框字体统一为系统无衬线字族，与右侧 AI 面板文本风格一致。
# 修复项
- 将开始推进弹窗中启动动作输入框的聚焦描边由粉色调整为金色。
# 修复项- 宽屏桌面下将圈圈 AI 助手与账户内容收敛到 app-main 可用区域居中，避免固定左对齐。
# 修复项- 将快速捕获弹窗的保存按钮和桌面标题栏快速捕获入口统一为火龙果粉，并限定样式范围避免影响其他主按钮。
# 增加项- 隐藏长期探索页面顶部说明区，将新建长期探索按钮移动到左侧探索列表标题行右侧。
# 修复项- 缩小长期探索左侧列表标题字号，避免与新建按钮挤压重叠。
# 修复项- 将行动、长期探索、方法工作台分栏固定为 24% / 76% 比例并移除拖拽调节，同时让长期探索列表标题完整换行显示。
# 修复项- 将长期探索列表标题改为“记录长期兴趣与历程”，并将新建长期探索按钮移动到左侧列表底部。
# 增加项
- 增加独立手机网页版手记与事项入口，复用现有认证、手记 API 和事项 API。
- 手机端支持日期手记历史、Markdown 编辑、自动保存、事项筛选、快速新建和状态推进。
