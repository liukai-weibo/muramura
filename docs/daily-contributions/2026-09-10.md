# 2026-09-10 每日贡献记录

## 增加项
1. 手机端「事项」按长期探索分组：进行中段以活跃探索名分组（含「已删除 / 不可用的探索」「未归入长期探索」），已复盘段始终显示探索归属徽标。
2. 手机端进行中事项新增「复盘」主操作：只保留「复盘结果」一项输入，提交后事项进入「已复盘」，不提供有效 / 阻力 / 新想法 / 方法字段。
3. 手机端已复盘事项新增「查看复盘结果」展开只读展示。
4. 手机端进行中事项新增「调整探索」操作：可从可选长期探索中重新归入或移除归入。
5. 手机端「新建事项」保持不变，筛选行由 3 列网格改为 2 列（仅「进行中 / 已复盘」两项）。

## 修复项
1. 手机端进行中事项「复盘」与「调整探索」改为同一行并排（去除自动换行，两按钮各占一半宽度）。
2. 手机端「调整探索」弹窗内可选探索与「移除归入」按钮文字由透明修复为可见（对按钮及后代文字节点追加 -webkit-text-fill-color 显式颜色，对齐桌面端既有修法）。