# 2026-09-08 每日贡献记录

## 增加项
- 今日饮食推荐默认提示词更新：建议饮食直接以「1+2+3」型式给出（简单直接、少用「或」、不给你太多选项），与个人档案「恢复默认」同步（`DEFAULT_DIET_AI_PROMPT` 含新文案）。
- 移动端页面（`pages/mobile`）样式优化三次微调：① 快速记录改为右下角悬浮圆形按钮（`position:fixed`，实机直径 100px、距底 100px+安全区、z-index 15、阴影 `0 8px 20px rgba(200,40,100,.3)`、按压缩小 `scale(.95)`），手记页显示、事项页随组件卸载隐藏；② 手记/快速记录/登录输入框统一圆角 20px、`padding:24px`、去除明显边框（`border: transparent`）仅靠 `#f5f1e9` 页面底色与 `#fffaf6` 输入框底色的微弱色差分区；③ 顶部标题 `MaruMaru` 加粗放大至 40px（实机 20px），`admin` 小字 22px（11px）浅灰置下，右上「退出」文字色改主粉 `--mobile-accent`（`#d63d73`），hover/active 透明度 `.72`，点击热区 `padding:16px 24px`；④ 「手记」标题行与右侧「日期历史」垂直对齐（按钮整形 `margin-top:-20px`），标题行上下留白 `margin:1.25rem 0 1rem`；⑤ 底部 Tab 选中态圆角与输入框统一 20px，次要按钮/日期历史容器圆角 18→20px；⑥ 页面内容底部预留增大至 `368px+安全区` 防悬浮按钮遮挡。

## 修复项
- 无。

## 未完成项
- 说明：UAT `knowledge_base_uat` 尚未执行 033（032 亦未执行），未迁移不得视为可用。

## 发布项
- 发布桌面版 v0.1.36（2026-09-08）：今日饮食推荐默认提示词改为「1+2+3」直接建议型；版本 0.1.35 → 0.1.36（`src-tauri/Cargo.toml`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.lock`），以仓库公开签名材料（`src-tauri/signing-material/`）签名构建，`MaruMaru_0.1.36_x64-setup.exe`（6,410,549 字节）+ `.sig`（420 字节）上传 GitHub Release v0.1.36（id 384409809，https://github.com/liukai-weibo/muramura/releases/tag/v0.1.36），updater.json 更新指向 v0.1.36（gh-proxy 实时代理，实测 HEAD 200 len=6410549 + raw 拉取 version=0.1.36 签名一致）。功能提交 05d7db8 + 发布提交 5c46ead 已直推 main。