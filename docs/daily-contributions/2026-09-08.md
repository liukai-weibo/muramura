# 2026-09-08 每日贡献记录

## 增加项
- 今日饮食推荐默认提示词更新：建议饮食直接以「1+2+3」型式给出（简单直接、少用「或」、不给你太多选项），与个人档案「恢复默认」同步（`DEFAULT_DIET_AI_PROMPT` 含新文案）。
- 移动端页面（`pages/mobile`）样式兼容优化：新增 `.mobile-page` 作用域内 `--mobile-*` 变量体系，正文深灰 `#544f49`、辅助文字浅灰 `#948e86`、页面底色极浅米 `#faf8f5`、输入底色 `#fffaf6`，主色由 `#e94781` 降饱和为 `#d63d73`、浅色由 `#f8e6eb` 调整为 `#fceef2`、链接/次按钮 `#a45a6d`、危险文字保留 `#9d4f64`；底部 Tab「手记/事项」按钮高 100px（手机上 50px·源 2.5rem）字号 32px（16px）非直角圆角激活；顶部「日期历史/快速记录」等小按钮 `padding: 6px 12px` 且 `min-height` 44px 触控热区；主手记/快速记录/事项详情等输入框 `padding: 20px` 左右圆角 16px 背景改极浅米白、placeholder 用浅灰；「已自动保存」移到输入区左下角浅灰小字；顶部导航标题「MaruMaru」32px（16px）加粗、用户名小字浅灰置下，右上「退出」由块状按钮改为文字链接按钮；模块纵向间距统一 `1rem`、事项卡片间距与弹窗底部操作从 10/12px 增至 16/24px、`.mobile-content` 底部预留 Tab 栏高度（120px+安全区）；正文 32px（16px）居左不缩小、日期/状态/操作等辅助信息 22-24px（11-12px）保留。

## 修复项
- 无。

## 未完成项
- 说明：UAT `knowledge_base_uat` 尚未执行 033（032 亦未执行），未迁移不得视为可用。

## 发布项
- 发布桌面版 v0.1.36（2026-09-08）：今日饮食推荐默认提示词改为「1+2+3」直接建议型；版本 0.1.35 → 0.1.36（`src-tauri/Cargo.toml`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.lock`），以仓库公开签名材料（`src-tauri/signing-material/`）签名构建，`MaruMaru_0.1.36_x64-setup.exe`（6,410,549 字节）+ `.sig`（420 字节）上传 GitHub Release v0.1.36（id 384409809，https://github.com/liukai-weibo/muramura/releases/tag/v0.1.36），updater.json 更新指向 v0.1.36（gh-proxy 实时代理，实测 HEAD 200 len=6410549 + raw 拉取 version=0.1.36 签名一致）。功能提交 05d7db8 + 发布提交 5c46ead 已直推 main。