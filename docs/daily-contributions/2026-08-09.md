# 2026-08-09

## Audit 精简调整

- 移除客户端未使用的 `@nutui/icons-react-taro` 直接依赖及其锁文件记录。
- 整理 docs 导航，将已停用的 SQLite 迁移、历史技术评审和平台权限 V1 中间过程移入归档目录。
- 新增当前文档导航和架构、产品历史归档说明。

## 增加项

1. 提升打开状态的会话操作菜单层级，避免重命名和删除菜单被相邻会话内容覆盖
2. 增加 CI 成功后通过 SSH 执行云服务器 `/root/start.sh` 的自动部署任务
3. 为服务器启动脚本增加受控 `--ci` 非交互部署模式
4. 兼容既有 CRLF Migration 校验和，避免历史数据库因换行格式差异阻断升级

## 修复项

## 未完成项

1. 暂无
## 回收站永久删除与批量操作

## 增加项

1. 增加回收站展开/收起、跨分页全选、单条永久删除和批量永久删除。
2. 增加永久删除二次确认、事项/方法/探索主线类型支持及 H5 响应式操作布局。
3. 增加回收站永久删除 API、Application 编排和 MySQL 单事务处理。
4. 回收站默认改为收起状态，保留手动展开/收起入口。

## 修复项

1. 修复永久删除失败时批量记录可能部分成功的问题，统一整批回滚。

## 未完成项

1. 微信小程序本轮不纳入验收。

## 长期探索展示文案统一

### 增加项

1. H5 页面中原“探索主线”展示名称统一调整为“长期探索”，覆盖导航、列表、详情、关联选择、回收站和数据统计。

### 修复项

1. 同步更新直接前端 UI 测试中的展示文案断言，避免界面名称与测试口径不一致。

### 未完成项

1. 内部 API、数据库、字段、类型名及历史归档文档保留原技术术语。

## 探索主线级联删除恢复与回收站详情

### 增加项

1. 探索主线软删除与恢复增加关联事项级联标记和同事务处理。
2. 回收站探索主线支持详情弹窗，展示绑定事项及删除状态。
3. 永久删除探索主线时在同一事务中清理关联事项；操作后刷新探索事实。

### 修复项

1. 恢复探索主线仅恢复本次级联删除的事项，避免误恢复用户单独删除的事项。
2. 回收站详情请求增加取消和过期请求保护，删除或恢复后清理旧详情。

### 未完成项

1. 微信小程序本轮不纳入验收。
