# 2026-08-10

## 本轮桌面端原生化

### 增加项

- 增加 Tauri 无框透明窗口、自定义标题栏、托盘恢复/退出和桌面快捷键。
- 增加行动、长期探索、方法工作台三栏布局及 280px～420px 可调中栏。
- 增加桌面端内存前进/后退历史与 H5 空实现桥接边界。

### 修复项

- 修复桌面开发窗口与页面边界融合问题，避免标题栏交互控件被拖拽区域吞掉。

### 未完成项

- Windows 11 多屏、高 DPI、托盘 Alt + Tab、最大化边缘和安装后业务链路仍需人工验收。

### 修复项

- 修复 Windows 已占用 `Alt + Space` 时 Tauri 启动阶段 panic 导致桌面端闪退的问题，快捷键冲突现在安全降级。
- 增加桌面端本地联调入口，统一启动 MySQL 检查和 API 健康检查，减少手动启动服务步骤。
- 完成桌面端与 Web 端托管解耦：Tauri 开发和生产均直接加载 `dist`，Web 端继续独立使用 10086。
- 调整 `fast-ui` 默认开发入口，不再默认启动 10086；保留 `dev:web` 供 Web 端专用开发使用。

## 修复项
- 撤回尚未形成实际页面价值的 Framer Motion 依赖和页面动效包装，恢复静态页面布局。

## 增加项
- 将首页轮播改为引用源码内置 SVG 资源，随 H5 与 NSIS 构建产物发布。
- 新增客户端静态帮助文档资源目录与按需 URL 入口。
- 新增字体资源目录说明，当前继续使用系统字体栈。

## 修复项
- 修复桌面端静态资源未纳入客户端资源目录的问题。

## 增加项

- fast-ui 分支新增首页、行动工作台、圈圈 AI 助手、数据管理和我的五项 H5 主导航。
- 行动工作台增加行动、长期探索、方法页签；数据管理和我的增加局部页签。
- 首页上半区增加本地主题轮播位，支持自动轮播、点击切换和减少动效降级。
- 新增 Tauri v2 桌面端第一阶段壳层配置、H5 构建命令和云端 API 地址环境变量入口。
- 安装 Rust MSVC、Visual C++ Build Tools 和应用图标，成功生成 Windows MSI 与 NSIS 安装包。
- H5 增加 Framer Motion 轻量页面壳层动效及减少动效降级。
- 工作台页签接入共享布局组，激活指示器和内容容器切换保持平滑过渡。
- 桌面端改为分组侧栏，移动端改为底部导航。

## 修复项

- 管理员入口从主导航收拢到“我的”管理区域，保留现有权限判断。

## 未完成项

- 尚未进行完整 H5 人工验收，未合并 fast-ui 到 main，也未触发生产 CI/CD。
- 首页除主题轮播外的具体内容与信息模块暂未确定，尚未实现。
- 当前安装包尚未使用实际云端 API 地址进行最终连接验收。

## 增加项

- 修正 Tauri 正式构建的客户端环境加载方式，使用客户端生产环境文件将云端 API 地址编译进桌面端 H5 产物。
- 重新生成包含云端 API 地址的 Windows MSI 与 NSIS 安装包。

## 修复项

- 修复桌面端构建时仅设置 PowerShell 环境变量但未进入 Taro 产物、导致请求错误回落到同源 `/api` 的问题。

## 未完成项

- 尚未完成安装包安装后的登录、行动、长期探索、AI SSE、桌宠、回收站和管理员页面人工验收；云端健康检查已通过。

## 增加项

- 桌面端和 H5 标题区域增加网络断开提示，恢复联网后自动清除，不改变 API 请求和业务数据。
- 重新生成包含网络状态提示的 Windows MSI 与 NSIS 安装包。

## 增加项

- 本地桌面联调包切换为 `http://127.0.0.1:32146`，不再指向云端 API。
- 本地 `knowledge_base` 已完成 Migration 016，重新启动本地 API 与 H5，供桌面包联调。

## 修复项

- 修复本地 API 旧进程未加载 Tauri CORS 配置导致桌面会话请求失败的问题。

## 修复项

- 修复 Tauri 登录成功后跨源会话 Cookie 未被后续 `/auth/session` 请求带回的问题；桌面联调使用本地 `localhost` API 和 Tauri 专用跨站会话 Cookie。
- 重新生成包含本地 API 与会话兼容修复的 NSIS 测试安装包。
## fast-ui 桌面顶轨与中栏收敛

### 增加项
- 增加桌面顶栏工作台页签、行动中栏紧凑日历与列表刷新入口。
- 为长期探索和方法列表增加刷新入口。

### 修复项
- 收敛桌面端重复搜索、快速捕获、页面 Header 和状态筛选占用空间。
- 统一顶栏、侧栏、中栏和详情区的桌面色阶与边界。

### 未完成项
- Windows 多屏、高 DPI、托盘 Alt + Tab 和最大化边缘仍需人工验收。
