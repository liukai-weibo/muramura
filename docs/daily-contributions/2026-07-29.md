# 2026-07-29

## 增加项

- 增加在线账户与单用户数据隔离 V0 切片 1 的注册、登录、退出与当前会话认证 API。
- 增加 Docker H5 发布地址、H5 宿主端口、API/MySQL 本机宿主端口和应用镜像标签的本机环境变量配置。
- 补齐日常 `.env` 的 Docker Compose 显式变量，并将新机器交付配置统一收敛至“维护相关/新机器需要的配置”。
- 增加 MaruMaru（圈圈）在 H5 浏览器标题、导航和 README 的统一品牌展示。
- 增加 Backup 恢复后读取失败、Abort 与响应丢失边界的 H5 行为回归覆盖。
- 增加数据管理四项真实统计同排展示与 Backup 探索主线数量预览。
- 增加在线账户与单用户数据隔离 V0 切片 0 的账户、会话、初始归属及十个业务集合 owner Schema。
- 公网服务器弄好，已经部署了第一版公网能访问

## 修复项
- 修复在线账户与单用户数据隔离 V0 切片 0 Schema 测试未使用独立临时账号的问题，并补齐冻结账户表、外键与 owner 索引断言。

- 修复 Docker 应用镜像入口脚本的 Linux LF 换行兼容性，并重新发布 1.1.1 与 latest 镜像标签。
- 修复 Docker 局域网 H5 经内部代理写入时被浏览器 Origin 误拒绝的问题，并发布 1.1.2 与 latest 镜像标签。
- 修复 Docker 空 volume 首次启动时 migrate 可能早于 MySQL TCP 就绪而退出的问题，增加最多 30 秒的有界连接等待。
- 修复备份恢复后探索主线与数据总览可能保留旧事实或将读取失败误作空数据的问题。
- 修复页面导航配置覆盖浏览器标题而仍显示“Sprint 0”的问题。
