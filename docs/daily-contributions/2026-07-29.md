# 2026-07-29 每日贡献

## 增加项

1. 增加 Docker H5 发布地址、H5 宿主端口、API/MySQL 本机宿主端口和应用镜像标签的本机环境变量配置。
2. 在运行事实锚点增加 API 错误分类穷尽性守卫收尾修正的授权条目。
3. 在运行事实锚点增加错误契约基础切片的授权条目。
4. 将 `BusinessErrorCode` 单一大联合按业务域拆分到 `packages/contracts/src/errors/` 下的 item、exploration-track、method、review、backup 独立模块文件，聚合联合与 `packages/domain` 的对外导出面保持不变。
5. 将 `ApiErrorCode`、`ApiErrorStatus`、`ApiErrorBody` 统一到 `packages/contracts`，原生与 Hono 两套 API 实现改为共用同一错误数据对象定义。
6. API 失败响应体与 `ApiFailure` 增加可选 `businessCode` 字段，业务规则失败时透出领域错误码，供前端按错误码分支而非匹配文案。

## 修复项

1. 修复 Docker 应用镜像入口脚本的 Linux LF 换行兼容性，并重新发布 1.1.1 与 latest 镜像标签。
2. 修复 Docker 局域网 H5 经内部代理写入时被浏览器 Origin 误拒绝的问题，并发布 1.1.2 与 latest 镜像标签。
3. 修复事项标题空值与超限校验在结构化 API 错误分层下可能误报为内部错误的问题。
4. 修复 `mapBusinessFailure` 穷尽性守卫以 `return unhandledCategory` 收尾的问题，改为显式抛出，避免业务错误分类超出联合类型时返回非 `ApiFailure` 值。
5. 修复 `shouldReportBusinessFailure` 同一处收尾问题，改为显式抛出，避免返回非布尔值被当作真值静默走错上报分支。

## 未完成

1. 独立 Docker 宿主机全链路 QA 仍待执行。
2. `tests/sqlite-s4.test.ts`、`tests/sqlite-method-lifecycle.test.ts`、`tests/unknown-outcome-ui.test.ts` 共 9 个预存失败与本次修改无关，尚未处理。
3. MySQL 集成测试需在显式 `.env.uat` 与 `/health` 确认后执行，本轮未跑。
4. 错误类型本轮只做到按业务域分文件，`BusinessError` 仍是单一具体类；抽象基类与各业务域独立错误子类待后端按业务域拆包后再落地。
5. `assertTransition`、`normalizeItemTitle`、`assertItemTitleLength` 等 item 域逻辑仍留在 `packages/domain/src/index.ts`，未与 item 错误模块同域归置。
6. `apps/api/src/hono/**` 尚未被 `main.ts` 接入，实际运行的仍是 `apps/api/src/index.ts` 原生实现；切换到 Hono 与后续 RPC 接入待独立切片。
7. 前端仍按文案与 HTTP status 判断错误，未消费 `businessCode`；前端错误处理改造随前端重写一并进行，本轮不动前端代码。
8. 字段级校验明细（哪个字段、为何被拒）仍未进入错误契约，待前端重写确认需求后再定。
