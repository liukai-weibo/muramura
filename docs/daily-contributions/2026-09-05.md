# 2026-09-05

## 增加项
- 三餐弹窗移除「今日三餐整体」星级展示（按饱腹度均值折算的 `filledStars` 及 `meal-avg-stars` 样式一并删除）：当日整体星级不再展示、不参与任何保存。已重新构建 H5 并部署到 10086（产物确认无 avg-stars/filledStars/今日三餐整体），git diff --check 干净。
- 三餐弹窗饱腹度胶囊交互：点选中的档位再次点击即取消选择（回到 0=未选择，不参与保存）；点其他档位直接切换为单选状态。已重新构建 H5 并部署到 10086（产物含 `feeling===n?0:n` 切换逻辑），typecheck 仅剩既存 mood-WIP 三处报错无关本项，git diff --check 干净。

## 修复项
- 修复：浏览器打开 http://127.0.0.1:10086 报 `Unexpected token '<' ... is not valid JSON`。根因：H5 静态服务仅回传 index.html（对 /api/v1 请求也返回 HTML），页面 API 响应被当作 JSON 解析失败——后端 API 本身正常（health ready，schemaVersion 32）。修复：H5 静态服务改为内置 /api 与 /health 反向代理（转发到 http://127.0.0.1:32146，透传 Cookie，origin 保持 10086 同源），并监听 0.0.0.0:10086 支持局域网手机访问；页面与 API 全部经同源路径请求。经 10086 代理端到端冒烟通过：注册 → 登录（Cookie）→ 记录情绪（moodLevel 4）→ 今日手记自动追加（04:55 + 情绪内容 + 感受：）→ 手记追加接口（模拟手机端快速记录）→ 读取验证；冒烟账号数据已按 FK 顺序全清（含 activity_audit_events / mood_entries / daily_notes / user_sessions / user_roles / users）。
## 未完成项
- 说明：API 进程由外部自启/守护管理（检测到两条 `node --import tsx src/main.ts` 监听 32146，非本会话任务持有），未做干预；H5 由仓库内临时脚本 `.tmp-serve-h5.mjs` 提供（含 API 代理），后台任务 pwsh-5 持有，请勿自行终止直至有正式静态服务脚本替代。

## 发布项
- 发布桌面版 v0.1.33（2026-09-05）：含三餐饱腹度三档（五分饱=5/七分饱=7/九分饱=9，默认未选=0、点选中档位再点取消）+ 移除「今日三餐整体」星级 + 情绪记录自动同步今日手记（跨端通知）；版本 0.1.32 → 0.1.33（`src-tauri/Cargo.toml`/`src-tauri/tauri.conf.json`/`src-tauri/Cargo.lock`），以仓库公开签名材料（`src-tauri/signing-material/`）签名构建，`MaruMaru_0.1.33_x64-setup.exe`（6,410,552 字节）+ `.sig` 上传 GitHub Release v0.1.33，updater.json 更新指向 v0.1.33（gh-proxy 实时代理）。功能提交 9d63d63 已直推 main。
