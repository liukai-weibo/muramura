增加项
- 隐藏长期探索左侧列表的重复“探索主线”小标题。
- 将长期探索左侧活跃主线列表改为每页固定 9 条的本地分页。
- 将备份恢复入口文案调整为“导入恢复”。
- 将长期探索列表说明调整为“记录一段长期兴趣与历程”。
- 将一级模块展示名称“探索主线”调整为“长期探索”，“数据与设置”调整为“数据管理”。
- 在统一回收站中增加已删除探索主线的筛选、展示与既有恢复入口。
- 将“查看已放弃记录”字重与“探索历史”统一。
- 方法模块改为去外框化双栏工作台：紧凑工具栏、暖灰导航面、近白阅读面与低强调证据/版本折叠行。
- 将新建探索主线入口改为与快速捕获一致的弹窗，提供取消与新建操作。
- 调整探索历史事项查看按钮为桌面端第一行固定定位，小屏保留流式布局。
- 将探索历史事项改为左右两列网格，避免查看按钮相互重叠；左侧主线名称改为单行省略。
- 将探索历史事项查看按钮内置于标题行，并将左侧主线名称截断标记改为字面量省略号。
- 调整探索历史事项说明文本的上下留白，使其与标题行及底部分隔线保持一致。
- 移除探索历史事项标题与准备说明之间的额外间距，提升单项信息密度。
- 隐藏探索历史中与状态标签重复的已完成行动摘要。
- 将探索历史的创建与更新时间改为无底色的低强调灰字，降低其层级。
- 进一步降低探索历史创建与更新时间的文字强调度。
- 探索主线内成功新增事项后同步重新读取行动事项列表，避免必须手动刷新才显示。
- 隐藏探索主线内新增事项区的自动归入提示。
- 探索主线内成功新增事项后同步重新读取当前主线历史，立即展示新增事项。
- 统一探索历史子项标题与主线名称的 18 字符字面量 `...` 截断规则，降低长标题对状态与操作的挤压。
- 将探索主线名称与探索历史子项标题的截断改为按实际渲染宽度计算，统一使用字面量 `...`。
- 将探索历史子项标题的可用宽度收窄至行宽一半，使长标题更早让位给状态、时间与操作。
- 方法模块采用外部页头与内部柔和双栏工作台：左侧暖灰导航、右侧近白阅读面，方法局部操作改为大圆角并保留主行动强调。
- 方法工作台扩展为与其他模块一致的页面主内容宽度，并将列表与详情统一为同一近白浅底色。
- 将方法工作台整体高度提升至原冻结高度的 150%，保留双栏内部滚动。
- 回收站条目恢复改为先显示确认层；点击遮罩外或取消均不写入，确认后才调用既有恢复操作。
- 探索主线改名改为仅保留保存；编辑区外首次点击消费原操作并保存冻结主线，失败与未知结果保留编辑输入。
- 将桌面端探索主线工作台整体高度提升至原来的 120%，小屏仍保持自动展开。
- 将探索历史与已放弃记录合并为同一列表区域的互斥切换，并在切换主线时回到探索历史。

- 调整探索历史子项标题为内容自适应宽度，状态与创建/更新时间紧随标题显示；长标题上限缩减至行宽四分之一。

- 修正探索主线改名输入框的行高，使文字在输入框内上下居中。
- 将数据管理回收站列表改为每页固定 8 条的本地分页。
- 将回收站分页按钮缩小并收拢至页码左右两侧。
- 为快速捕获、长期探索创建和方法应用创建的事项标题增加最多 20 个 Unicode 字素簇的前端输入限制与计数提示。
- 将新建事项标题计数收纳至各创建输入框的右侧末尾。
- 浅化新建事项标题计数的文字颜色。
- 为方法列表子项标题增加单行省略显示。
- 将事项池子项标题的溢出标记改为字面量 `...`。

修复项
- 修复长期探索首页和末页的不可用翻页按钮仍使用正常强调色的问题，改为低强调浅色。
- 为长期探索首尾分页按钮增加点击硬门禁，避免禁用控件事件穿透触发翻页。
- 修复 Taro 可用分页按钮输出 `disabled="false"` 被 CSS 误判为禁用，恢复可用按钮的正常样式与翻页点击。
- 将长期探索左侧主线单项最小高度提升至 68px，减少九条分页列表下方留白并保持分页栏底部对齐。
- 将长期探索左侧主线列表整体下移 14px，保持底部分页栏定位不变。
- 仅在真实存在已放弃记录时展示“查看已放弃记录”切换入口。
- 修复事项状态迁移成功后当前长期探索历史未同步刷新：列表真实读取确认选中主线仍存在后，再读取该主线历史。
- 扩展事项状态迁移成功后的真实刷新定位，覆盖想试试与以后再说、已开始与已暂停的双向切换、两条进入已放弃的迁移及已放弃恢复为想试试。
- 修复数据管理回收站内容超出工作区时无法纵向滚动的问题，仅在该模块按需恢复右侧滚动。
- 修复长期探索左侧第 9 条和分页控件被单条额外内边距挤出可视区的问题。
- 移除桌面端行动、长期探索、方法、观察与数据管理共用页面右侧滚动条，保留各模块工作区内的必要滚动。
- 修复五个主模块在内容未超出视口时仍出现浏览器纵向滚动条的问题：桌面端锁定根页面滚动，仅保留主内容区按需滚动。
- 为探索主线右侧详情区保留稳定的内部滚动条槽位，避免历史过长时详情内容横向偏移。
- 修复事项从想试试移至以后再说、或从已开始移至已暂停后仍停留在旧筛选的问题：仅在真实刷新确认同一事项已处于目标状态后，定位至目标筛选、所在页并选中该事项。
- 修复方法模块工作台被居中限宽与列表内层滚动收缩的问题，改为全宽主容器及双栏内部滚动。
- 为已放弃事项增加探索关联调整和移除的 MySQL 事务只读硬门禁。
- 为重新开始事项增加经确认后覆盖既有启动动作的原子写入边界。
- 修复恢复探索主线后当前事项仍保留已删除关联的问题，恢复路径同步重新读取当前事项真实关联状态。
- 修复删除探索主线后当前事项关联 Context 因选择 ID 不变而未刷新；改为显式重读当前事项的真实关联事实。
- 修复删除探索主线后事项池当前详情仍显示旧可用关联的问题，删除成功后立即重新读取事项真实关联状态。
- 修复已删除探索主线恢复后因选择 ID 未变化而保留旧只读详情的问题，恢复成功后立即重新读取真实详情。
- 移除探索历史详情替换的低优先级渲染调度，避免长历史子项切换时旧内容被延后替换而产生闪烁。
- 修正探索主线改名输入框的行高，使文字在输入框内上下居中。
- 修正探索主线后台刷新及模块切换时以 loading 空屏替换已读取内容的问题，保留真实内容并显示局部更新状态。
- 撤销探索主线本轮界面回退，恢复此前的弹层新建、标题/时间呈现与详情布局。
- 恢复探索主线左侧“定位一段长期行动历程”标题的单行展示。
- 移除探索主线逐项 canvas 测量与 ResizeObserver 截断链，改由 CSS 省略以消除主线切换时的批量布局测量卡顿。
- 减少探索历史刷新时的逐项重渲染、日期格式化与视口外布局绘制，改善长历史列表切换卡顿。
- 为探索主线详情滚动容器固定预留滚动条槽位，避免历史出现或消失时内容横向偏移。
- 将新历史详情替换改为非紧急渲染更新，并移除可能造成可见条目延迟呈现的视口外绘制策略，减少切换闪烁。
- 修正探索历史标题占满行宽导致状态与时间偏移，恢复标签与时间紧随标题显示。
- 事项开始执行并完成刷新后，重新读取当前探索主线历史，及时展示最新状态事实。
- 修复事项完成复盘后探索主线历史未同步更新状态标签的问题。
- 修复事项调整探索主线关联及状态切换后，探索历史未同步更新的问题。
- 修复事项移入回收站或恢复后，探索历史未同步更新的问题。
- 已放弃事项继续展示真实探索主线关联，但隐藏关联调整、移除、归入与选择器入口。
- 将“查看已放弃记录”字号与“探索历史”统一。
- 为重新考虑后的既有启动动作增加明确覆盖确认层，取消保留草稿且未知结果仅允许重新读取真实数据。
- 压缩方法模块工作台外层留白，按冻结比例与视口高度调整桌面双栏布局并保留小屏单栏。
- 移除方法模块冗余外层白框，使标题与工作台层级与探索主线一致。
- 修复删除探索主线确认层未居中且点击弹框外不会取消的问题：改为全屏遮罩居中展示，遮罩点击取消而弹框内操作保持不受影响。

未完成项
- 无。

增加项
- 新增本机日常库初始化、启动与受控停止脚本，包含显式 Backup V3 恢复和 UAT/unknown API 端口拒绝保护。
- 新增本机迁移与一键启动演练说明。
- 新增事项标题按 Unicode 字素簇限制为最多 20 个。
- 新增全栈 Docker Compose 本机启动能力：应用镜像运行 H5、API 与内部代理，并复用官方 MySQL 镜像、命名数据卷和既有空库迁移。
- 将 MySQL 账号初始化规则内置到应用镜像的 migration 模式，部署时不再依赖本地 `docker/mysql/` 目录。
- 将 Compose 切换为阿里云应用镜像 `:latest` 拉取模式，并为当前镜像创建 `:latest` 与 `:1.0.0` 本地发布标签。
- 将当前候选应用镜像的 `:1.0.0` 与 `:latest` 标签上传至指定阿里云私有镜像仓库。
- 移除运行镜像内的 Taro CLI 测试夹具与全部 `.env` 文件，并发布不可变修复候选 `:1.0.1-qa.1`。
- 将干净交付包的镜像解析默认值与 `.env.example` 固定为修复候选 `:1.0.1-qa.1`。
