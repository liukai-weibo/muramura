# 2026-08-06

## 修复项补充
- 将 AI 流式内容改为定时批量刷新，减少长回复期间的重复 Markdown 解析和短生命周期对象。
- 复杂 AI 建议改为至少使用一个贴合语境的 Emoji，简单问答保持简洁。

## 修复项补充
- 隐藏 AI Markdown 生成的仅含标点列表项，避免显示空项目符号。
- 复杂 AI 建议改为至少使用一个贴合语境的 Emoji，简单问答保持简洁。
- 增加 Windows 本地 API/H5 编排的进程树清理支持，解除统一启动器对 WSL/Linux 的强制限制。
- 将复杂 AI 建议的 Emoji 频率调整为通常 2～3 个，并完成 Windows 本地重启验证。
- 修复 Markdown 有序列表跨子列表后序号重置为 1 的问题，改为沿用原始序号。
- 清理 Markdown 列表项开头多余的冒号，避免显示“• ：”这类重复标记。
- 兼容加粗包裹的列表前置冒号，避免“**：**内容”继续渲染为重复标记。
- 扩展列表前置冒号清理，兼容带空格、重复和加粗标记的变体。
- 在流式 Markdown 分行前统一清理列表标记后的冒号，避免生成过程中出现临时拼接残留。
- 支持带缩进的 Markdown 引用，并减少流式全文扫描，将刷新间隔调整为 32ms 以恢复顺滑输出。
- 归档当前 Hunter Prompt、Markdown 格式约束、Emoji、AI 参数和流式渲染基线。

## 增加项

- 优化 Hunter 参谋回复为自然段落与加粗核心词表达
- 增加 AI 回复的轻量 Markdown 展示能力
- 收紧 AI 长回复的段落、列表和引用间距，改善连续阅读感
- 允许本机 localhost H5 来源访问 API
- 增加温暖工作台视觉 Token、卡片层级和首页整体视觉基础
- 调整左侧导航层级，将观察模块归入数据管理分组
- 重做 AI 页面工作台式布局、会话侧栏层级、对话阅读宽度和输入区视觉
- 为新会话空态增加轻量引导文案，减少页面空白感
- 统一 AI 页面暖灰工作台布局，收窄全局侧栏并取消会话白板孤岛效果
- 将发送控件改为暗色箭头按钮，生成中保留停止生成文字与无障碍标签
- 修复 AI 页面历史样式覆盖导致的会话白板、橙色发送按钮和橙红选中态未生效问题
- 收敛 AI 页面最终视觉覆盖，确保会话栏透明分割、灰色选中态和暗色发送图标在运行 H5 中生效
- 隐藏 AI 消息滚动条上下三角按钮，保留滚轮、拖动和历史分页能力
- 兼容桌面浏览器单按钮滚动条变体，进一步隐藏上下三角滚动按钮
- 隐藏 AI 消息区原生滚动条装饰，保留滚轮、触摸和历史分页滚动能力

## 修复项

- 移除强框架回复中的固定显式结构标题要求，降低模板化机器感
- 修复 localhost 页面保存 AI 配置时被来源校验拦截的问题
- 补充 IPv6 loopback H5 来源，避免本机不同地址打开页面时触发来源拦截
- 兼容本机代理将来源映射到 API loopback 端口的写入请求
- 打开快速捕获时清除过期的全局错误提示，避免遮挡本次事项创建结果
- 将 AI 回复中的标题、列表、引用、加粗和行内代码转换为可读布局
- 引导复杂建议使用少量语境化 Emoji，避免回复过于素白
- 增强加粗标记的空格与跨行容错，避免原始 Markdown 符号直接显示
- 修复普通段落未经过行内 Markdown 解析，以及编号列表被空行重置的问题

## 未完成项

- 无

## 修复项

- 将 AI 消息区原生滚动条替换为仅保留细滑块的轻量自定义滚动条，隐藏上下三角按钮并保留滚轮、触摸、拖动和历史分页能力
- 降低 AI 消息区自定义滚动条的常态与悬停对比度，减少视觉干扰
- 进一步降低 AI 消息区滚动条对比度，使其更接近背景弱提示
- 将用户管理与 AI 参数配置改为独立视图，避免两个管理模块同时渲染
- 为 AI 生成参数增加数字输入与滑块双向同步，并统一管理页暖灰工作台样式
- 增加 H5 AI 页面轻量圆环吉祥物占位组件，使用独立 SVG/CSS 待机、眨眼和漂浮动画
- 将吉祥物绑定到 AI 悬浮输入框，增加输入倾听、AI 思考状态和可访问的本地轮换气泡互动
- 修复 Dockerfile 对已归档 IndexedDB workspace 路径的引用，并补齐 storage-secrets workspace 清单复制
- 为用户管理操作通知增加关闭入口
- 修复用户管理搜索清除按钮的文字对比度
- 统一用户管理 AI 配置输入框的圆角与暖灰工作台样式
- 隐藏侧栏本地数据库状态及非行动页面的全局统计副标题
- 修复隐藏侧栏状态后账户退出区失去底部定位的问题
- 强化用户管理 AI 配置输入框的实际渲染圆角、尺寸和焦点样式
- 兼容 Taro H5 输入节点，确保用户管理 AI 配置输入框的圆角样式落到最终渲染层
- 为用户管理 AI 配置控件增加稳定专用 class，移除对 Taro 会改写的 input 标签选择器依赖
- 修复 AI 配置输入框内容盒计算导致的多列横向重叠
- 为 Docker Compose 增加独立的 AI 密钥持久化 named volume 与文件型 SecretStore 配置。
- 修复 Dockerfile 对已归档 IndexedDB workspace 路径的引用，并补齐 storage-secrets workspace 文件复制。
- 增加容器内 AI 配置文件的目录与文件权限保护，避免 API Key 写入镜像层或普通环境配置。
- 新机器尚未完成容器重建后的 API Key 持久化人工验收。
- 基于本次 Docker 持久化修复构建并推送应用镜像 `dnf:1.1.4` 与 `dnf:latest`。
