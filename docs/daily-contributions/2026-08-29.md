# 2026-08-29 每日贡献

## 增加项
- 首页 AI 卡片刷新操作审计语义化：新增审计动作 `refresh`（此前复用 `update` 显示为「编辑」），刷新生成写缓存后记录为「刷新」。
- 审计内容快照改为可读：刷新的快照包含当前卡片标题与缓存日期（此前为 `cacheDate`/`cacheId` 两条无价值内部信息）。

## 修复项
- activity_audit_events 的 `action_code` CHECK 约束未含 `refresh`，新增的刷新审计写入被数据库约束拒绝（safeAuditRecord 静默跳过）；新增 Migration 029 扩展约束枚举，已应用（schemaVersion 28 → 29）。
- 前端/API 审计动作中文映射补充 `refresh: '刷新'`（后端审计路由 CSV 导出与审计中心筛选项/标签）。
- 硬件工程基线：tauri cli 升至 ^2.11.0（匹配 tauri crate 2.11.x），桌面构建产物新增 updater 签名（.sig）；新增 updater.json（当前版本 v0.1.13，endpoint 指向仓库根）与私有签名密钥 `src-tauri/private.key`（不入库，.gitignore 排除）。构建需注入 TAURI_SIGNING_PRIVATE_KEY 与 TAURI_SIGNING_PRIVATE_KEY_PASSWORD（空密码会被判定为未设置而挂起等待输入）。
- 安全审计中心报「审计事件响应结果无效」：api-client 的 auditActionsOrder 硬编码枚举漏了 refresh，后端新增 refresh 事件后前端响应校验拒收；已补 refresh，接口事件全部可解析。
- 编辑自定义卡片时 AI 提示指令编辑框只显示两行（Taro textarea-core 外层 110px 但内部 textarea 高 40px，多余文字提前滚动、下方留白）：给外层固定 height 并让内部 textarea height:100% 撑满，现可见约 4 行、多行才滚动。
- 桌面端「检查更新」：账户页修改密码上方新增入口，弹窗展示当前/最新版本，有更新可下载并自动安装重启（Tauri updater 插件 + GitHub Release 数据源，updater 公钥可离线验证）。
- 发布桌面版 v0.1.14（含检查更新等本轮改动）：构建 `MaruMaru_0.1.14_x64-setup.exe`（6,401,711 字节）+ updater 签名上传 GitHub Release v0.1.14，updater.json 指向 v0.1.14。
- 修复检查更新 endpoint 在部分网络不可达：raw.githubusercontent.com 改由 jsDelivr CDN 托管 updater.json（endpoint 指向 cdn.jsdelivr.net/gh/liukai-weibo/muramura@main/updater.json），已重建 0.1.14 并重传 Release。
- 发布桌面版 v0.1.15：endpoint 已改 jsDelivr CDN 版，`MaruMaru_0.1.15_x64-setup.exe`（6,403,432 字节）+ .sig 上传 Release v0.1.15，updater.json 指向 v0.1.15。
- 修复检查更新下载进度条恒为 0（分块字节未累计，改累加）；发布 v0.1.16（镜像下载）/v0.1.17（进度修复）。
- 发布 v0.1.24：修复用户管理「更多」菜单被列表 overflow 裁剪（删除账号需滚动）；改 fixed 顶层面板 z999。此前发布 v0.1.22（进度平滑+字节）、v0.1.23（复核）。
- 增加项：今日饮食推荐 AI 提示词更新为「顶级营养师」版（含身高/体重、按时间分析今日剩余饮食、推荐简单好记、结尾无问句钩子、结论不重复基础信息）。
- 发布 v0.1.25：今日饮食推荐提示词营养师版（含身高体重/按时间分析/简单好记/无问句钩子/不重复基础信息）。
- 修复项:云服务器 docker 拉取镜像报 no space left on device（磁盘满）；部署 workflow 在 start.sh 前加 docker system prune -af 清理未使用镜像/缓存，CI run #94 恢复 success。
- 增加项：今日饮食推荐新增「个人档案」配置（身高/体重/年龄/性别/目标/日常活动量/健康状态可单独配置并拼入提示词；提示词移除硬编码身高体重；默认新增盒马等常见食材偏好句；入口为首页卡齿轮角标 + 详情弹窗「个人档案」按钮；配置存 localStorage「mararumu.daily-diet.profile」；综合采用 buildDietProfileSegment 纯函数）。
