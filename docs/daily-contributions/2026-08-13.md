# 2026-08-13

- Fixed missing CORS headers on AI SSE responses for the desktop WebView.
- Fixed missing CORS headers on the daily-note AI SSE response used by all note assistant commands.
- Refined the desktop daily-note timeline, editor toolbar, empty-state guidance, local Markdown export, and two-second autosave feedback.
- Protected daily-note AI streams against stale-date updates and locked individual todo creation actions against duplicate clicks.
- DeepSeek reasoning display was evaluated and intentionally deferred; no related code remains.
- Improved daily-note todo extraction parsing for fenced or wrapped JSON responses and added an explicit empty-result state.
- Refreshed the action workbench after AI-extracted todo creation so newly created items appear immediately.
- Added desktop global shortcuts for quick notes and today's notes, plus an immediate sidebar reminder for empty daily notes.
- Fixed tray-menu exit handling so quit requests are received from every authenticated page, not only the daily-note page.
- Added daily-note emotion shortcuts, multi-select action insertion, editor reset/template actions, save retry feedback, and lighter AI panel guidance.

## 增加项

- AI 流请求失败时展示本地 API、HTTP 状态或上游连接的可辨识错误信息，避免仅显示泛化失败提示。

## 修复项

- 将圈圈 AI 路由纳入已登录会话认证链，确保桌面端 Bearer 会话可用于 AI 流请求。

## 未完成项

- 无。
