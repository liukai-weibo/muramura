# 2026-08-24

## 增加项
- 封板：fast-ui 分支（奶油线稿视觉基线 + Schema 21/22 模块）合并 main 并推送，Docker 镜像构建与云部署 CI 通过；桌面版本升至 0.1.7，构建 NSIS 安装包 MaruMaru_0.1.7_x64-setup.exe 并发布 GitHub Release v0.1.7。
- 新增「今日状态小结」模块（首页第三卡）：首页情绪/三餐同规格奶油渐变卡，展示当日小结预览（2 行截断）；点击打开详情弹窗（日期前后切换、空态、ESC/蒙层关闭、手动重新生成按钮）。
- 新增 daily_summaries 表与 Migration 023（每 owner/日期一行 upsert，content 最长 4000，无 DELETE）、Contracts/Application/Hono API（GET /api/v1/daily-summaries 范围读取 + GET /{entryDate} + PUT /{entryDate}）与 Backup V8（含 dailySummaries 导出/恢复，≤V7 恢复缺字段视为空）。
- 每日状态小结自动生成：进入首页跨天检测 + 页面开着跨 00:00 定时补触发（无系统级调度器）；当天已有小结不重复调用；AI 未配置静默降级为空态 + 手动按钮；incomplete/error 不落库不自动重试。
- 新增非 admin AI 端点：GET /experimental/ai-config-status（返回 {configured}，普通用户可判断 AI 是否已配置）、POST /experimental/ai-chat/stream-ephemeral（不落 AI 会话历史的流式通道，避免污染用户 AI 对话历史）。
- 增加 3 组自动测试：每日状态小结 Application 校验、MySQL 仓储集成（upsert 语义 + 范围/隔离 + 备份往返）、Backup V8 导出/恢复/兼容；更新 hono-route-table 路由断言（新增 5 条路由）。
- 日常 knowledge_base 执行 Migration 023，实测 Schema 23，状态小结可验收（自动/手动生成经 Playwright 实测通过，AI 已配置时首页自动生成并展示；迁移前已做逻辑备份 schema22-before-023-2026-08-24-17-03-22.sql）。
- 每日状态小结体验收敛：详情弹窗暂时移除日期前后切换（前一天/后一天），固定只看今天；`initialDate` 接口保留兼容，SCSS 导航样式保留待日后恢复。
- AI 知识范围扩展：圈圈助手（主 AI）上下文新增全日期情绪/三餐快照（AiKnowledgeOverview 扩展 + formatKnowledgeContext 注入，不注入每日状态小结）；手记侧 AI 仅读取该手记当日的情绪与三餐（路由层按 entryDate 过滤拼接，AI 层零改动）。
