增加项

- 将新机器交付目录的 Compose 与本机 `.env` 同步为不可变候选镜像 `:1.0.1-qa.1`。
- 为 Compose 固定项目名，使交付目录使用中文或其他非 ASCII 名称时仍可解析配置。
- 增加同时保留本机回环与局域网 H5 入口的 Compose 发布/恢复脚本，允许 Private/Public 网络类别并复用既有 MySQL 项目，API 和 MySQL 继续仅本机访问。
- 将本地 Taro + Vite H5 开发服务器调整为局域网测试监听，并通过既有允许 Origin 代理到回环 API，保留 API 与 MySQL 的回环边界。

修复项

- 修复交付包仍解析失败候选 `:latest`、且未传递 `KB_APP_IMAGE_TAG` 的问题。
- 将本地 Taro + Vite H5 开发服务器从局域网测试监听恢复为仅回环监听，移除 LAN 测试代理覆盖。

未完成项

- 独立 Docker 宿主机全链路 QA。
