# 2026-07-28 每日贡献

## 增加项

1. 在运行事实锚点增加探索主线 V1 解封板裁决与 Repository 查询优化切片授权条目。
2. 将新机器交付目录的 Compose 与本机 `.env` 同步为不可变候选镜像 `:1.0.1-qa.1`。
3. 为 Compose 固定项目名，使交付目录使用中文或其他非 ASCII 名称时仍可解析配置。
4. 增加同时保留本机回环与局域网 H5 入口的 Compose 发布/恢复脚本，允许 Private/Public 网络类别并复用既有 MySQL 项目，API 和 MySQL 继续仅本机访问。
5. 将本地 Taro + Vite H5 开发服务器调整为局域网测试监听，并通过既有允许 Origin 代理到回环 API，保留 API 与 MySQL 的回环边界。
6. 重新开启本地 Taro + Vite H5 的局域网监听复测，保留 API 与 MySQL 的回环边界。

## 修复项

1. 修复 `MySqlExplorationTrackRepository.listActive` 的 N+1 查询：逐主线查最新关联事项改为批量查询。
2. 修复 `MySqlExplorationTrackRepository.historyEntries` 的 N+1 查询：逐事项查复盘改为按 `item_id IN (...)` 批量查询。
3. 将 `exploration-track-repository.ts` 内全部 `SELECT *` 替换为显式列名，消除加列后 mapper 漏映射的脆弱性。
4. 修复交付包仍解析失败候选 `:latest`、且未传递 `KB_APP_IMAGE_TAG` 的问题。
5. 将本地 Taro + Vite H5 开发服务器从局域网测试监听恢复为仅回环监听，移除 LAN 测试代理覆盖。
6. 完成局域网监听复测后，将本地 Taro + Vite H5 恢复为仅回环监听。

## 未完成

1. Repository 内 `assignItemToExplorationTrack` 与 `removeItemFromExplorationTrack` 仍在 Repository 层 `new Date()` 生成 `updated_at`，与其他方法由 Application 传入时间戳不一致，待后续切片统一。
2. MySQL 集成测试需在显式 `.env.uat` 与 `/health` 确认后执行，本轮未跑。
3. 独立 Docker 宿主机全链路 QA。
