# 2026-08-25

## 增加项
- 新增「今日饮食推荐」模块（首页第三行第二卡，2×3 布局）：首页手记/横幅之下第 2 行（情绪+三餐）、第 3 行（状态小结+饮食推荐）各两卡；饮食卡为暖橙奶油渐变卡，预览 2 行截断；点击打开详情弹窗（固定今日、空态、ESC/蒙层关闭、手动重新生成按钮——无日期切换）。≤720px 时 second 两行均退回单列纵向排布。
- 新增 daily_diet_recommendations 表与 Migration 024（每 owner/日期一行 upsert，content 最长 1200，无 DELETE）、Contracts/Application/Hono API（GET /api/v1/daily-diet 范围读取 + GET /{entryDate} + PUT /{entryDate}）与 Backup V9（含 dailyDietRecommendations 导出/恢复；恢复时对 V8/V9 备份的 dailySummaries 一并恢复，修复此前 V9 恢复丢弃状态小结的版本门槛）。
- 今日饮食推荐自动生成：跨天检测 + 页面开着跨 00:00 定时补触发（当天已有不再调用；AI 未配置静默降级为空态 + 手动按钮）；生成复用全局 AI 接口 /experimental/ai-chat/stream-ephemeral（先分析最近 7 天饮食/手记/情绪，只针对饮食给 1-2 点建议，150 字内，不编造），不注入状态小结。
- 首页布局调整：.home-checkin-row 由三卡改两卡列（情绪/三餐），新增 .home-dynamic-row 两卡列（状态小结/饮食推荐），media 720px 内两行均单列。
- 增加 3 组自动测试：今日饮食推荐 Application 校验、MySQL 仓储集成（upsert 语义 + 范围/隔离 + 备份往返）、Backup V9 导出/恢复/兼容；更新 hono-route-table 路由断言（新增 3 条 daily-diet 路由）与 hono-rpc 备份版本断言（version 含 9）。
- 日常 knowledge_base 执行 Migration 024，实测 Schema 24，今日饮食推荐可验收（自动生成 + 手动生成 + 2×3 布局 + 720px 单列降级经 Playwright 实测通过；迁移前已做逻辑备份 schema23-before-024-2026-08-25.sql）。
- 近期状态小结 / 今日饮食推荐两张首页卡片：纵向尺寸对齐手记卡（min-height 260px），并改为「左下内容列纵向 space-between + 右侧箭头垂直居中」布局，主内容预览放大（字号 15px、行高 1.75、最多 6 行，预览字符 44→150）以填满高度、消除上下大片留白。
- 「今日状态小结」更名为「近期状态小结」（首页卡 kicker/空态标题、弹窗标题/空态/按钮、auto 提示词，仅用户可见文案；表/路由/方法名/备份不变）；「今日饮食推荐」名称保持，其生成改为先分析最近 7 天的饮食/手记/情绪记录再给 1-2 条只针对饮食的建议（buildDietPrompt 新增纳入最近 7 天手记，DIET_PROMPT 措辞同步）。
- 今日饮食推荐角色收敛：推荐生成改为「仅基于最近 7 天三餐与情绪、只针对饮食」——前端读取最近 7 天三餐/情绪拼入提示词（buildDietPrompt），明确饮食营养师角色（具体到食物/怎么吃/补水/避免什么），不再泛化到作息/行动/整体心情，与「今日状态小结」（全量整体分析+阻力+建议）明确区分角色；手动生成弹窗同步复用该提示词。
- 「近期状态小结」生成提示词（SUMMARY_PROMPT）由多行结构化指令简化为「分析下我最近的状态」（daily-summary-auto.ts，自动生成与手动生成共用；纯前端文案，无后端/表/路由改动）。
- 新增「安全审计中心」（仅平台管理员可见/可用）：activity_audit_events 表 + Migration 025；Contracts/Application（ActivityAuditRecorder、ScopedActivityAuditRecorder、safeAuditRecord）、MySQL 仓储（MySqlActivityAuditRepository）、Hono API（GET /api/v1/admin/audit/events 分页 + GET /api/v1/admin/audit/export CSV，requirePlatformAdministrator 鉴权）与 H5 页面（共享筛选栏按用户/功能模块/时间/操作类型/关键词过滤，6 列表格，20 条/页，≤720px 单列）；在业务写入成功后追加审计事件（手记新建/编辑、情绪新建/编辑/删除、三餐整日保存、事项新建/编辑/删除/恢复、全局搜索非空关键词），审计写入失败不阻断业务写入；MYSQL_REQUIRED_SCHEMA_VERSION 升至 25。
- 增加审计中心自动测试：activity-audit-application（safeAuditRecord 不阻断业务写入 + 各捕获点事件映射）、mysql-activity-audit.integration、audit-authorization.integration（未登录 401 / member 403 / platform_admin 200 + CSV headers）；更新 hono-route-table（+2 条审计路由 + admin/audit 中间件前缀）与 api-startup-diagnostics（Schema 25 断言）。
- 新增「首页自定义 AI 卡片」模块：首页系统卡之下新增 .home-custom-ai-row 行（两列网格，≤720px 单列，行尾虚线「➕ 新增自定义AI卡片」占位）；每张卡片可配置标题/提示词/尺寸（small 紧凑 1 列 ~120px、medium 标准 1 列 260px、large 跨两列整行）/主题（cream/green/beige 三档奶油线稿色板，含深色主题适配）/刷新方式（daily 每天自动、manual 手动）。卡片点击打开详情弹窗（完整内容 pre-wrap、ESC/蒙层关闭、加载/失败/空态）；⚙️ 编辑按钮打开编辑弹窗（标题 ≤50 字、提示词 ≤2000 字、尺寸/主题/刷新方式分段选择）；卡片管理入口含删除确认。
- 新增 user_home_ai_cards 表与 Migration 026（每用户最多 12 张卡，sort_index 递增，card_size/card_theme/refresh_mode 枚举，is_hidden 预留）、user_home_ai_card_caches 表（每 (owner_user_id, card_id, cache_date) 一行，UNIQUE 幂等）、Contracts/Application/Hono API（GET /api/v1/home-ai-cards 列表 + GET /caches?date= 当日缓存 + POST 创建 + PUT /:cardId 更新 + DELETE /:cardId 删除 + PUT /:cardId/caches/:cacheDate 缓存 upsert）与 Backup V10（含 homeAiCards/homeAiCardCaches 导出/恢复；恢复 V9 及以下备份时两集合重置为空）。
- 首页自定义 AI 卡片自动生成：跨天检测 + 持仓页跨午夜定时补触发（daily 且当天天未生成才调用，已有缓存不重复生成，AI 未配置静默降级为空态 + 手动按钮）；生成复用全局 AI 接口 /experimental/ai-chat/stream-ephemeral（以卡片自身 aiPrompt 为提示词），成功后写入当日缓存；卡片失败不自动重试。MYSQL_REQUIRED_SCHEMA_VERSION 升至 26。
- 增加首页自定义 AI 卡片自动测试：home-ai-card-application（长度/枚举/12 卡上限/缓存日期校验与 upsert/排序/删除级联）、mysql-home-ai-cards.integration（随机临时库：排序索引、owner 隔离、删除级联清缓存、upsert 幂等、备份往返）、backup-v10-home-ai-cards（导出/解析/恢复往返、非法记录拒绝、V9 恢复两集合清空）；更新 hono-route-table（+6 条 home-ai-cards 路由）、hono-rpc 备份版本断言（version 含 10）与 api-startup-diagnostics（Schema 26 断言）。
- 日常 knowledge_base 执行 Migration 026，实测 Schema 26（/health 为 ready / knowledge_base / schemaVersion=26；迁移前已做逻辑备份 schema25-before-026-2026-08-26-00-59-44.sql，项目外 维护相关/运行库备份/）。

- 归档发布：fast-ui 三笔提交（AI 上下文扩展 / 今日饮食推荐+改名+布局 / 版本 0.1.8+README）合并 main 并推送；Tauri 桌面构建 MaruMaru_0.1.8_x64-setup.exe，发布 GitHub Release v0.1.8（含安装包资产）；README 同步 Schema 24 与近期状态小结/今日饮食推荐能力。

## 修复项
- 修复审计中心列表「审计事件响应结构无效」：三餐整日保存产生的 meal/update 审计事件 `entity_id` 为 NULL，后端仓储在 entity_id 为空时省略 entityId 键（返回 8 键），而前端 parseActivityAuditEvent 用 hasExactKeys 强制精确 9 键（含 entityId）导致校验失败；改为 hasKeysWithOptional 接受 entityId 键可选（空时省略、存在时须为 string），并新增 audit-center-api-client 测试（带/不带 entityId 均接受，entityId=null、多余键、非字符串 snapshot、未知 module 均拒绝）。
- 修复审计中心搜索框无样式：审计中心筛选输入框原用原生 input 元素选择器，被 Taro 编译为 taro-input-core 后失配，改为 .audit-center-text-input 类选择器并补齐 4 个输入框 className，恢复灰棕边框/8px 圆角/14px 字号/灰棕聚焦环。
- 修复审计中心筛选/清除/导出按钮样式与字体颜色：筛选按钮原复用 platform-administration-search-button 但缺少 .platform-administration-search 祖先使深底白字未生效，在审计中心内补主按钮深底白字与 hover/active/disabled 态；导出按钮补审计中心统一边框圆角与深字白底（覆盖全局奶油主题胶囊样式）。
