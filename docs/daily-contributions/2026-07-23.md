# 2026-07-23

## 增加项

- 增加 SQLite S5 备份文档解析、恢复与九集合规范化等价性测试。
- 增加 SQLite S5 非法备份零覆盖、末端写入回滚与系统元数据隔离测试。
- 增加 SQLite S5 断裂可选方法版本来源的解析、恢复与导出归一化闭环测试。
- 增加 S6-A 本机 Local API 的 loopback 健康检查与 SQLite 不可用保护测试。
- 增加 S6-A 静态文件托管、缺失资源与编码路径穿越拒绝测试。
- 增加候选 SQLite 环境诊断接口、无持久化写锁探测与稳定失败分类测试。
- 增加候选环境四类数据库打开失败的脱敏 HTTP 诊断契约测试。
- 增加 MySQL Docker 新 volume 初始化与已有 volume 可重复 root 权限收敛路径，维护独立 app / migrator 最小权限并清理历史 DDL 测试遗留表。
- 完成 MySQL M1 的真实 Compose 验收：健康检查、幂等迁移、checksum 失配拒绝、app 最小权限、健康检查故障降级与命名 Volume 重启持久化。
- 增加 MySQL M1 可重复集成测试，覆盖迁移幂等与 checksum 拒绝、app DML/DDL 权限和 HTTP 健康检查脱敏契约。
- 增加 MySQL 专用临时未迁移数据库的真实 `/health` 验证，确认 `MYSQL_SCHEMA_NOT_READY` DTO 与清理边界。
- 增加 MySQL M1 稳定审阅结论，冻结 M2 评审授权、候选 Repository 边界与 MySQL DDL 迁移语义勘误。
- 增加 MySQL M2-A Item Repository，实现事项、状态历史、启动动作、说明、回收站和安全永久清理的候选数据层能力。
- 增加 MySQL M2-B Review Repository 与 BackupData 九集合原子导入导出候选数据层能力。
- 增加 MySQL 私有 `system_metadata` 基础设施 migration，并保持其与业务备份隔离。
- 增加 MySQL M2-B 临时 migrator 最小权限验证，以及 Review 被方法证据或版本来源关联时删除拒绝的零副作用集成测试。
- 完成 MySQL M2-A 稳定审阅并封板，授权在既定边界内实施 M2-B 的 Review 与 Backup 候选 Repository。
- 增加 M2-B 稳定审阅与 IndexedDB 状态历史排序 P1 分流裁决，冻结其最小修复与回归封板门槛。
- 完成 MySQL M2-B 最终稳定审阅并封板，确认 IndexedDB 状态历史排序 P1 的修复与全量回归闭环。
- 增加 MySQL M3 分段架构任务书，冻结方法生命周期、003 约束 migration、墓碑和跨对象永久清理的候选实现边界。
- 增加 MySQL 快速接入：H5 通过开发代理调用 loopback API，并以 MySQL 为唯一运行数据源。
- 增加 H5 用户主动刷新入口，以及 API / MySQL 不可用时的初始化错误反馈。
- 增加本地 Docker MySQL、API 启动、迁移和环境变量说明。
- 增加 H5 API Adapter 的读取取消、写入未知结果与已确认 HTTP 失败回归测试。
- 增加 unknown-outcome 不自动刷新、不重发写请求、保留草稿及用户主动刷新入口的前端回归测试。

## 修复项

- 修复 H5 QA 入口指向不存在的 `192.168.128.1`，收敛为仅本机回环地址并同步精确 CORS 白名单。

- 修复 MySQL Docker 默认 app 用户被官方镜像授予业务库 `ALL PRIVILEGES`、既有数据卷未执行权限初始化的问题。
- 同步 MySQL M1 Schema v3 成功路径测试基线。
- 增加 MySQL M3-A 方法、版本与证据候选 Repository 基础生命周期及 migration 约束验证。
- 增加 MySQL 003 migration 四类脏数据预检失败的零 DDL、零成功 migration 记录真实集成测试。
- 增加 MySQL M3-A 稳定审阅与 Schema v3 测试基线裁决，冻结 M1 集成测试的最小预期同步范围。
- 完成 MySQL M3-A 正式封板，授权 M3-B 的方法应用、墓碑与跨对象永久清理候选实现。
- 增加 MySQL M3-B 前置测试基线同步裁决，授权将三条 M2-A 关联永久清理旧拒绝预期更新为已冻结的跨对象清理验证。
- 完成 MySQL M3-B 正式封板，授权 M3-C 仅验证完整方法生命周期的 BackupData 等价、引用校验与原子恢复。
- 完成 MySQL M3 总体稳定审阅与最终封板，冻结候选 Repository 验证边界并要求后续运行时接入另行立项。
- 增加 MySQL M4 架构冻结与分段实施任务书，限定完整 ReviewWorkflow 候选实现的跨对象事务、重试边界与验证门。
- 完成 MySQL M4-A 正式封板，授权 M4-B 在单一事务内验证方法形成、验证与修订复盘路径。
- 完成 MySQL M4-B 正式封板，授权 M4-C 验证既有派生事项、derived_from_review ItemLink 与完整复盘数据的 BackupData 等价。
- 完成 MySQL M4 总体稳定审阅与最终封板，冻结候选 ReviewWorkflow 验证边界并要求运行时接入另行立项。
- 增加 MySQL M5 架构冻结与分段实施任务书，限定 loopback API、前端单写准备、未知提交结果与运行验证边界。
- 增加 M5-A 架构稳定审阅与 Search 一致性读模型修复裁决，冻结单连接一致性快照与定向回归要求。
- 完成 M5-A Search 一致性修复复审，要求补齐冻结 methods 与 review detail 路由的 API Contract 验收证据。
- 完成 M5-A 最终架构复审，授权 QA 对冻结只读 API 路由与一致性读模型进行定向复验。
- 增加 MySQL M3-B 方法应用读写、方法墓碑与跨对象永久清理候选 Repository 及真实 MySQL 集成测试。
- 修复 MySQL M2-A 永久清理测试仍断言旧安全拒绝、未覆盖 M3-B 跨对象清理结果的问题。
- 增加 MySQL M3-C 完整方法生命周期 BackupData 等价、引用校验、原子恢复与 system_metadata 隔离真实集成测试。
- 增加 MySQL M4-A 最小复盘原子闭环候选 Workflow Repository 及真实 MySQL 并发、失败回滚验证。

- 增加 MySQL M4-B 在单一事务内完成复盘、方法形成、验证与修订的候选 Workflow 实现及真实 MySQL 原子回滚、并发验证。

- 增加 MySQL M4-C 派生事项、derived_from_review ItemLink 与完整 M4 BackupData 闭环候选实现及真实 MySQL 原子回滚、并发验证。

- 增加 MySQL M5-A 候选只读 API 运行组合、Search / Dashboard 可信读模型及 API 错误边界集成测试。
- 修复 MySQL Search 跨连接读取可能混合多个事务快照的问题。
- 增加 MySQL M5-A `/api/v1/methods` 与 `/api/v1/reviews/:id` 的只读路由 Contract、回收站隔离及 MySQL 不可用错误映射测试。
- 增加 MySQL M5-B1 候选 loopback API 的既有 Application 读写路由矩阵、传输边界与真实 MySQL 集成测试。
- 修复 M5-B1 未分类 API 异常误报 MySQL 不可用及 method-source-displays URL 长度未受限的问题。
- 修复 M5-B1 已知 API 路径使用未允许 HTTP 方法时错误返回 404 而非 405 的问题。
- 增加 M5-B1 已知路径错误方法与未知路径的完整 HTTP 错误 DTO 回归断言。
- 修复 H5 使用局域网工作台 Origin 时被 API CORS 拒绝的问题，限制为两个精确本地 Origin。
- 修复补充说明写入结果未确认时仍显示“重试”入口的问题，改为仅刷新真实数据确认。
- 修复 unknown-outcome 自动读取真实数据的问题，改为仅提示并由用户主动刷新确认。

## 未完成项

- H5 浏览器 DevTools 的 IndexedDB 业务写入与 Network 请求人工证据待 QA 验收。
