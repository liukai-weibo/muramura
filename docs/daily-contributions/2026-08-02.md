# 2026-08-02 每日贡献

## 增加项

1. 将业务错误码按域过碎拆分收回 `packages/contracts/src/errors/business.ts`，目录仅保留 `business.ts`、`api.ts` 与 `index.ts`；各域错误码类型名与聚合联合导出面不变。
2. 将共享 Contracts 按账户权限、事项与探索主线、复盘与方法、备份及组合读模型整理为五个内聚模块，保留统一根入口和既有公开契约。
3. 统一认证、平台管理、备份归属、初始归属与既有业务失败为单一 `BusinessError` 契约，分类只能由穷尽中央码表派生，认证参数格式失败与认证校验失败使用明确不同的码；补齐对外错误码白名单，删除平行 Error 类、层级私有码与手动翻译表，API 业务失败只保留一条映射路径。
4. 将 Application 单文件按账户与权限、备份、事项与探索主线、复盘与方法、读模型、回收站六个职责拆分，根入口保留统一公共导出与既有导入方式。
5. 将重复的主线名称规范化收敛为唯一 Domain 函数，并将备份解析校验、Dashboard 报告计算和回收站排序提取为可直接测试的规则函数；Application Service 保留为依赖注入与流程编排外壳。
6. 将日常 API 完整切换到 Hono OpenAPIHono：补齐 Cookie 会话、auth/admin、按登录用户隔离的 scoped services，业务路由迁至 `@hono/zod-openapi`，并挂载 `/openapi.json` 与 Scalar `/docs`；`createApiServer` 改为 `@hono/node-server` 薄适配。
7. 导出 Hono RPC 用 `AppType`（`@knowledge-base/api/rpc`）：路由改为链式 `.openapi` / `.route` 组装以保留类型；`requireJson` 收入各 `createRoute.middleware`。

## 修复项

1. 事项探索主线上下文在无关联时按既有语义返回 404；若干 owner 隔离路径上的纯 `Error` 改为 `BusinessError`，使跨用户访问稳定映射为 404。
2. 管理用户列表查询恢复严格 page/query 校验（未知键、重复键、非正整数页码等拒绝）。

## 未完成

（无）
