# 2026-08-23

## 增加项
- 方法创建行动入口文案改为“加入todo”。
- 首页暂时隐藏“系统心法与行动指南”区域。
- 新增情绪模块：月度情绪日历 + 卡片网格，五档温和情绪分级，支持按日记录、编辑、删除与等级筛选。
- 情绪模块接入主导航（瑜伽 / 情绪），懒加载挂载并保留模块状态。
- 新增 mood_entries 表与 Schema 21：数据层、契约、Application 服务与 Hono API（/api/v1/mood-entries，含 GET/POST/PUT/DELETE）。
- JSON 备份升级到版本 6：包含 moodEntries 导出与恢复，恢复时校验情绪记录字段。
- 增加 4 组自动测试：情绪等级加权、日历网格、Application 校验、MySQL 仓储集成。
- 日常 knowledge_base 按授权顺序执行迁移 019–021，实测 Schema 21，情绪模块可验收。

## 修复项
- 修复方法工作台右侧详情无法滚动到内容底部的问题。
- 修复迁移 021 TEXT 列带 DEFAULT 在 MySQL 8 报错的问题（移除 tags 默认值后重跑成功）。
- 修复情绪日历在宽屏下被 aspect-ratio 撑爆页面（日历高度 1137px → 442px），卡片网格恢复可见与滚动。
- 修复新建情绪弹窗标签行布局：Taro Button 默认样式把「添加」按钮撑满整行、标签输入框被挤压到不可见；改为与页面一致的 View 按钮 + 输入框占满剩余宽度。
- 情绪模块迭代：移除标签输入与标签展示（数据层 tags 字段保留兼容）；新建已选日期预填（支持按日期新建）；日历默认年视图（12 个月份小历，点击某天进入月视图），支持年/月视图切换与年份导航。
- 情绪模块再迭代：日历仅显示当月日期（跨月位置空占位、不可点）；新建弹窗日期只读（不可手改）；默认月视图且刷新恢复默认；年视图点击仅选中当日（不跳月，再点取消）；等级筛选下拉可用（按 1–5 级过滤日历与卡片）。
- 情绪模块色板重设计：1–5 级由暖灰/粉色系改为 紫 / 蓝紫 / 暖白 / 绿 / 暖黄（紫=不愉快，暖黄=愉快），浅深两套主题同步更新（cream-ui-theme.scss 变量 + mood-levels.ts 常量）。
- 首页新增情绪渐变卡（提示语「今天有感到开心吗？」、紫→暖黄对角渐变 + 光斑质感，深浅主题适配）；点击卡片任意位置直接进入灵感todo·情绪并自动打开今日新建弹窗（日期只读=今天）。
- 情绪模块从主导航独立项迁移为 灵感todo 第四个子 Tab（行动/长期探索/方法/情绪），保留 module-retained 状态；PrimaryModule 移除 mood，WorkbenchTab 增加 mood。
- 修复：首页情绪卡改为点击直接弹出新建弹窗（不再跳转 workbench）；新建弹窗日期弱化为 12px 提示小字；取消/保存按钮等宽等高并补齐悬停反馈；首页情绪卡渐变由四色收敛为两色（浅色 暖黄→绿，深色 深暖黄→深绿）。
- 新增三餐模块：首页「今天吃了吗」渐变卡（点击弹窗不跳转）；灵感todo 第五个子 Tab「三餐」（月视图 + 年表格，早/午/晚按 feeling 五档着色）；记录/编辑今日三餐弹窗（早餐/午餐/晚餐 + 感受 1–5）。
- 新增 meal_entries 表与 Migration 022（每 owner/日期/餐次一行 upsert）、Contracts/Application/Hono API（GET /api/v1/meal-entries 范围读取 + PUT /api/v1/meal-entries/{entryDate} 整日保存）与 Backup V7（含 mealEntries 导出/恢复）。
- 增加 3 组自动测试：三餐 Application 校验、日历网格工具、MySQL 仓储集成（upsert 语义 + 备份往返）。
- 日常 knowledge_base 执行 Migration 022，实测 Schema 22，三餐模块可验收（迁移前已做逻辑备份 schema21-before-022-2026-08-23-17-07-51.sql）。
- 首页情绪卡与三餐卡由各自占一整行改为同一行左右各占一半的并排布局，整体压缩高度与字号（标题 24→20px、描述 13→12px、内边距与最小高度收窄），≤720px 自动回退单列堆叠；两卡点击弹窗等交互保持不变。
- 三餐年视图由 12×31 年表格改为情绪式 12 个迷你月历（早/午/晚按 feeling 五档着色的小圆点），视图切换文案 年表格 → 年视图；点击某天仍弹出三餐弹窗。
- 情绪年、月视图同步三餐的背景与交互观感：月历日期格圆角 8→12px、悬停补边框高亮（`border-color` + 缩放波纹），保留整格情绪色热力图呈现（经用户确认不换“浅色卡+圆点”）。
- 取消右下角“速记悬浮球”与左下角“速记悬浮球”开关按钮：整栈移除 QuickNoteFab（组件、index.tsx 装配：imports/state/挂载/导航开关/桌面快捷键监听、display-effect-preference 的可见性读写、cream-ui-theme.scss 全部 quick-note 样式、Rust main.rs 的 CTRL+SHIFT+M 速记触发分支），保留手记数据模型与既有速记入口（首页“快速记录”、移动端手记）不受影响。
- 三餐记录弹窗视觉升级（方案一全部 + 方案二无需新资源项）：卡片 16→24px 圆角、内边距加大、头部加极简餐具线稿图标并生活化日期（8 月 24 日 周一）；餐段由灰框改为留白 + 极细分隔线；感受选择器由纯色圆点改为“短文字 + 柔色底”胶囊（不太舒服/平平淡淡/刚刚好/吃得开心/超满足，默认选中刚刚好，选中加 2px 柔粉描边）；输入框占位改为“吃了什么？（可简写）”；底部新增当日三餐平均星级小反馈（纯展示，按三餐感受均值折算）；新增柔性弹出动效。年视图图例文案同步更新，日历圆点色保持不变以保证可见。
- 三餐弹窗头部图标由内联 SVG 换成固定 emoji 🍚；三个餐段由“留白 + 分隔线”升级为浅米色小卡片（16px 圆角），每餐段标题前加 emoji 图标（早餐🥐/午餐🍱/晚餐🍲）。
- 情绪记录弹窗视觉升级：标题 16px/750 降为 14px/600 并左置；头部加 emoji 图标（💗，软爱心）+ 生活化日期（8 月 24 日 周一）；删除“（必填）”写死、事件区与对策区加小标签（发生了什么 / 今天状态怎么样？ / 补充感受与对策（可选））与温柔占位（简单记录一件小事～ / 当下的感受，下次怎么应对 / 延续）；感受选择由 5 个无文字纯色圆点改为“文字 + 柔色底”胶囊并默认选中平静如常；对策区新增快捷标签（小确幸 / 内耗 / 日常，一键填入）；报错文案软化为“先写点什么再保存吧～”；支持 Ctrl+Enter 快速保存（捕获阶段监听，规避 Taro Textarea 冒泡阶段重派发丢失 key）；弹窗新增弹性弹出动效。日历热力图 moodLevelColors 保持不变，仅弹窗用新 moodLevelPillColors。
- 情绪记录弹窗移除“Ctrl + Enter 快速保存”底部提示文案（快捷键功能保留）；情绪与三餐五档颜色统一为同一套暖奶油低饱和色板（弹窗胶囊 / 日历单日格子 / 日历圆点 / 图例共用），同步更新 mood-levels.ts 与 meal-levels.ts 常量、弹窗引用与 cream-ui-theme.scss 的 --mood-1..5 浅深主题变量。
- 情绪记录弹窗移除“+小确幸 / +内耗 / +日常”三个快捷标签（删除 QUICK_TAGS 常量、appendTag 函数、mood-quick-tags 区块及 mood-page.scss 中对应样式），对策区输入框保留。

## 修复项
- 修复情绪年视图迷你月历星期行高度泄漏基类 44px（标签仅 14px）导致标签下方约 28px 空白的问题：`.mood-year-weekday-row` 由 `grid-template-rows: none` 改为 `grid-auto-rows: 14px`。
- 修复情绪/三餐年视图迷你月历悬停时备注与月份标题被相邻（后一个）月卡遮蔽：改为悬停月卡用 `:has()` 提升层级（`.mood-year-month-card:has(.mood-calendar-cell:hover)` 与 `.meals-year-month-card:has(.meals-year-cell:hover)` 置 `position:relative; z-index:5`），使备注/月份标题绘制于相邻月卡之上。
- 修复情绪月/年视图日期格悬停放大（`scale(1.08)`）被相邻日格遮住：悬停格补 `position:relative; z-index:2`，置于兄弟日格之上（同网格 stacking context）。
- 修复情绪年视图备注由上一轮“可换行 + `max-width:180px`”被折成约 13 行的问题：恢复 `white-space: nowrap` 紧凑单/两行；覆盖仍由悬停月卡 `:has()` 层级保障，不再引入换行。
- 修复 MySQL 三餐集成测试在 5s 默认超时下随机临时库全量迁移被误杀的问题（改为 30s 超时）。
- 修复 Backup V7 引入后 `hono-route-table` 冻结路由表未同步新增三餐路由、`sprint-three` 用已支持的 version 4 断言「不支持的备份版本」的两处回归。

## 未完成项
