# 2026-07-30 每日贡献

## 增加项

- 将本机隔离 UAT 数据库初始化为包含账户、会话及业务 owner 列的 Schema 5 空库。
- 将当前登录用户作用域接入事项、方法、复盘、探索主线、回收站、搜索和 Dashboard 的 MySQL 读写链路。
- 增加双用户全业务隔离、跨用户资源统一不可见和十个业务集合归属校验场景。
- 增加显式目标账户的初始个人数据归属命令、按当前用户的 Backup V1/V2/V3 隔离能力及账户登录门端到端 QA 场景。
- 增加固定 `member`、`platform_admin` 角色、脱敏安全审计事件及受控初始管理员 CLI。
- 增加管理员用户分页搜索、角色调整、会话撤销及 H5 管理入口的冻结能力。
- 将 Hono 的十组 v1 路由收拢到单一版本前缀挂载点，并增加完整路由表回归保护。
- 更新代码地图、业务错误分层、Domain / Application / Repository / API / 页面边界及 Contracts 分区说明。

## 修复项

- 将本机启动健康门由 Schema 4 修正为 Schema 5，并修复停止脚本的父进程树终止问题。
- 修复认证夹具、无用户作用域直测、超长随机标题、跨用户 Backup 恢复和认证上下文竞态问题。
- 修复登录、注册或退出结果未知时可能丢失草稿或误判认证结果的问题。
- 修复平台管理员最后一人保护中的不可达公开错误码。
- 修复 main 与 ty 合并时账户 owner scope、结构化业务错误和探索主线批量查询之间的兼容冲突。
- 修正 HTTP 状态码依赖中文错误文案匹配的过期说明及各层文件规模数据。

## 未完成

- Hono 尚未接入当前 API 主入口，运行时仍使用既有原生实现。
- 独立 Docker 宿主机全链路 QA、通用业务规则盘点与 Domain 最小提取切片仍待后续立项。
