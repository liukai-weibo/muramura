# 探索主线 V1 S3：六岗位新对话统一交接与协作章程

> 创建日期：2026-07-25
>
> 责任岗：产品经理
>
> 用途：供产品经理、产品设计师、架构师、数据 / Application / Repository 工程师、前端工程师、QA 六个独立新对话作为唯一启动交接。
>
> 当前总裁决：**探索主线 V1 的 S1、S2 已封板；S3 已完成产品范围冻结，但尚未完成 S3 架构冻结与产品书面编码授权。当前不得进行任何 S3 业务编码。**

---

## 0. 所有人先读：唯一事实、当前门槛与工作方式

### 0.1 强制先读文档

每个新对话启动后的第一步，必须阅读下列文件，且以第一份为最高优先级：

1. `docs/product/当前运行事实.md`：唯一运行路径、数据源、端口、阶段、禁止事项锚点；
2. `AGENTS.md`：六岗位职责、越权边界、流转格式、每日贡献同步规则；
3. `docs/product/探索主线-V1-S3-可信运行时接入产品范围冻结.md`：S3 的产品边界和验收标准；
4. 与本岗位相关的已封板文档（见第 3 节）。

历史任务书、聊天转述、旧阶段文档均不能覆盖 `当前运行事实.md`。如果发现文档互相矛盾，必须停止推断，记录冲突，转产品经理裁决；运行路径、运行数据库、阶段或禁止项发生真实变更时，必须先更新 `当前运行事实.md`，再实施或流转。

### 0.2 当前系统事实

当前正式运行路径：

```text
H5
→ /api 开发代理
→ loopback Node API（127.0.0.1:32146）
→ Application
→ MySQL
```

数据库与隔离：

```text
单一 MySQL Docker 容器：127.0.0.1:3306
├─ knowledge_base      日常 H5 / API 数据库
└─ knowledge_base_uat  隔离 UAT / 破坏性测试数据库
```

固定入口：

```text
H5：仅 http://127.0.0.1:10086
API：仅 http://127.0.0.1:32146
H5 /api 与 /health：代理至 127.0.0.1:32146
```

数据主权边界：

```text
MySQL      = 当前 H5 唯一运行主库
IndexedDB  = 不参与当前 H5 运行时读写；仅保留旧本地资产
SQLite     = 实验 / 测试资产
```

进入任何运行时测试前，执行者必须独立验证：

```text
GET http://127.0.0.1:32146/health
```

并记录实际 `database` 与 `schemaVersion`。日常 API 只允许显式加载 `.env` 并确认 `database = knowledge_base`；UAT / 破坏性操作只允许显式加载 `.env.uat` 并确认 `database = knowledge_base_uat`。绝不根据端口、终端窗口名称或历史记录猜测连到了哪个库。

### 0.3 当前阶段门

```text
探索主线 V1
├─ S1：Schema 004 与基础 Contracts                 已封板
├─ S2：Repository / Application 与原子工作流       已封板
└─ S3：Backup V3 → API → H5 真实接入 → 浏览器 UAT  产品范围已冻结，尚待架构冻结与产品授权
```

S1/S2 封板不代表探索主线 V1 已完成。S3 未经架构师输出最小任务书、产品经理书面授权前：

```text
禁止 Backup V3 编码
禁止 API 路由、DTO 映射或 HTTP 编码
禁止 H5 真实 API 接入
禁止解除原型控件禁用
禁止真实探索主线读写
禁止以“前后端并行”为由绕过架构冻结
```

当前允许的唯一并行工作：

```text
产品：澄清范围、裁决问题、维护冻结与验收文件
设计：仅审查已冻结原型与交互说明，不新增业务能力
架构：产出 S3 最小架构任务书
数据 / Application / Repository：只读预研、风险清单；不得编码
前端：只读预研、组件盘点；不得改造 fixture 为 API
QA：测试设计、用例与风险矩阵；不得执行破坏性运行库操作
```

### 0.4 永久禁止项

```text
IndexedDB / MySQL 双写、同步、回填、fallback 或合并展示
浏览器直连 MySQL
真实 IndexedDB 历史数据迁移（除非用户重新明确提出）
远程 / 公网 API、0.0.0.0、反向代理、隧道
多用户、登录、JWT、Cookie、RBAC、协作
多主线关联、自动关联、自动归类
进度、完成率、事项计数、计划、日期、提醒、子任务、看板
AI 推荐、自动下一步
主线物理清理、墓碑、级联删除、自动解除关联
```

### 0.5 Zed 稳定性工作约束

当前已确认 Zed Agent 在超长、高密度编辑历史线程中，遇到流式 `edit_file` 失败、中断或 `old_text` 不匹配时，可能发生请求期主进程内存异常放大。此约束适用于全部岗位：

```text
每个新对话仅负责一个明确、可验收的工作切片。
完成一个切片，或约 30–50 次工具调用后，必须输出摘要并新建会话。
遇到 edit_file 不匹配、流式工具输入中断或 Zed 内存持续上升：立即 Cancel；不在同一线程硬重试编辑。
禁止用一条会话跨越产品 → 架构 → 实现 → 调试 → QA → 验收。
不上传项目私密数据、完整 threads.db、.env 或数据库备份到上游问题单。
```

Windows `nul` / `NUL` Git checkpoint 报错为独立兼容性问题，可能加剧压力，但目前不是内存暴涨的唯一充分原因。不得为了处理该问题执行 `git clean -fdx`、`git reset --hard` 或清理 `mysql-data/`。

---

## 1. S3 的唯一目标与用户闭环

S3 不创造任何新业务语义，只把 S1/S2 已封板的可信能力以安全方式接入当前 H5 运行闭环：

```text
Backup V3
→ 冻结 API 路由、DTO、稳定错误语义
→ H5 从 fixture 原型切换为真实 API 结构化读模型
→ knowledge_base_uat 浏览器级 UAT
→ QA 与产品最终验收
```

目标用户闭环：

1. 用户创建、改名、软删除、恢复探索主线；
2. 用户快速捕获事项时可明确选择已有主线，或暂存一个新主线名称；
3. 仅在用户提交事项时，才原子创建新主线（如选择 new）、事项、初始状态事件和关联；取消选择零写入；
4. 用户可查看主线的当前关联事项、探索历史、已放弃记录与已删除主线历史；
5. 用户可在事项详情查看、调整或移除仍允许调整的关联；
6. 已删除主线与不可用关联须诚实降级，不伪装成无关联；
7. 用户可通过 Backup V3 安全导出、恢复主线和关联；
8. API、网络或提交结果未知时，页面不伪造成功、不自动重试，只保留草稿并重新读取真实数据确认。

---

## 2. 已冻结的业务事实（不得重议、改写或扩张）

### 2.1 主线与事项关系

```text
一个 Item 只能关联 0 或 1 条 Exploration Track。
关联只能由用户明确创建、调整或移除。
前端、后端均不得依据标题、时间、文案、状态、计数、相似度、历史或缓存自动推断关联。
```

### 2.2 主线生命周期与名称

```text
主线可创建、改名、软删除、恢复。
删除为软删除：不删除、不解除、不改写关联事项、复盘和历史事实。
名称使用 NFKC → trim → lower-case，Unicode code point 长度 1..80。
名称在全部记录全生命周期唯一；软删除主线仍占用名称。
不做物理永久清理、墓碑或 purge。
```

### 2.3 可信降级

```text
no-association：事项没有关联主线。
available：关联的活跃主线可用。
track-deleted：关联主线已软删除，保留真实 Track 和 deletedAt；只读解释。
unavailable：关联 ID 非空但结构化目标不可用；必须保留原 trackId，不能降级成无关联，不能自动清空、替代或修复。
```

对 `track-deleted` 或 `unavailable`：改派、移除均须稳定拒绝且零副作用。

### 2.4 原子性与 unknown-outcome

```text
existing Track：锁定活跃主线 → 创建 Item → 初始 ItemStatusEvent → 写关联
new Track：创建 Track → 创建 Item → 初始 ItemStatusEvent → 写关联
```

两条路径均须在同一 MySQL transaction 中全有或全无。S3 继续遵守：

```text
无请求幂等键
无自动写入重试
超时、断线、页面关闭或响应丢失时，不得从表单或本地状态推断成功 / 失败
只能重新读取真实 API 数据确认
```

### 2.5 API 冻结方向

S3 只能实现既有产品范围中的下列路由集合，最终 DTO、body 限制、参数与错误映射须由 S3 架构任务书一次性冻结：

```text
GET    /api/v1/exploration-tracks
GET    /api/v1/exploration-tracks/selectable
GET    /api/v1/exploration-tracks/deleted
GET    /api/v1/exploration-tracks/:id/history
POST   /api/v1/exploration-tracks
PATCH  /api/v1/exploration-tracks/:id
DELETE /api/v1/exploration-tracks/:id
POST   /api/v1/exploration-tracks/:id/restore

POST   /api/v1/items（仅扩展 explorationTrack 的显式可选选择）
GET    /api/v1/items/:id/exploration-track
PUT    /api/v1/items/:id/exploration-track
DELETE /api/v1/items/:id/exploration-track
GET    /api/v1/items?status=<冻结状态>&explorationTrackId=<id>
```

固定错误语义方向：

```text
400 VALIDATION_FAILED
404 NOT_FOUND
409 CONFLICT
503 MYSQL_SCHEMA_NOT_READY / MYSQL_UNAVAILABLE
500 INTERNAL_ERROR
保留既有 413、415、405 与 NOT_FOUND_ROUTE
所有失败响应脱敏并含 requestId
```

### 2.6 Backup V3 冻结方向

```text
新增 BackupDocumentV3 / explorationTracks。
V1 / V2 导入：主线与事项主线关联安全降级为空；不失败、不猜测、不生成关联。
V3 导出：保留主线、软删除状态与 Item explorationTrackId。
V3 不可信输入：断裂引用、非法 ID / 名称 / 规范名 / 时间、名称冲突或任一既有集合不可信时，事务前整体拒绝；十个业务集合零写入。
replaceData()：十个业务集合须同一 MySQL DML transaction，末端失败整体回滚。
system_metadata：不导出、不清理、不恢复、不覆盖。
```

---

## 3. 必读的现有归档材料

### 所有岗位共读

```text
docs/product/当前运行事实.md
docs/product/探索主线-V1-S3-可信运行时接入产品范围冻结.md
docs/product/探索主线-V1-产品评审结论.md
docs/product/探索主线-V1-API契约产品确认结论.md
docs/design/探索主线-V1-交互与视觉设计冻结.md
docs/design/探索主线-V1-设计冻结正式验收结论.md
docs/product/探索主线-V1-S1-Schema004与基础Contracts封板结论.md
docs/product/探索主线-V1-S2-RepositoryApplication与原子工作流封板结论.md
```

### 架构 / 数据 / 前端 / QA 强制补读

```text
docs/architecture/探索主线-V1-数据ApplicationRepository实施任务书-封板后生效.md
docs/architecture/探索主线-V1-API路由矩阵与DTO补充冻结.md
docs/architecture/探索主线-V1-S2-RepositoryApplication与原子工作流架构任务书.md
docs/architecture/探索主线-V1-S2总体稳定复审结论与封板建议.md
```

这些文档是已封板事实与候选技术依据；若与 `当前运行事实.md` 或本章程的阶段门冲突，必须以当前运行事实和本章程“未授权 S3 编码”为准。

---

## 4. 六岗位独立新对话：首轮任务、边界与交付

## 4.1 产品经理

### 当前职责

产品经理负责 S3 的范围守门和跨岗裁决，不写业务代码、页面样式、Schema、Migration、接口或部署脚本。

### 首轮任务

1. 确认本章程与 `当前运行事实.md` 一致；若发现 API 健康检查历史记录、schemaVersion 或当前 API 实际数据库的表述无法同时成立，先要求执行者进行只读健康检查并更新唯一事实；
2. 审核架构师提交的 S3 最小架构任务书是否完整覆盖 Backup V3、API、H5、unknown-outcome、隔离和测试门；
3. 对任务书做“可编码 / 不可编码”书面裁决；
4. 编码完成后，根据 QA 浏览器 UAT 与运行库隔离结果执行最终验收、归档和封板。

### 不得做

```text
提前授权 S3 编码
把 S1/S2 的既有事实重新设计为新需求
以构建通过替代浏览器 UAT
把前端原型展示误认为真实交付
允许任何岗位因“实现方便”新增字段、能力或业务语义
```

### 期望输出

```text
【结论】
【当前阶段与编码门判断】
【确认的范围】
【不允许项】
【需架构补齐的问题】
【验收标准】
【下一责任岗】
【是否允许写代码】
```

## 4.2 产品设计师（UX / UI）

### 当前职责

设计已完成探索主线 V1 的交互与视觉冻结。设计师当前不应重新发散新功能，而应为 S3 的真实接入做一致性审查：确保 loading、空态、错误、unknown-outcome、已删除和 unavailable 降级与已冻结交互一致。

### 首轮任务

1. 复核 `docs/design/探索主线-V1-交互与视觉设计冻结.md` 与 S3 产品范围是否一致；
2. 将所有“原型禁用控件”在 S3 真实接入后的状态转换规则列成一页实施核对清单：何时可用、何时 loading、何时保留草稿、何时只读、何时稳定报错；
3. 明确文本截断规则：补充说明区域初始只显示一行视觉高度，但预留最多两行的内容显示空间；超过两行必须截断并显示省略号，不能通过无限增高挤压列表；
4. 对主线列表 / 详情、快速捕获、事项关联、已删除主线、unavailable 关联与小屏返回路径提供人工验收要点；
5. 如发现新问题，必须标记为“需产品裁决”，不得自行把它转成字段、对象、API 或新交互能力。

### 不得做

```text
新增主线业务对象、字段、状态、进度、日期、提醒或自动化
更改 0..1 关联、软删除或 unavailable 语义
为填补布局留白增加低决策价值信息
把设计稿改写为技术实现任务书
```

### 期望输出

```text
【推荐方案】
【S3 接入状态对照表】
【桌面端与小屏人工验收要点】
【截断、滚动、草稿保留规则】
【对现有业务规则的影响：无 / 需产品确认】
【需要产品经理裁决的问题】
【交付给前端的实施说明】
```

## 4.3 架构师

### 当前职责

架构师是当前唯一可以推动阶段门的人：产出 S3 的一次性、最小且可执行的架构任务书。此阶段不写业务代码，不得把 S3 拆成“先偷跑 API”或“前端先接真实数据”的隐性授权。

### 首轮任务：S3 最小架构任务书

任务书必须明确：

1. 实施切片及其依赖顺序。建议以可回归、可封板的最小切片组织，而不是按个人或目录随意拆分；
2. Backup V3 Contracts、导出 / 恢复校验、十集合事务、`system_metadata` 隔离、V1/V2 降级、V3 断裂引用整体拒绝；
3. API route → Application → DTO → HTTP 错误映射，body / query 限制、路由冲突、稳定排序、`requestId` 和脱敏；
4. unknown-outcome 的端到端保护：前端保留草稿、无自动重试、以真实读取确认；
5. H5 fixture → API Client / Adapter 的替换边界，页面状态、取消请求和竞态保护；
6. MySQL 日常库、UAT 库、随机临时测试库的严格职责，迁移、破坏性测试、快照和清理的安全步骤；
7. 自动化测试矩阵、浏览器 UAT 门、回归范围及每一项的可信数据源；
8. 允许修改的层和文件范围；
9. 明确禁止修改的既有状态机、复盘、方法、回收站、ItemLink 语义以及禁止双写 / fallback 的保护；
10. 编码前置检查、实施后架构复审输入与产品验收交接清单。

### 特别技术约束

```text
H5 只能调用 API Client / Adapter，不能操作 MySQL、Repository、IndexedDB 或 SQLite。
前端不能从历史、缓存、标题、时间或文案推断关联。
API 只能调用 Application；不能绕过 Application 直接拼底层表。
Backup 恢复的拒绝验证必须发生在任何业务集合写入前。
所有 MySQL 集成测试只使用随机临时 database；不得触及 knowledge_base 或 knowledge_base_uat。
真实浏览器 UAT 的写入只能在显式 .env.uat 与健康检查确认后进行。
```

### 不得做

```text
擅自实施任何 S3 代码
新增 Schema、Migration、字段或备份外的业务能力
用 IndexedDB fallback 规避 API / MySQL 失败
以“技术优化”更改产品冻结语义
```

### 期望输出

```text
【技术结论：可行 / 有条件可行 / 不建议】
【可复用现有能力】
【S3 最小新增能力与实施切片】
【数据可信边界】
【Backup / API / H5 接入方案】
【是否涉及 Schema / Migration / 备份】
【运行库隔离与风险保护策略】
【允许修改的文件或层】
【自动化测试与 UAT 建议】
【交付给产品经理的授权条件】
```

## 4.4 数据 / Application / Repository 工程师

### 当前职责

当前仅可做只读预研与实施准备，不得编码。产品授权后，才按架构任务书实现 Backup V3、Application 编排、Repository 可信读写和 API 所需的结构化服务能力；具体 API / HTTP 层归属按架构任务书确定，不能自行接管前端。

### 首轮任务（未授权状态）

1. 阅读 S1/S2 Contracts、Repository / Application 任务书及现有 Backup 实现；
2. 输出“现有能力 → S3 需求”的只读差距表；
3. 识别 Backup V3 验证顺序、transaction 边界、错误分层、测试隔离和已有 Contracts 可复用项；
4. 只报告必须由架构 / 产品裁决的问题，不修改任何源码、测试、配置、数据或 migration。

### 获授权后的实施边界

```text
只可修改架构任务书明确允许的 Contracts、Application、Repository、Backup、API 层与对应测试。
不得修改 004 migration 或对运行库执行 DDL / DML，除非任务书和执行窗口明确授权。
不得改状态机、复盘、方法、回收站、ItemLink 或既有备份可信语义。
不得双写、同步、回填、fallback 或浏览器直连 MySQL。
```

### 期望输出

```text
【修改文件】
【新增或修改的契约】
【业务语义说明】
【transaction 与可信数据来源】
【数据降级与异常处理】
【自动化测试场景】
【typecheck / test / build 结果】
【确认未修改的边界】
【待确认问题】
```

## 4.5 前端工程师

### 当前职责

前端已经获准存在纯展示、fixture 驱动的探索主线原型；S3 前不得把原型改为真实运行能力。当前只可盘点接入点、组件状态和测试风险，不得写业务接入代码。

### 首轮任务（未授权状态）

1. 只读梳理探索主线原型涉及的页面、组件、fixture、状态和禁用控件；
2. 输出 fixture → API Client / Adapter 的候选替换清单，但不做任何替换；
3. 根据设计冻结文档列出每个交互的 loading、空态、失败、unknown-outcome、已删除、unavailable、草稿保护和小屏要求；
4. 指出会导致异步竞态、用户输入覆盖或错误定位的风险点；
5. 不连接 API，不读写 MySQL、IndexedDB、SQLite、localStorage 或其他浏览器持久化。

### 获授权后的实施边界

```text
只能调用冻结 API 和结构化 DTO；不得直连 MySQL / Repository。
不从前端过滤历史推断当前关联事项；只显示 API 返回的结构化读模型。
unknown-outcome：保留用户输入与选择；不自动重试；只重读 API 确认真相。
旧请求、默认值、列表刷新不得覆盖当前草稿或明确选择。
控件只在 API 与状态机允许时解除禁用；失败时不伪造成功。
补充说明：初始单行视觉高度，最多扩展 / 展示两行，超出两行以省略号截断；具体 CSS 和可访问性实现按设计核对清单执行，不改变业务字段语义。
```

### 不得做

```text
提前连接真实 API
用 fixture 或本地状态伪造写入成功
自行新增 API、字段、业务关系、筛选或状态
为了页面方便隐藏 deleted / unavailable 的真实差异
```

### 期望输出

```text
【修改文件】
【实现的交互与状态说明】
【fixture → API 替换说明】
【异步竞态与用户操作保护】
【调用的 Application / API 契约】
【未实现项与原因】
【验证命令及结果】
【人工验收步骤】
【截图或录屏说明】
```

## 4.6 QA

### 当前职责

QA 当前应先建立 S3 可执行测试设计与证据规范；架构 / 产品授权前不得进行会污染运行库的迁移、恢复、清库、破坏性测试或伪造验收。

### 首轮任务

1. 基于 S3 产品范围与现有设计冻结输出测试矩阵，覆盖：
   - 主线生命周期；
   - existing / new 关联的原子创建及取消零写入；
   - 关联调整与移除；
   - `track-deleted` / `unavailable` 的展示、拒绝与零副作用；
   - 当前关联事项、状态定位、历史和已放弃收纳；
   - Backup V1/V2 安全降级、V3 关系保留、V3 非法输入整体拒绝；
   - API 错误映射、MySQL 不可用、unknown-outcome、草稿保护；
   - H5 小屏 / 桌面、两行截断和可用性；
   - 无 IndexedDB fallback、日常库隔离、UAT 可追溯。
2. 为每一场破坏性测试预先定义：目标库、启动命令、健康检查、前后快照、清理方式、证据和恢复条件；
3. 明确 P0–P3 分级与阻断规则；
4. 等待实施完成再执行测试，绝不以自动化测试通过代替浏览器级 UAT。

### 关键隔离规则

```text
任何 UAT / 清库 / restore / 破坏性操作前：确认 API 健康检查为 knowledge_base_uat。
日常 knowledge_base 必须有前后快照或等价稳定证据，证明未被触及。
不得删除 mysql-data。
不得将 API 端口或 H5 页面能打开当作数据库隔离已验证。
```

### 期望输出

```text
【测试范围】
【环境与版本】
【通过场景】
【失败场景】
【问题清单：级别 / 复现步骤 / 实际结果 / 期望结果】
【运行库隔离证据】
【回归风险】
【是否建议验收：通过 / 有条件通过 / 不通过】
```

---

## 5. 允许的流转顺序

当前正确路径：

```text
产品经理确认当前阶段与范围
→ 架构师输出 S3 最小架构任务书
→ 产品经理书面评审并授权明确切片
→ 数据 / Application / Repository 与前端按任务书在可并行边界实施
→ QA 自动化验证与隔离浏览器 UAT
→ 架构复审
→ 产品经理最终验收、归档与探索主线 V1 封板
```

不得发生的流转：

```text
前端原型 → 直接真实 API 接入
S2 封板 → 自动开始 S3
数据层先写 → 事后补架构任务书
构建通过 → 宣称探索主线 V1 完成
UAT API 正在连接 knowledge_base_uat → 被误当作日常运行完成
```

---

## 6. 工程验证与日常贡献记录

任何获得编码授权的岗位，在每轮完成下列任意工程验证或 H5 人工验收后，必须追加当天的：

```text
docs/daily-contributions/YYYY-MM-DD.md
```

适用验证：

```text
typecheck
test
build:h5
git diff --check
H5 人工验收
```

记录格式仅允许分类：

```text
增加项
修复项
未完成项
```

不要在 daily contribution 中写验证结果、长过程、关键决策、关联记录或测试报告；这些应留在对应任务书、QA 报告、验收记录中。同日只追加，不建重复日期文件。

---

## 7. 新对话启动时可直接复制的最小提示

每个岗位都应先自行读取本文件与规定的当前运行事实。以下提示仅用于启动，不能替代文件内容。

### 产品经理

```text
你是 Knowledge_Base 产品经理。先读 AGENTS.md、docs/product/当前运行事实.md 与根目录 S3-六岗位新对话统一交接与协作章程.md。探索主线 V1 的 S1/S2 已封板，S3 仅产品范围冻结，当前禁止编码。请执行产品守门：核对当前阶段、审阅后续 S3 架构任务书的授权条件；不写任何业务代码。按 AGENTS.md 的产品输出格式回答。
```

### 产品设计师

```text
你是 Knowledge_Base 产品设计师。先读 AGENTS.md、docs/product/当前运行事实.md、docs/design/探索主线-V1-交互与视觉设计冻结.md 与根目录 S3-六岗位新对话统一交接与协作章程.md。当前 S3 禁止编码。请做 S3 真实接入前的设计一致性审查和状态核对清单，不新增业务语义或技术实现。特别确认补充说明初始单行、最多两行、超出省略号的展示规则。按 AGENTS.md 的设计输出格式回答。
```

### 架构师

```text
你是 Knowledge_Base 架构师。先读 AGENTS.md、docs/product/当前运行事实.md、docs/product/探索主线-V1-S3-可信运行时接入产品范围冻结.md 与根目录 S3-六岗位新对话统一交接与协作章程.md。请产出探索主线 V1 S3 的最小架构任务书，覆盖 Backup V3、API、H5 真实接入、unknown-outcome、测试与 UAT 隔离。当前禁止编码；不得新增业务语义。按 AGENTS.md 的架构输出格式回答。
```

### 数据 / Application / Repository 工程师

```text
你是 Knowledge_Base 数据 / Application / Repository 工程师。先读 AGENTS.md、docs/product/当前运行事实.md、相关 S1/S2 架构任务书，以及根目录 S3-六岗位新对话统一交接与协作章程.md。S3 尚未架构冻结和产品授权，当前禁止编码。请仅做只读差距分析：盘点 Backup V3、Application、Repository、API 所需现有能力和风险，不修改源码、测试、数据或配置。按 AGENTS.md 的本地后端输出格式回答。
```

### 前端工程师

```text
你是 Knowledge_Base 前端工程师。先读 AGENTS.md、docs/product/当前运行事实.md、探索主线设计冻结稿、S3 产品范围冻结稿，以及根目录 S3-六岗位新对话统一交接与协作章程.md。S3 当前禁止编码；现有前端仅允许 fixture 驱动纯展示，控件保持禁用。请只读盘点 fixture 到 API Adapter 的接入点、状态机展示、异步竞态和草稿保护风险；不得连接 API 或修改代码。按 AGENTS.md 的前端输出格式回答。
```

### QA

```text
你是 Knowledge_Base QA。先读 AGENTS.md、docs/product/当前运行事实.md、探索主线 V1 S3 产品范围冻结稿，以及根目录 S3-六岗位新对话统一交接与协作章程.md。S3 当前禁止编码和破坏性运行库操作。请建立 S3 测试矩阵、UAT 隔离 SOP、P0-P3 阻断条件和所需证据；不要运行迁移、清库、恢复或写入测试。按 AGENTS.md 的 QA 输出格式回答。
```

---

## 8. 产品经理的下一次裁决条件

只有同时满足下列条件，产品经理才可讨论对 S3 的局部编码授权：

1. 架构师已提交完整 S3 最小任务书；
2. 任务书明确 Backup V3、API、H5、unknown-outcome、测试和 UAT 的顺序、边界及允许修改范围；
3. 无任何新增业务对象、字段、状态、关系、migration 或范围漂移；
4. 产品经理已书面确认可实施的切片、禁止项和验收门；
5. 数据 / 前端 / QA 已明确各自不重叠的写入范围与验证责任；
6. 执行者确认会使用短 Zed 会话，避免长会话编辑造成的稳定性风险。

在此之前，本章程不是编码授权书。
# 当前协作模式（单会话优先）

默认在同一个 Codex 会话内完成需求分析、设计、架构判断、实现与验证；不再为不同岗位或阶段新建其他会话。

复杂任务、多文件变更或需要拆分步骤时，使用 `/plan` 先制定计划，再在当前会话继续执行。简单任务直接实施，不强制创建计划或补充任务书。

产品、设计、架构、前端、后端与 QA 仍作为当前会话内的职责视角，用于约束范围和检查风险，不代表必须进行会话间流转。只有用户明确要求转交、任务确实需要其他责任人继续处理，或当前会话无法继续时，才使用交接格式。

本模式不降低以下硬约束：当前运行事实优先、数据安全与备份边界、已封板范围、必要的架构评审、工程验证和每日贡献记录。
