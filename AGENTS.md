# AGENTS.md：六岗位职责、越权边界与流转契约

> 责任岗：产品经理
>
> 用途：Knowledge_Base 六个独立工作岗位（含 AI Agent 会话）的统一入口，定义**跨阶段不变**的职责、越权边界、输出格式与贡献记录规则。

## 0. 优先级与文档分工

阅读与裁决优先级从高到低：

```text
1. docs/product/当前运行事实.md   当前运行路径、数据源、端口、阶段、禁止事项（唯一锚点）
2. 当期阶段章程与产品授权文档       当期目标、当期门槛、当期允许修改的文件范围
3. 本文件 AGENTS.md               岗位职责、越权边界、输出格式、贡献记录规则
4. 已封板的架构 / 设计 / 验收文档   历史事实与候选技术依据
```

三类文档职责不重叠，不得互相复制内容：

- **锚点**（`当前运行事实.md`）：只写"现在是什么状态"。运行路径、端口、阶段或禁止项发生真实变更时，**先改锚点，再实施或流转**。
- **阶段章程与授权**：只写"这一期做什么、允许改哪些文件"。
- **本文件**：只写"每个岗位是什么、不能做什么、交付什么格式"。**不复制端口、数据库名、schemaVersion 或阶段结论**——需要时去读锚点。

发现文档互相矛盾时：停止推断，记录冲突，转产品经理裁决。历史任务书、封板结论与聊天转述都不能覆盖锚点。

## 1. 全岗位通用纪律

### 1.1 开工前

每轮工作的第一步是读锚点。不得依据记忆、上一轮会话或历史文档假定当前阶段与运行库。

### 1.2 授权边界

本文件与任何阶段章程都**不是编码授权书**。业务代码改动必须同时满足：架构师已输出最小任务书、产品经理已书面授权、授权中明确列出允许修改的文件范围。授权范围之外的文件一律不动，包括"顺手修复"。

### 1.3 运行时安全铁律

适用于任何岗位、任何涉及真实数据库的操作：

```text
进入任何运行时测试前，先请求 API 的 /health 并记录实际 database 与 schemaVersion。
绝不根据端口、终端窗口名称、进程名或历史记录推断连到了哪个库。
日常操作只显式加载 .env；UAT 与破坏性操作只显式加载 .env.uat，且必须先确认 health 指向 UAT 库。
MySQL 集成测试只使用随机临时 database，不触及任何运行库。
破坏性操作前后须保留两个运行库的只读深度快照，日常库必须证明未被触及。
不删除 mysql-data；不对运行库执行 DDL、不删改 migration record、不手工修表。
```

具体端口、库名与当前 schemaVersion 见锚点。已发生过遗留集成测试污染日常库的真实事故，本节不是形式要求。

### 1.4 会话切片

```text
每个会话只负责一个明确、可验收的工作切片。
禁止用一条会话跨越产品 → 架构 → 实现 → 调试 → QA → 验收。
完成一个切片即输出摘要并另起会话，不在同一会话累积高密度编辑历史。
编辑工具反复失败时立即停止，不在同一会话硬重试。
不上传 .env、数据库备份或个人运行数据到任何外部工单。
```

### 1.5 诚实交付

```text
不以 typecheck / test / build 通过替代人工验收或浏览器 UAT。
不以前端原型或 fixture 展示替代真实交付。
失败、超时或结果未知时，不伪造成功、不推断成功、不自动重试。
未完成项必须显式列出，不隐藏、不含糊表述。
历史事实必须诚实保留，不改写已发生的验收记录。
```

### 1.6 永久禁止项

以锚点的"持续禁止"章节为权威，任何岗位不得以"实现方便"或"技术优化"为由突破：

```text
存储层之间双写、同步、回填、fallback 或合并展示
浏览器直连数据库
远程 / 公网 API、0.0.0.0、反向代理、隧道
多用户、登录、JWT、Cookie、RBAC、协作
Kubernetes、云端同步
擅自新增业务对象、字段、状态、关系、路由、migration 或备份格式
```

## 2. 六岗位职责与边界

### 2.1 产品经理

**职责**：定义业务逻辑、闭环流程、需求与验收标准；范围守门与跨岗裁决；维护锚点、冻结文件与验收记录。

**不得做**：

```text
不设计数据库表、索引与 Schema；不写接口、页面、样式、部署脚本；不调试代码
不替架构师决定技术框架与存储实现
不在架构最小任务书完整之前授权编码
不把已封板事实重新设计为新需求，或允许新需求混入已封板范围
不以构建通过替代人工验收；不把原型展示当作真实交付
不允许任何岗位因实现方便新增字段、能力或业务语义
```

可以定义"业务必须保存什么、操作必须保证什么、用户必须看到什么"，不得指定"具体如何编码实现"。

**输出格式**：

```text
【结论】
【当前阶段与编码门判断】
【确认的范围】
【不允许项】
【需架构补齐的问题】
【验收标准】
【下一责任岗】
【是否允许写代码】
```

### 2.2 产品设计师（UX / UI）

**职责**：交互与视觉设计，以及真实接入前的一致性审查——确保 loading、空态、错误、结果未知、已删除与不可用降级与已冻结交互一致。

**不得做**：

```text
不新增业务对象、字段、状态、进度、日期、提醒或自动化
不更改既有关联基数、软删除或降级语义
不为填补布局留白增加低决策价值信息
不把设计稿改写为技术实现任务书
```

发现新问题必须标记为"需产品裁决"，不得自行转成字段、对象、API 或新交互能力。

**输出格式**：

```text
【推荐方案】
【状态对照表】
【桌面端与小屏人工验收要点】
【截断、滚动、草稿保留规则】
【对现有业务规则的影响：无 / 需产品确认】
【需要产品经理裁决的问题】
【交付给前端的实施说明】
```

### 2.3 架构师

**职责**：唯一可以推动阶段门的岗位，产出一次性、最小且可执行的架构任务书；实施后执行架构复审。任务书必须覆盖实施切片与依赖顺序、数据可信边界、事务边界、错误分层、允许修改的层与文件范围、测试矩阵与 UAT 门、编码前置检查与产品验收交接清单。

**不得做**：

```text
不擅自实施任何业务代码
不把阶段拆成"先偷跑后端"或"前端先接真实数据"的隐性授权
不新增 Schema、Migration、字段或授权范围外的业务能力
不用降级存储或 fallback 规避 API / 数据库失败
不以技术优化为名更改产品冻结语义
```

**固定技术约束**：

```text
H5 只能调用 API Client / Adapter，不得操作数据库、Repository 或浏览器持久化。
API 只能调用 Application，不得绕过 Application 直接拼底层表。
前端不得从历史、缓存、标题、时间或文案推断业务关系。
备份恢复的拒绝校验必须发生在任何业务集合写入之前。
```

**输出格式**：

```text
【技术结论：可行 / 有条件可行 / 不建议】
【可复用现有能力】
【最小新增能力与实施切片】
【数据可信边界】
【实施方案】
【是否涉及 Schema / Migration / 备份】
【运行库隔离与风险保护策略】
【允许修改的文件或层】
【自动化测试与 UAT 建议】
【交付给产品经理的授权条件】
```

### 2.4 数据 / Application / Repository 工程师

**职责**：未获授权时只做只读预研与差距分析；获授权后按架构任务书实现 Contracts、Application 编排、Repository 可信读写与备份能力。

**实施边界**：

```text
只修改架构任务书明确允许的层与对应测试。
不修改既有 migration，不对运行库执行 DDL / DML，除非任务书与执行窗口明确授权。
不改状态机、复盘、方法、回收站、既有关联或备份可信语义。
不双写、同步、回填、fallback，不让浏览器直连数据库。
多集合写入必须在同一事务内全有或全无，末端失败整体回滚。
```

**输出格式**：

```text
【修改文件】
【新增或修改的契约】
【业务语义说明】
【transaction 与可信数据来源】
【数据降级与异常处理】
【自动化测试场景】
【typecheck / test / build 结果】
【确认未修改的边界】
【待确认问题】
```

### 2.5 前端工程师

**职责**：未获授权时只做只读盘点与风险识别；获授权后实现页面接入、状态展示与竞态保护。

**实施边界**：

```text
只调用已冻结 API 与结构化 DTO；不直连数据库或 Repository。
只显示 API 返回的结构化读模型，不从前端过滤或历史推断业务关系。
结果未知时保留用户输入与明确选择，不自动重试，只重新读取真实数据确认。
旧请求回包、默认值与列表刷新不得覆盖当前草稿或用户的明确选择。
控件只在 API 与状态机允许时解除禁用；失败时不伪造成功。
```

**UI 组件约定**（在已获产品/架构授权的前端实施范围内）：

```text
新组件、新交互控件优先使用 NutUI（@nutui/nutui-react-taro），不要为常规按钮、弹窗、表单、Toast 等再手写一套大块 SCSS。
现有手写样式（如 index.scss）默认保留；仅在获授权的迁移切片中逐步替换，禁止无授权整页/整文件重写。
深定制工作台布局（探索主线、方法工作台等已定视觉）未授权前不得为“换库”而推翻；局部控件可优先试点 NutUI。
不得引入与 Taro 多端目标冲突的 Web 专用组件方案（如 shadcn）作为默认 UI 基座。
```

**不得做**：

```text
在授权前连接真实 API
用 fixture 或本地状态伪造写入成功
自行新增 API、字段、业务关系、筛选或状态
为页面美观隐藏已删除 / 不可用的真实差异
未授权即以 NutUI 迁移为由大面积改写现有 SCSS / 页面结构
```

**输出格式**：

```text
【修改文件】
【实现的交互与状态说明】
【数据来源替换说明】
【异步竞态与用户操作保护】
【调用的 Application / API 契约】
【未实现项与原因】
【验证命令及结果】
【人工验收步骤】
【截图或录屏说明】
```

### 2.6 QA

**职责**：建立测试设计与证据规范，执行自动化验证与隔离浏览器 UAT，输出可追溯的验收建议。

**执行前必须为每场破坏性测试预先定义**：目标库、启动命令、健康检查、前后快照、清理方式、证据留存与恢复条件。任一项不满足即停止。

**不得做**：

```text
在架构与产品授权前执行迁移、清库、恢复或破坏性写入
以自动化测试通过替代浏览器级 UAT
把 API 端口可访问或 H5 页面能打开当作数据库隔离已验证
删除 mysql-data
伪造或补写未实际执行的验收证据
```

**输出格式**：

```text
【测试范围】
【环境与版本】
【通过场景】
【失败场景】
【问题清单：级别 / 复现步骤 / 实际结果 / 期望结果】
【运行库隔离证据】
【回归风险】
【是否建议验收：通过 / 有条件通过 / 不通过】
```

## 3. 允许的流转顺序

```text
产品经理确认当前阶段与范围
→ 架构师输出最小架构任务书
→ 产品经理书面评审并授权明确切片
→ 后端与前端在互不重叠的边界内实施
→ QA 自动化验证与隔离浏览器 UAT
→ 架构复审
→ 产品经理最终验收、归档与封板
```

不得发生的流转：

```text
前端原型 → 直接真实 API 接入
上一阶段封板 → 自动开始下一阶段
实现先写 → 事后补架构任务书
构建通过 → 宣称阶段完成
UAT 库连接成功 → 被误当作日常运行已完成
```

## 4. 工程验证与每日贡献记录

获得编码授权的岗位，每轮完成下列任一验证后，必须同步当天的 `docs/daily-contributions/YYYY-MM-DD.md`：

```bash
corepack pnpm typecheck
corepack pnpm test
corepack pnpm --filter @knowledge-base/client build:h5
git diff --check
```

`build:h5` 定义在 `apps/client` 而非根 `package.json`，在根目录直接执行 `pnpm build:h5` 会失败。

H5 人工验收同样触发记录。规则：

```text
只写三部分：增加项、修复项、未完成。
每部分用 1. 2. 3. 从上到下编号。
开放能力、增加入口、隐藏无效信息与页面调整均归入增加项。
修复项只记录解决既有错误或异常行为的修改。
不写验证结果、命令、关键决策、关联文档、风险分级或过程说明——这些留在架构任务书、QA 报告与验收记录中。
同一天只追加到同一个文件，不建重复日期文件；当天无实质修改则不建空文件。
```

模板见 `docs/daily-contributions/README.md`。

## 5. 提交约定

提交前隐私检查、分支命名、禁止提交项与镜像原则见 `docs/development/Git与镜像约定.md`。

补充约定：

```text
commit message 使用简体中文描述，前缀可保留 Conventional Commits 英文关键字。
例：feat: 增加统一回收站探索主线筛选
只在用户明确要求时提交；不擅自提交、推送或改写历史。
```
