# 开发协作原则

这个项目的目标是把事情做对、做完，而不是生产流程和文档。用户提出的需求只要足够清楚，就直接检查代码、实现、验证并说明结果，不需要先扮演产品经理、架构师或 QA，也不需要为了开工补任务书和授权文件。

## 什么时候需要停下来

只有继续执行可能明显做错产品、破坏真实数据、改写 Git 历史、发布到外部或产生费用时，才向用户确认。普通的 Schema、认证、API 和前端联动属于正常开发，可以一次完成。不要把技术上能判断的问题变成审批问题，也不要借当前需求扩大业务范围。

## 文档怎么写

普通功能默认不写过程文档。确实需要留下长期决定或运行记录时，在 `docs/tasks/YYYY-MM-DD-任务名称/` 放一份 `README.md`；日期使用任务首次创建日期。

文档是给人读和审核的。开头先说要解决什么问题和最终会变成什么样，正文必须讲清主要任务、需要改动的位置、每项改动的原因以及实施注意事项，最后说明当前进度。信息不能为了简短而省掉，也不能拆成审批表或模板填空；涉及多处改动时可以用一张简洁表格建立“改哪里—怎么改—为什么”的对应关系。旧文档不用为了形式批量重写。

## 真实环境安全

涉及运行服务、数据库、迁移或部署时，先读 `docs/product/当前运行事实.md`。操作真实 API 或数据库前必须通过 `/health` 确认实际数据库和 Schema 版本，不能靠端口或记忆猜测。日常环境使用 `.env`，UAT 使用 `.env.uat`；MySQL 自动化测试使用随机临时库。

不要删除 `mysql-data`，不要手工篡改 `schema_migrations`，不要修改已经应用的 Migration。即使开发数据可以丢弃，清库、重建或迁移前也要确认准确目标。不得输出或提交密码、Token、私有 `.env`、备份和个人数据。

## 代码和验证

H5 通过 API Client 访问 API，API 通过 Application 组织业务，Repository 负责数据库读写。多表写入需要事务；不引入双写、回填或失败时偷偷切换存储。前端不从样式、文案或缓存猜测服务端事实，写入结果未知时通过可信读取确认，不自动重复提交。

验证强度与风险匹配：小改动跑定向测试，跨层或 Schema 修改补相关集成测试和 typecheck，涉及 H5 时再构建 H5。失败、超时和未执行的验证都如实说明。

保留用户已有改动。只有用户明确要求时才提交、推送或改写 Git 历史；提交前检查私密配置、数据库文件、备份和构建产物没有进入暂存区。
